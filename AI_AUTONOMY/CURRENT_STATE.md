# CURRENT_STATE — Ordía

> Fotografía ACTUAL del estado de Ordía. No es un historial; el historial está en `RUN_LOG.md`.
> Actualizar AL FINAL de cada sesión autónoma (reescribir, no acumular).

## Modo continuo (supervisor persistente)

- **Arquitectura de continuidad real**: `tools/ordia_supervisor.py` (+ `ordia_supervisor.sh`,
  `SUPERVISOR.md`). Un proceso persistente en una máquina siempre encendida del usuario orquesta
  la Automation `Ordía Continuous Evolution` (id `b3bd3870-…`), garantiza `MAX_CONCURRENT_RUNS=1`
  y encadena runs en ~15–40 s (no horas). Deshabilita el cron al arrancar y lo rehabilita al parar.
- **Sin supervisor**: el cron cada 15 min es modo degradado. **Con supervisor**: continuidad de
  segundos, 1 agente. Ver `tools/SUPERVISOR.md`.

## Estado

- **Fecha (UTC)**: 2026-08-14 (ciclo 103; fix P1 parser: fracciones compuestas plurales `"+ tres cuartos"` y `"+ y cuarto"` sobre multi-cuarto. `compoundFractionalRelativePattern` (c.94b) solo admitía `media|un cuarto|cuarto` como fracción final, NO los plurales "tres cuartos"/"dos cuartos", así `"en una hora y tres cuartos"` (105 min) caía a `relativePattern` que robaba solo "en una hora" (+60) dejando "y tres cuartos" como residuo en el título (agendaba 45 min antes). Y `multiQuarterRelativePattern` robaba solo "en tres cuartos de hora" (+45) sin consumir "+ y cuarto" (quedaba "llamar y cuarto", +45 en vez de +60). Fix (A) el compuesto añade `tres\s+cuartos|dos\s+cuartos` + resolver (45/30/30/15) min; (B) el multi-cuarto añade sufijo opcional `(?:\s+y\s+cuarto)?` + +1 cuarto extra. Reutilizan TODO el flujo existente. +4 tests; **709 domain tests PASS** (705 base c.102 + 4 c.103); smoke 25 OK; sin regresión "en una hora y media"=+90/"en una hora y cuarto"=+75/"en tres cuartos de hora"=+45 intactos; NO VERIFICADO gradle/Android/UI/Room. STALE_RUN reconstruido seguro: base local 84f634c obsoleta (remoto avanzó a 0285a20, c.102 "a última hora" de otra ejecución) → stash + fast-forward + pop + resolución conflicto docs; mi trabajo renombrado c.102→c.103. + c.102 fix P1 parser: "a última hora" (fin de jornada ~18:00) no se interpretaba como hora canónica → tarea SIN vencimiento o agendada a 09:00 (mañana) Y "a última hora" como residuo en el título. Asimetría con "a primera hora" (09:00) que SÍ funcionaba. Además "última hora" sin conector "a" (con tilde) NO casaba: el `\b` inicial ASCII-only no reconoce "ú" como carácter de palabra → `dueAt=null` silencioso. Fix: nuevo `ultimaHoraPattern` con lookbehind Unicode-safe `(?<![a-záéíóúñ])` + `ultimaHoraTime=18:00`, simétrico a `primeraHoraPattern`; match + resolución de respaldo + limpieza del título; si hay parte del día explícita ("de la tarde") ésta tiene prioridad. + fix "a mediodía"/"a medianoche" (sin "la") dejan residuo "a" en el título → patterns aceptan `a\s+`. +8 tests TDD RED→GREEN. **705 domain tests PASS** (697 base c.101 + 8); smoke 25 OK; probe 9 casos verde sin falsos positivos; NO VERIFICADO gradle/Android/UI/Room. + c.101 fix P2 parser: `"en media hora y cuarto"`/`"en un cuarto de hora y cuarto"` (dos fracciones sumadas sin número entero) no se parseaban como punto relativo: `fractionalRelativePattern` robaba solo "en media hora" (+30 min) y dejaba "y cuarto" como residuo en el título ("cita en media hora y cuarto" → título "cita y cuarto", vencimiento 12:30 en vez de 12:45). La cita se agendaba 15 min antes de lo pedido y con título sucio. Asimetría: "en una hora y cuarto" (75 min, c.94b) sí funcionaba pero la fracción sin número entero no. Fix: nuevo `fractionalAndQuarterRelativePattern = prefijo + (media hora|(un )?cuarto (de )?hora) + "y cuarto"` → base (30 si "media" | 15 si "cuarto") + 15 min; se procesa ANTES que `fractionalRelativePattern` (roba la frase completa, sin residuo) y antes de la duración. Incluido en `effectiveRelativeDueAt` (prioridad sobre `fractionalRelativeDueAt`) y `relativeIsDays=false` (sub-hora). +4 tests; **697 domain tests PASS**; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room. + c.100 fix P2 parser: `"cada fin de semana"` (forma larga + "cada") caía a `rec=NONE` (fecha única) en vez de hábito WEEKLY 6,7 — el usuario pedía un hábito de fin de semana y la tarea aparecía una sola vez, el recordatorio no repetía. `weekendPattern` (detección temprana de fecha) consumía "cada fin de semana" ANTES de `parseRecurrence` y `weekendRecurrencePattern` (hábito) solo casaba "fines de semana"/"findes" (apócope), no la forma larga con "cada". Asimetría: "cada finde" (apócope) SÍ era hábito, "cada fin de semana" no. Fix: (1) `weekendPattern` gana lookbehind negativo `(?<!cada\s)(?<!los\s)` en la rama `fin|finales de semana` para no consumir el hábito (llega intacto a `parseRecurrence`); (2) `weekendRecurrencePattern` añade alternancia `cada\s+fin\s+de\s+semana\b`. Reutiliza TODO el flujo (`detectWeekInterval`, WEEKLY 6,7, primera ocurrencia próximo sábado). +1 test; **686 domain tests PASS**; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room. + c.99 fix P1 parser: `"este finde"` (apócope singular) se capturaba como recurrencia semanal WEEKLY en vez de fecha única (over-recurrence: la tarea nunca dejaba de reaparecer); `weekendPattern` (fecha) acepta "finde" singular con lookbehind negativo y `weekendRecurrencePattern` (hábito) pierde la alternancia `este\s+`. +4 tests; **685 domain tests PASS**; smoke 25 OK. + c.98 fix P1 parser: `"el día N"` (forma cotidiana de día de mes suelto con la palabra "día") NO se parseaba → cita olvidada (`dueAt=null`) o título corrupto ("reunión el día"). `dayOfMonthPattern`/`nextMonthDayPattern`/`monthlyDayPattern`/`monthNamePattern` solo esperaban "el N" (artículo), no "el día N" (artículo + palabra "día"); sin consumir "día" el grupo de día no casaba → `dueAt=null`, y cuando otra rama capturaba la fecha sin consumir "día" quedaba como residuo en el título. Asimetría flagrante: "el 30" funcionaba (c.68/c.70), "el día 30" no. Fix: los cuatro patrones añaden `(?:d[ií]a\s+)?` opcional antes del grupo de día (casa con y sin tilde); reusan TODO el flujo existente (fecha, hora, recurrencia mensual, clamp fin-de-mes, roll si pasado). "el día N" → misma fecha que "el N" + título limpio. +6 tests; **681 domain tests PASS**; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room. + c.97 fix P1 parser: `"cada dos semanas los lunes"` (intervalo ESCRITO + lista de días) se agendaba al DOBLE de frecuencia (WEEKLY+1 en vez de WEEKLY+2) Y dejaba "cada dos semanas" como residuo en el título. La persona pedía rutina quincenal y recibía semanal. Asimetría flagrante: la forma con dígitos `"cada 2 semanas los lunes"` ya funcionaba (c.54) y el intervalo escrito sin días `"cada dos semanas"` también (c.57 vía `intervalPattern`), pero la combinación escrita+días caía en el bug: `parseRecurrence` procesa la rama de lista de días ANTES que `intervalPattern`, y su helper `detectWeekInterval()` solo reconocía `\d{1,3}` para "cada N semanas", NO números escritos → devolvía null → interval por defecto 1 (semanal). Fix: `detectWeekInterval()` regex ahora admite `(\d{1,3}|$writtenNumberGroup)` + `parseWrittenNumber` reutilizado; `writtenNumberGroup` se trasladó al inicio de `parseRecurrence` para que el helper (que aparece antes que `intervalPattern`) pueda usarlo. Así "cada dos semanas los lunes"→WEEKLY+2 (simétrico a dígitos) y la frase se consume → título limpio. +4 tests; **675 domain tests PASS**; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room. + c.96 fix P1 parser: `"a la una"` (hora 1, femenino singular) no se parseaba → cita sin vencimiento + título sucio. La hora 1 se dice "a la una" (no "a las 1"), con conector "a la" en vez de "a las"; el patrón `a las N` excluía `un/una/uno` de `WRITTEN_HOUR_ALT` justamente para no confundirlo, así que `"reunión a la una"` caía sin `dueAt` y "a la una" quedaba como residuo en el título → la cita se olvidaba. Además `"a la una del mediodía"` caía a la canónica NOON (12:00) en vez de 13:00. Fix: (1) nuevo patrón `a la una` al frente de `timePatterns` con mismo layout de grupos que `a las N` (hora/:MM/y media|cuarto/meridiem/horas) → `explicitTimeData` lo procesa sin ramificación; (2) el meridiem `del mediodía` se añade a la alternación de ambos patrones y `isPm` reconoce `delmediodía`/`delmediodia` (PM, hora 1→13); (3) la rama NOON/MIDNIGHT de `explicitTimeData` se protege: solo aplica a frases puras "al mediodía"/"a la medianoche" (sin hora ni minutos capturados — grupos 1/2 vacíos), para que "a la una del mediodía" NO serobe como NOON y caiga a la rama genérica de hora. +6 tests; **671 domain tests PASS**; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room. + c.94 fix P1 parser "en media hora"/"en un cuarto de hora"/"dentro de media hora"/"de aquí a media hora" se parseaban como DURACIÓN (sin vencimiento) en vez de punto en el tiempo (+30/+15 min): `relativePattern` solo aceptaba números escritos enteros o dígitos, NO las fracciones cotidianas "media hora"/"cuarto de hora" → caían a `fractionalDurationPattern` → `dueAt=null`, `durationMinutes=30/15` y el prefijo "en" quedaba como residuo en el título ("llamar en media hora" → "llamar en"). La tarea quedaba SIN vencimiento → invisible en What Now/planificador, recordatorio imposible. Asimetría: "en treinta minutos"/"en una hora" sí eran fecha relativa. Fix: nuevo `fractionalRelativePattern` (prefijo `en/dentro de/de aquí a/de acá a` + `media hora`/`(un) cuarto de hora`) simétrico a `relativePattern`, procesado ANTES que la duración para no ser robado. `fractionalRelativeDueAt = now+(30|15)min`, frase completa consumida → título limpio. El prefijo obligatorio preserva la duración real ("reunión media hora" sin prefijo sigue siendo `durationMinutes=30`) y no choca con el recordatorio ("media hora antes"). +7 tests; **655 domain tests PASS**; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room. + c.93 fix P1 parser recurrencias adjetivas (mensual/semanal/anual/quincenal/bimestral/trimestral/semestral) → ya no caen a NONE; + c.92 fix P1 parser "cierre de mes"/"cierre del mes" → dueAt (vencimiento); + + c.99b fix P1 parser: "este finde"  + c.99a fix P1 parser: números escritos >30 y compuestos (run paralelo): números escritos > 30 y compuestos no se parseaban → tarea SIN vencimiento (olvidada). `"llamar en cuarenta y cinco minutos"`, `"en cincuenta minutos"`, `"en sesenta minutos"`, `"en veinticinco minutos"`, `"en treinta y cinco minutos"` caían a `dueAt=null` (y a veces `durationMinutes=5` por la unidad baja "cinco" que rezumaba como duración). Causa raíz: las listas de palabras admitidas estaban acotadas a 1-30 y DUPLICADAS en 6 patrones (`relativePattern`, `compoundFractionalRelativePattern`, `multiQuarterRelativePattern`, `agoPattern`, `writtenAmountPattern`/`reminderPatterns`/`writtenDurationPattern`, y `writtenNumberGroup` local de `parseRecurrence`). `parseWrittenNumber` solo mapeaba 1-21 y 30. Así "cuarenta"/"cincuenta"/"sesenta" no casaban, y la forma compuesta estándar del español "cuarenta y cinco" tampoco. La persona decía "llamar en cuarenta y cinco minutos" y Ordía agendaba una tarea SIN fecha → invisible en What Now/planificador, sin recordatorio posible. Asimetría flagrante: "en 45 minutos" (dígitos) sí funcionaba. Fix: (1) un ÚNICO `writtenNumberGroup` (fragmento regex, 1-99, formas compuestas "treinta y cinco"/"veinte y dos" + veintidós-veintinueve + decenas treinta-noventa) declarado al inicio del object y reutilizado por TODOS los patrones que antes duplicaban la lista → se elimina la duplicación (menos código, consistencia: el bug no puede reaparecer en un patrón olvidado); (2) `parseWrittenNumber` reescrito con un `wordToNumber` map (1-99) + resolución de compuesto "decena y unidad" (left ∈ decenas redondas {20..90}, right ∈ 1..9 → l+r). +7 tests (compuesto, decenas, no-leak de unidad baja, veinticinco, compuesto 35/75, duración compuesta, recordatorio compuesto). **688 domain tests PASS**; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room. + c.98 fix P1 parser: `"el día N"` (forma cotidiana de día de mes suelto con la palabra "día") NO se parseaba → cita olvidada (`dueAt=null`) o título corrupto ("reunión el día"). `dayOfMonthPattern`/`nextMonthDayPattern`/`monthlyDayPattern`/`monthNamePattern` solo esperaban "el N" (artículo), no "el día N" (artículo + palabra "día"); sin consumir "día" el grupo de día no casaba → `dueAt=null`, y cuando otra rama capturaba la fecha sin consumir "día" quedaba como residuo en el título. Asimetría flagrante: "el 30" funcionaba (c.68/c.70), "el día 30" no. Fix: los cuatro patrones añaden `(?:d[ií]a\s+)?` opcional antes del grupo de día (casa con y sin tilde); reusan TODO el flujo existente (fecha, hora, recurrencia mensual, clamp fin-de-mes, roll si pasado). "el día N" → misma fecha que "el N" + título limpio. +6 tests; **681 domain tests PASS**; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room. + c.97 fix P1 parser: `"cada dos semanas los lunes"` (intervalo ESCRITO + lista de días) se agendaba al DOBLE de frecuencia (WEEKLY+1 en vez de WEEKLY+2) Y dejaba "cada dos semanas" como residuo en el título. La persona pedía rutina quincenal y recibía semanal. Asimetría flagrante: la forma con dígitos `"cada 2 semanas los lunes"` ya funcionaba (c.54) y el intervalo escrito sin días `"cada dos semanas"` también (c.57 vía `intervalPattern`), pero la combinación escrita+días caía en el bug: `parseRecurrence` procesa la rama de lista de días ANTES que `intervalPattern`, y su helper `detectWeekInterval()` solo reconocía `\d{1,3}` para "cada N semanas", NO números escritos → devolvía null → interval por defecto 1 (semanal). Fix: `detectWeekInterval()` regex ahora admite `(\d{1,3}|$writtenNumberGroup)` + `parseWrittenNumber` reutilizado; `writtenNumberGroup` se trasladó al inicio de `parseRecurrence` para que el helper (que aparece antes que `intervalPattern`) pueda usarlo. Así "cada dos semanas los lunes"→WEEKLY+2 (simétrico a dígitos) y la frase se consume → título limpio. +4 tests; **675 domain tests PASS**; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room. + c.96 fix P1 parser: `"a la una"` (hora 1, femenino singular) no se parseaba → cita sin vencimiento + título sucio. La hora 1 se dice "a la una" (no "a las 1"), con conector "a la" en vez de "a las"; el patrón `a las N` excluía `un/una/uno` de `WRITTEN_HOUR_ALT` justamente para no confundirlo, así que `"reunión a la una"` caía sin `dueAt` y "a la una" quedaba como residuo en el título → la cita se olvidaba. Además `"a la una del mediodía"` caía a la canónica NOON (12:00) en vez de 13:00. Fix: (1) nuevo patrón `a la una` al frente de `timePatterns` con mismo layout de grupos que `a las N` (hora/:MM/y media|cuarto/meridiem/horas) → `explicitTimeData` lo procesa sin ramificación; (2) el meridiem `del mediodía` se añade a la alternación de ambos patrones y `isPm` reconoce `delmediodía`/`delmediodia` (PM, hora 1→13); (3) la rama NOON/MIDNIGHT de `explicitTimeData` se protege: solo aplica a frases puras "al mediodía"/"a la medianoche" (sin hora ni minutos capturados — grupos 1/2 vacíos), para que "a la una del mediodía" NO serobe como NOON y caiga a la rama genérica de hora. +6 tests; **671 domain tests PASS**; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room. + c.94 fix P1 parser "en media hora"/"en un cuarto de hora"/"dentro de media hora"/"de aquí a media hora" se parseaban como DURACIÓN (sin vencimiento) en vez de punto en el tiempo (+30/+15 min): `relativePattern` solo aceptaba números escritos enteros o dígitos, NO las fracciones cotidianas "media hora"/"cuarto de hora" → caían a `fractionalDurationPattern` → `dueAt=null`, `durationMinutes=30/15` y el prefijo "en" quedaba como residuo en el título ("llamar en media hora" → "llamar en"). La tarea quedaba SIN vencimiento → invisible en What Now/planificador, recordatorio imposible. Asimetría: "en treinta minutos"/"en una hora" sí eran fecha relativa. Fix: nuevo `fractionalRelativePattern` (prefijo `en/dentro de/de aquí a/de acá a` + `media hora`/`(un) cuarto de hora`) simétrico a `relativePattern`, procesado ANTES que la duración para no ser robado. `fractionalRelativeDueAt = now+(30|15)min`, frase completa consumida → título limpio. El prefijo obligatorio preserva la duración real ("reunión media hora" sin prefijo sigue siendo `durationMinutes=30`) y no choca con el recordatorio ("media hora antes"). +7 tests; **655 domain tests PASS**; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room. + c.93 fix P1 parser recurrencias adjetivas (mensual/semanal/anual/quincenal/bimestral/trimestral/semestral) → ya no caen a NONE; + c.92 fix P1 parser "cierre de mes"/"cierre del mes" → dueAt (vencimiento); + + c.98 fix P1 parser: "el día N"
- **Fecha (UTC)**: 2026-08-14 (ciclo 93; fix P1 parser: recurrencias en forma ADJETIVA ("pago mensual"/"reunión semanal"/"renta anual"/"reporte quincenal"/"factura bimestral"/"impuesto trimestral"/"cierre semestral") caían a `rec=NONE` (compromiso periódico olvidado): solo se reconocía el adverbio "-mente" (mensualmente/semanalmente/...) y, para quincenal, las frases "cada quincena"/"quincenalmente"; el adjetivo suelto "quincenal"/"bimestral"/"trimestral"/"semestral" no casaba ninguna rama → la tarea nacía SIN cadencia (recordatorio disparaba una vez y nunca más, invisible en planificador de hábitos). Sutil: la fecha explícita sí se conservaba ("pago mensual el 10" → dueAt=día 10, rec=NONE) así la persona creía la recurrencia puesta. Solución: (1) bloque quincenal `quincenalmente`→`quincenal(?:mente)?` captura el adjetivo (WEEKLY+2); (2) nuevo bloque plurimensual antes de fixedPatterns: `bimestral`→MONTHLY+2, `trimestral`→MONTHLY+3, `semestral`→MONTHLY+6 (reutiliza RecurrenceEngine.plusMonths); (3) fixedPatterns añade `\bsemanal\b`(WEEKLY), `\bmensual\b`(MONTHLY), `\banual\b`(YEARLY) — adjetivos de intervalo 1; límites \b evitan colisión con "mensualmente" y "manual". Sin enum/migración/pantalla nueva. +8 tests; 649 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room; + c.92 fix P1 parser "cierre de mes"/"cierre del mes" no se parseaba → dueAt=null (vencimiento olvidado): `endOfMonthPattern` solo reconocía `fin`/`finales`/`último día` (c.91), no el sinónimo financiero "cierre de mes"/"cierre del mes". Probe JVM confirmó RED (`due=null`) antes y GREEN (`due=07-31`) tras fix. Fix: alternativa `cierre` en la regex + `monthBaseForBoundary` reconoce `cierre` como "end"; reusa todo el flujo de `fin de mes` (modificador "que viene"/"próximo" ya resuelto en `isNext`). +4 tests; **641 domain tests PASS**; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room. + c.91 fix P1 parser "último día del mes" no se parseaba → dueAt=null (vencimiento olvidado): `endOfMonthPattern` solo reconocía `fin`/`finales`, no el sinónimo cotidiano "último día del mes"; además el `\b` inicial (ASCII-only) no casaba antes de la `ú` no-ASCII — trampa sutil: añadir la alternativa sola no funcionaba. Fix: alternativa `[uú]ltim[oa]\s+d[ií]a` + `(?<!\p{L})` (lookbehind Unicode-safe); `monthBaseForBoundary` reconoce `últim`/`ultim` como "end". Reusa todo el flujo de `fin de mes`. +5 tests; **637 domain tests PASS**; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room. + c.88 fix P1 parser "próximo" sin tilde dicho hoy=ese día → HOY en vez de +7; c.87 feat P2 búsqueda: `SearchEngine` reconoce **"semana que viene"/"próxima semana"** como nuevo `DateScope.NEXT_WEEK` → filtra tareas de lunes-domingo de la semana próxima (antes "semana que viene" caía a `THIS_WEEK` por `WEEK_TOKENS` sin distinguir modificador, mostrando la semana actual en vez de la próxima — la distinción planificar-próxima-semana se perdía). Detección: `NEXT_WEEK_TOKENS = {proxima, proximas, viene}`; rama `hasWeek && hasNext → NEXT_WEEK` antes de `hasWeek → THIS_WEEK`. Rango: `startNextWeek = today + daysToSunday + 1` (lunes próximo), `endNextWeek = startNextWeek + 6` (domingo próximo); reusa la misma fórmula `(7 - dayOfWeek) % 7` del fix de domingo del c.86. Sin nueva pantalla/botón (reuso el `SearchEngine` existente — recuperación de información). "esta semana"/"semana" sola siguen siendo `THIS_WEEK` (no-regresión). +5 tests; 617 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room; + c.86 fix P1 búsqueda Sunday-boundary `SearchEngine` + refactor P2 DRY `timeRank` TaskRules/WhatNowEngine (detallado abajo); + c.85 feat P1 parser: duraciones con número escrito "dos horas"/"treinta minutos"/"un par de horas" → durationMinutes real, antes null; c.84 asistente "tareas rápidas" alineado a ranking What Now; c.83 "¿Qué hago ahora?" delega en `WhatNowEngine.suggest` (motor honesto de What Now) y **explica por qué** esa tarea va primero ("está vencida"/"empieza enseguida"/"vence hoy"/"es urgente"… vía nuevo `WhatNowEngine.reasonLabel`) + avisa cuántas hay vencidas ("Además, tienes N vencidas"). El "plan mínimo" reemplaza su ordenador divergente (`priority`→`dueAt`) por `WhatNowEngine.ordered` —misma ordenación que What Now/widget—, así las tres superficies (What Now, asistente, widget) muestran el mismo orden. Antes el asistente usaba `TaskRules.nextBestTask` (ya time-aware desde c.45) pero NO explicaba la razón ni mencionaba vencidas, y el plan mínimo tenía un comparador aparte que divergía del de What Now (p.ej. vencida de prioridad normal vs urgente no vencida: What Now = vencida por rango, plan mínimo viejo = urgente por prioridad). Refactor DRY: `WhatNowEngine.suggest` ahora reusa el nuevo `ordered(...)` (fuente única del ranking, antes el comparador estaba duplicado). Sin nueva pantalla/botón. +2 tests (`whatNow_explainsWhyAndMentionsOverdue`, `planMinimo_ranksOverdueFirst`); 602 domain tests PASS (assistant compilado/ejecutado aparte: 5 AssistantEngineTest PASS); smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room; + c.82 fix P1 parser rango horario con meridiem solo en el INICIO "de 6pm a 8"/"de 2pm a 4"/"de 6 de la tarde a 8": el fin bare NO heredaba el PM del inicio → `endHr=8 < startHr=18` violaba la guarda `endMin>startMin` → rango rechazado, `durationMinutes=null` Y título sucio ("Reunión de a 8"); ahora `startPmEffective` propaga el PM del inicio al fin bare (mismo lado del mediodía) → 18:00→20:00, dur 120, con anti-falso-positivo (`midnightWrap` solo si el fin NO va seguido de un sustantivo de cantidad "de 2pm a 4 entradas") y cruce inverso "de 11pm a 1" (inicio PM, fin bare con `endHr<startHr` → NO hereda PM, envuelve +24h → 23:00→01:00, dur 120); unifica en un único bloque `rangeMatch` que cubre mismo-día, medianoche y el simétrico cruce de mediodía, reemplazando el wrap ad hoc de c.80; +12 tests, 593 domain tests PASS; + c.81 feat P2 búsqueda semántica por fecha en `SearchEngine` ("hoy"/"mañana"/"esta semana"/"atrasadas/vencidas" filtran por `dueAt` no por texto; +9 tests, 597 domain tests PASS); + c.80 fix P1 parser cruce de medianoche "Cena de 10 de la noche a 1 de la madrugada": `rangeMatch` exigía `endMin > startMin` → overnight rechazado y `durationMinutes=null`; ahora `sameDay` vs overnight con `rawDuration = if (sameDay) end−start else end+24h−start`, clamp 5..24h, cruce solo con señal clara; +4 tests, 588 domain tests PASS; + c.79 fix P1 parser cruce del mediodía en rango horario con meridiem solo al final "de 12 a 2 de la tarde"/"de 11 a 1 de la tarde": (1) la duración se computaba con horas crudas del texto (2−12=−600) y coerceIn dejaba 5 min en vez de 120; (2) la propagación PM del c.76 era ciega → "de 11 a 1 de la tarde" convertía el inicio 11→23 (dueAt=23:00); ahora la duración usa horas absolutas resueltas (sAbs/eAbs) y la propagación PM es condicional `startHr <= endHr` (mismo lado del mediodía) aplicada simétricamente a rangeMatch/rangeStartTime; +6 tests, 584 domain tests PASS; + c.78 fix P1 parser rango horario con meridiem compacto solo en el extremo final "de 6 a 8 pm"/"de 9 a 11 am": el `explicitTime` capturaba el extremo final "8 pm"→20:00 y sombreaba `rangeStartTime` (18:00) → dueAt caía a la hora de FIN (20:00), no de inicio (18:00); recordatorio 2h tarde; ahora detección posicional `explicitTimeIsRangeEnd` deja ganar a `rangeStartTime` (inicio, con propagación PM del c.76) → 18:00, duración correcta 120; horas sueltas "Llamada 8pm"→20:00 sin regresión; + c.77 feat P1 resumen: el veredicto del día `DayLoad` ahora usa la ventana de jornada APRENDIDA (`LearningProfile`) en vez de la fija 9–18 → la tarjeta de hoy deja de mentir para horarios no estándar (jornada 9–23 a las 17:00 = ON_TRACK no OVERLOADED; jornada 6–14 a las 13:00 = OVERLOADED no ON_TRACK); `SummaryEngine.summarize`/`assessDayLoad` reciben `dayStartMinute`/`dayEndMinute` (defaults 9–18) + sobrecarga con `LearningProfile?`; `TodayScreen` computa el perfil igual que `PlannerScreen` y lo pasa; +4 tests; + c.76 fix P1 parser rango horario "de 6 a 8 de la tarde": el meridiem PM del extremo final no se propagaba al inicio sin meridiem → agendaba 06:00 en vez de 18:00 (recordatorio 12h antes de la cita real); ahora el inicio bare hereda el PM del extremo final "de la tarde/noche" → 18:00, duración correcta 120; + c.75 fix parser "fin/mediados/principios del mes que viene/próximo" ancla al mes siguiente (no al actual) + fix "antepasado mañana" → +3 días y título limpio; + c.74 fix P1 RecurrenceEngine anual anclado a 29 de febrero: `plusYears(1)` clampaba a 28/2 y derivaba el ancla para siempre (cumpleaños/aniversario bisiesto perdido) → ahora `nextYearly` salta años no bisiestos, simétrico al `nextMonthly` 31 de c.18; + c.73 fix parser "la semana que viene el lunes/viernes" → día objetivo de la semana próxima, no +7d genérico; + c.72 fix parser "jueves que viene" dicho en jueves -> proxima semana (no HOY); + c.71 fix parser orden inverso "el mes que viene el N" agenda día N del mes siguiente; + c.70 fix P0 crash parser día fuera de rango "el 0/99 de mes" + fix P1 "el 15 de agosto del 2027" no capturaba año "del" → fecha errónea 2026↔2027 y título "del 2027" huérfano; + c.69 feat resumen accionable: un toque pospone la tarea sugerida a mañana); c.90 fix P1 parser: horas escritas "a las nueve"/"diez de la noche"/"doce de la noche"/"ocho y media"/"nueve y cuarto"/"dos de la tarde" -> NaturalTaskParser ahora resuelve numeros escritos (uno-doce, cero) como hora, antes solo los digitos 0-12. Antes "Cita a las diez de la noche" no casaba patron de hora (solo digito) -> la hora quedaba en el titulo Y/o se agendaba a la canonica de la parte del dia ("doce de la noche"->21:00 en vez de 00:00 medianoche). Solucion: parseHour helper que mapea token escrito->1-12 + WRITTEN_HOUR_ALT en timePatterns y patron standalone de parte-del-dia; resolucion AM/PM identica a la de digitos (madrugada/manana AM, tarde/noche PM, 12 de la noche=medianoche, 12 del mediodia=12:00). +11 tests; 632 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room; + c.89 fix P1 parser asimetría de acentos en bare-plural `"sabados"` sin tilde: `"fútbol sabados"` caía en `recurrence=NONE` y "sabados" quedaba en el título → la rutina se olvidaba (pérdida de datos). La regex `dayListPattern` ya casa `s[aá]bados?` (ambas formas), pero el guard `barePluralSingle` solo comprobaba `g.contains("sábados")` con tilde; la forma sin tilde pasaba el guard como no-plural y no se reconocía como recurrencia de un solo día bare. Simétrico al c.88 ("próximo" sin tilde) y al c.41 (que añadió los plurales a la regex pero no al guard). Fix: `g.contains("sábados") || g.contains("sabados") || g.contains("domingos")`. +2 tests; **622 domain tests PASS**; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room. Auditoría de acentos completada (TASK-3): los 7 `contains`/`==` acento-sensibles del parser están balanceados — `mediodía`/`mediodia` (977) y `próxim`/`proxim` (925/1614) ya cubren ambas; meridiem `delamañana`/`delamanaana` (1011) cubre la forma sin tilde habitual (probe: "9 de la manana" parses OK); `sabados` fixed (1457). Sin más asimetrías activas. + c.88 fix P1 parser "próximo" sin tilde dicho hoy=ese día → HOY en vez de +7; c.87 feat P2 búsqueda: `SearchEngine` reconoce **"semana que viene"/"próxima semana"** como nuevo `DateScope.NEXT_WEEK` → filtra tareas de lunes-domingo de la semana próxima (antes "semana que viene" caía a `THIS_WEEK` por `WEEK_TOKENS` sin distinguir modificador, mostrando la semana actual en vez de la próxima — la distinción planificar-próxima-semana se perdía). Detección: `NEXT_WEEK_TOKENS = {proxima, proximas, viene}`; rama `hasWeek && hasNext → NEXT_WEEK` antes de `hasWeek → THIS_WEEK`. Rango: `startNextWeek = today + daysToSunday + 1` (lunes próximo), `endNextWeek = startNextWeek + 6` (domingo próximo); reusa la misma fórmula `(7 - dayOfWeek) % 7` del fix de domingo del c.86. Sin nueva pantalla/botón (reuso el `SearchEngine` existente — recuperación de información). "esta semana"/"semana" sola siguen siendo `THIS_WEEK` (no-regresión). +5 tests; 617 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room; + c.86 fix P1 búsqueda Sunday-boundary `SearchEngine` + refactor P2 DRY `timeRank` TaskRules/WhatNowEngine (detallado abajo); + c.85 feat P1 parser: duraciones con número escrito "dos horas"/"treinta minutos"/"un par de horas" → durationMinutes real, antes null; c.84 asistente "tareas rápidas" alineado a ranking What Now; c.83 "¿Qué hago ahora?" delega en `WhatNowEngine.suggest` (motor honesto de What Now) y **explica por qué** esa tarea va primero ("está vencida"/"empieza enseguida"/"vence hoy"/"es urgente"… vía nuevo `WhatNowEngine.reasonLabel`) + avisa cuántas hay vencidas ("Además, tienes N vencidas"). El "plan mínimo" reemplaza su ordenador divergente (`priority`→`dueAt`) por `WhatNowEngine.ordered` —misma ordenación que What Now/widget—, así las tres superficies (What Now, asistente, widget) muestran el mismo orden. Antes el asistente usaba `TaskRules.nextBestTask` (ya time-aware desde c.45) pero NO explicaba la razón ni mencionaba vencidas, y el plan mínimo tenía un comparador aparte que divergía del de What Now (p.ej. vencida de prioridad normal vs urgente no vencida: What Now = vencida por rango, plan mínimo viejo = urgente por prioridad). Refactor DRY: `WhatNowEngine.suggest` ahora reusa el nuevo `ordered(...)` (fuente única del ranking, antes el comparador estaba duplicado). Sin nueva pantalla/botón. +2 tests (`whatNow_explainsWhyAndMentionsOverdue`, `planMinimo_ranksOverdueFirst`); 602 domain tests PASS (assistant compilado/ejecutado aparte: 5 AssistantEngineTest PASS); smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room; + c.82 fix P1 parser rango horario con meridiem solo en el INICIO "de 6pm a 8"/"de 2pm a 4"/"de 6 de la tarde a 8": el fin bare NO heredaba el PM del inicio → `endHr=8 < startHr=18` violaba la guarda `endMin>startMin` → rango rechazado, `durationMinutes=null` Y título sucio ("Reunión de a 8"); ahora `startPmEffective` propaga el PM del inicio al fin bare (mismo lado del mediodía) → 18:00→20:00, dur 120, con anti-falso-positivo (`midnightWrap` solo si el fin NO va seguido de un sustantivo de cantidad "de 2pm a 4 entradas") y cruce inverso "de 11pm a 1" (inicio PM, fin bare con `endHr<startHr` → NO hereda PM, envuelve +24h → 23:00→01:00, dur 120); unifica en un único bloque `rangeMatch` que cubre mismo-día, medianoche y el simétrico cruce de mediodía, reemplazando el wrap ad hoc de c.80; +12 tests, 593 domain tests PASS; + c.81 feat P2 búsqueda semántica por fecha en `SearchEngine` ("hoy"/"mañana"/"esta semana"/"atrasadas/vencidas" filtran por `dueAt` no por texto; +9 tests, 597 domain tests PASS); + c.80 fix P1 parser cruce de medianoche "Cena de 10 de la noche a 1 de la madrugada": `rangeMatch` exigía `endMin > startMin` → overnight rechazado y `durationMinutes=null`; ahora `sameDay` vs overnight con `rawDuration = if (sameDay) end−start else end+24h−start`, clamp 5..24h, cruce solo con señal clara; +4 tests, 588 domain tests PASS; + c.79 fix P1 parser cruce del mediodía en rango horario con meridiem solo al final "de 12 a 2 de la tarde"/"de 11 a 1 de la tarde": (1) la duración se computaba con horas crudas del texto (2−12=−600) y coerceIn dejaba 5 min en vez de 120; (2) la propagación PM del c.76 era ciega → "de 11 a 1 de la tarde" convertía el inicio 11→23 (dueAt=23:00); ahora la duración usa horas absolutas resueltas (sAbs/eAbs) y la propagación PM es condicional `startHr <= endHr` (mismo lado del mediodía) aplicada simétricamente a rangeMatch/rangeStartTime; +6 tests, 584 domain tests PASS; + c.78 fix P1 parser rango horario con meridiem compacto solo en el extremo final "de 6 a 8 pm"/"de 9 a 11 am": el `explicitTime` capturaba el extremo final "8 pm"→20:00 y sombreaba `rangeStartTime` (18:00) → dueAt caía a la hora de FIN (20:00), no de inicio (18:00); recordatorio 2h tarde; ahora detección posicional `explicitTimeIsRangeEnd` deja ganar a `rangeStartTime` (inicio, con propagación PM del c.76) → 18:00, duración correcta 120; horas sueltas "Llamada 8pm"→20:00 sin regresión; + c.77 feat P1 resumen: el veredicto del día `DayLoad` ahora usa la ventana de jornada APRENDIDA (`LearningProfile`) en vez de la fija 9–18 → la tarjeta de hoy deja de mentir para horarios no estándar (jornada 9–23 a las 17:00 = ON_TRACK no OVERLOADED; jornada 6–14 a las 13:00 = OVERLOADED no ON_TRACK); `SummaryEngine.summarize`/`assessDayLoad` reciben `dayStartMinute`/`dayEndMinute` (defaults 9–18) + sobrecarga con `LearningProfile?`; `TodayScreen` computa el perfil igual que `PlannerScreen` y lo pasa; +4 tests; + c.76 fix P1 parser rango horario "de 6 a 8 de la tarde": el meridiem PM del extremo final no se propagaba al inicio sin meridiem → agendaba 06:00 en vez de 18:00 (recordatorio 12h antes de la cita real); ahora el inicio bare hereda el PM del extremo final "de la tarde/noche" → 18:00, duración correcta 120; + c.75 fix parser "fin/mediados/principios del mes que viene/próximo" ancla al mes siguiente (no al actual) + fix "antepasado mañana" → +3 días y título limpio; + c.74 fix P1 RecurrenceEngine anual anclado a 29 de febrero: `plusYears(1)` clampaba a 28/2 y derivaba el ancla para siempre (cumpleaños/aniversario bisiesto perdido) → ahora `nextYearly` salta años no bisiestos, simétrico al `nextMonthly` 31 de c.18; + c.73 fix parser "la semana que viene el lunes/viernes" → día objetivo de la semana próxima, no +7d genérico; + c.72 fix parser "jueves que viene" dicho en jueves -> proxima semana (no HOY); + c.71 fix parser orden inverso "el mes que viene el N" agenda día N del mes siguiente; + c.70 fix P0 crash parser día fuera de rango "el 0/99 de mes" + fix P1 "el 15 de agosto del 2027" no capturaba año "del" → fecha errónea 2026↔2027 y título "del 2027" huérfano; + c.69 feat resumen accionable: un toque pospone la tarea sugerida a mañana) (fix(parser): "en media hora"/"en un cuarto de hora" ya son fecha relativa (P1))
- **Branch de trabajo**: `openhands/autonomous-ordia` (sobre c.70 fix parser P0/P1 + c.69 feat resumen accionable un-toque + c.68 fix parser "el N del mes que viene" día N del mes siguiente + c.67 refina sugerencia de posposición: nunca nombra tareas en curso ni inminentes + c.66 sugerencia concreta de tarea a posponer cuando el día está saturado + c.65 veredicto honesto del día + c.64 fix parser standalone "N de la tarde/noche" + fix P1 `monthNameMatch.find()` casaba mes inválido + c.63 detección de vencidas importantes en el Guardián + c.62 integridad de recordatorios en el editor + c.61 meridiem sin "a las" + c.60 rango-minutos/meridiem + c.59 verbo-recordatorio + c.58 fracción sub-hora/"en la tarde" + c.57 número-escrito + c.56 subtarea-autocomplete + c.55 partOfDay DAILY + c.54 intervalo+días + c.53 What Now + c.52 snooze)
- **main**: contiene SOLO infraestructura de orquestación (workflows); no el rebuild de la app.
- **Workflows autónomos (en `main`)**: `ordia-autonomous-jules.yml` (cron `17 */2 * * *` + dispatch)
  y `ordia-autonomous-merge.yml` (pull_request_target + cron `*/15 * * * *` + dispatch).
- **Release workflow**: publica APK firmada en cada push a `openhands/autonomous-ordia` (incluso
  docs-only) → los commits de código generan releases automáticamente.

| P1/P2 | Parser — fechas relativas/pasadas/imposibles + rango horario + recurrencias laborables/quincenal/bare + día de mes suelto | FIXED → VERIFIED: "esta semana" c.34; "un par de" c.35; "mediados de semana" c.36; "a las N horas" c.37 cont. (316 tests); "a finales de semana" c.37 (319 tests); fechas pasadas "hace N"/"la semana/el mes pasado" + recuperación fechas imposibles (29 feb, 31 abr) c.38 (329 tests); fix "de/por/a la mañana" (hora) vs fecha "mañana" c.39 (336 tests); recordatorios con números escritos y fracciones c.40 (344 tests); listas de días sin coma + plurales sábados/domingos c.41 (350 tests); rango horario sin "horas" ambas < 13 c.42 base (353 tests) + ampliación followers c.42 cont. (358 tests); recurrencia quincenal "cada quincena"/"quincenalmente" c.42 (365 tests); día de semana suelto hoy con hora futura → hoy c.42 cont.2 (362 tests); listas de días sin prefijo ("gym sábados y domingos") c.42 (369 tests); "entre semana"/"días laborables/hábiles"/"de lunes a viernes" = WEEKLY [1-5] c.43 (376 tests); fecha/hito "la quincena" (1ra/2da/sin cualificar) c.44 (388 tests); `nextBestTask` time-aware (widget/asistente) c.45 (394 tests); **"el 15" día de mes suelto con artículo** c.47 (394+4 tests); **"de aquí a N"/"de acá a N" prefijo relativo coloquial** c.50 (413 tests); **DayPlanner conflicto startAt otro día** c.51 (415 tests); **intervalo+días "cada 2 semanas los lunes"/"cada quincena los lunes y viernes"/"cada 3 semanas de lunes a viernes"** c.54 (428 tests); **"cada mañana/tarde/noche/madrugada" + "todas las mañanas/tardes/noches" como recurrencia DIARIA** con hora canónica (c.55, 435 tests); **autocompletar padre al cerrar última subtarea desde notificación** (`ReminderActionReceiver.ACTION_COMPLETE` ↔ `SubtaskRules.shouldAutoCompleteParent`) (c.56); **intervalo con número escrito "cada dos semanas"/"cada tres meses"/"cada quince días"/"cada dos años"** (c.57, 439 tests); **fracción sub-hora "a las 9 y media"/"a las 3 y cuarto" (media→30, cuarto→15) + conector caribeño "en la tarde/noche/mañana"** (c.58, 450 tests); **verbo de recordatorio sin cantidad "recuérdame/avísame/no dejes que olvide" con fecha límite → recordatorio 30 min antes + verbo limpiado del título** (c.59, 455 tests); **rango horario con minutos/meridiem en ambos extremos "clase de 9:30 a 11"/"de 9am a 11am"/"de 2pm a 4pm" → duración real (fin−inicio) en minutos + título limpio** (c.60, 463 tests); **meridiem sin "a las" "Reunión 2pm"→14:00 (era 02:00) + hora de inicio del rango como dueAt "de 9 de la tarde a 11 de la noche"→21:00 (era 15:00)** (c.61, 469 tests); **editor de tareas preserva el offset de recordatorio personalizado al editar campos no relacionados** (`ReminderRules.resolveReminderAt` ↔ `EditorDialogs`): antes `reminderAt = due - 30min` siempre destruía offsets explícitos ("2h antes") al cambiar prioridad/proyecto/etiquetas, y para recurrentes corrompía el recordatorio de TODAS las ocurrencias futuras (c.62, 475 tests; 481 tras rebase con run paralelo c.61); **Guardián detecta vencidas importantes: la tarea más atrasada con ≥2 días (olvidada) sube a `Tone.FOCUSED` y surface cuánto tiempo lleva ("3 días"/"2 semanas") pidiendo la decisión real (hacer hoy/reprogramar/quitar) en vez de "empieza por esta"** (`GuardianCoach.forgottenAgeLabel` ↔ `FORGOTTEN_DAYS_THRESHOLD`): antes toda vencida recibía `GENTLE` + mensaje genérico idéntico sin distinguir 10 min de 10 días, sin ayudar a recuperar compromisos olvidados (c.63, 485 tests); **veredicto honesto del día en el resumen de Today: `SummaryEngine` calcula `DayLoad` (LIGHT/ON_TRACK/FULL/OVERLOADED) comparando los minutos restantes con el tiempo libre hasta el fin de jornada (9–18) y lo surface como UNA línea accionable en la tarjeta existente ("El día va a tiempo. Sigue con la siguiente tarea." / "El día está lleno pero cabe. Empieza ya la próxima tarea." / "No cabe todo hoy. Elige qué dejar para mañana.") en vez de obligar al usuario a hacer la aritmética mental (c.65, 500 tests); **sugerencia concreta de tarea a posponer cuando el día está saturado: cuando `DayLoad == OVERLOADED`, `SummaryEngine` propone UNA tarea real (la de menor prioridad y, a igual prioridad, la que vence más tarde = más margen), nunca una vencida, y la surface en la misma línea ("No cabe todo hoy. Una opción es dejar para mañana «…».") en vez del vago "elige qué dejar" (c.67, 516 tests)**; **sugerencia accionable con UN toque: la línea OVERLOADED es ahora tappable —"Toca para mover «…» a mañana"— y `TaskRules.deferToNextDay` (regla pura) traslada la tarea a mañana a la misma hora local (vía `ZonedDateTime`, correcto frente a DST) preservando el offset del recordatorio (crítico para recurrentes) y la distancia inicio→vencimiento; `OrdiaViewModel.deferTaskToTomorrow` reusa `saveTask` (reagenda el recordatorio). Sin nueva pantalla/botón: la sugerencia pasa de pasiva a accionable en la misma línea (c.69, 522 tests)**; "el N del mes que viene" → día N del mes siguiente (c.68, 516 tests); **"el 0/99 de <mes>" (día fuera de rango) ya no crashea: `parseMonthNameDate` valida `day in 1..31` antes de crear `LocalDate` (P0 crash → dueAt=null sin caer); "el 15 de agosto del 2027" (español usa "del" antes del año) ahora captura el año: `monthNamePattern` acepta `del?` antes del año → agenda 2027 (no 2026) y no deja "del 2027" huérfano en el título (c.70, 528 tests) **"jueves que viene"/"próximo día" dicho en el propio día objetivo ("jueves que viene" dicho un jueves) → +7 en vez de HOY: `weekdayPattern` capturaba el sufijo "que viene"/"próximo" como grupo no capturador, así el código nunca distinguía "el próximo jueves" de "el jueves" suelto; `weekdaySameDayCandidate` permitía hoy y `nextWeekdayOrSame` devolvía hoy → tarea agendada el día equivocado (P1 integridad de agenda). Ahora `nextExplicit = match.value` contiene "que viene"/"próxim" → fuerza `nextWeekday` estricto (+7) y desactiva `weekdaySameDayCandidate`; el día suelto "el jueves a las 18" dicho en jueves sigue pudiendo ser hoy si la hora no pasó (no-regresión). +7 tests; 541 domain tests PASS (c.72, 534→541)**; **"la semana que viene el lunes/viernes"/"el lunes de la semana que viene" → día objetivo de la semana próxima (no +7d genérico): nuevos `nextWeekWeekdayReversePattern`/`nextWeekWeekdayForwardPattern` + helper `nextWeekWeekdayDate` ancla al próximo lunes estricto + offset del weekday; procesados antes que `nextPeriodPattern` (c.73, 549 tests)**; **límites mensuales con modificador "mes que viene"/"próximo" anclan al mes SIGUIENTE (no al actual): `endOfMonthPattern`/`midOfMonthPattern`/`startOfMonthPattern` ahora capturan el calificador y `monthBaseForBoundary` lo resuelve (anti-doble-desplazamiento para "principios") + **"antepasado mañana" → +3 días y título limpio** (antes "mañana" casaba como fecha suelta → +1 y "antepasado" quedaba en el título) (c.75, 563 tests)**; **rango horario con meridiem solo en el extremo final "de 6 a 8 de la tarde"/"de 2 a 4 de la noche": el inicio bare hereda el PM del extremo final → 18:00/14:00 (era 06:00/02:00, recordatorio 12h antes de la cita real); `rangeMatch`/`rangeStartTime` propagan `endPm` al inicio sin meridiem + reorden del `when`; no se propaga en sentido inverso (evita falso positivo "de 2pm a 4 entradas") (c.76, 568 tests)**; **"próximo" sin tilde ("proximo viernes"/"viernes proximo") dicho en el propio día objetivo → +7 en vez de HOY: `nextExplicit` (línea 919) solo detectaba la forma acentuada `"próxim"`, la sin tilde `"proxim"` caía en HOY (P1 cita una semana antes); alineado con `monthBaseForBoundary` (c.88, 620 tests)** |


## Último trabajo — Ciclo 102: Parser — "a última hora" (fin de jornada) no se interpretaba como hora → tarea SIN vencimiento/agendada a 09:00 + título sucio; además `\b` ASCII fallaba con "ú" acentuada (P1 captura/agenda)

Bug **P1 (captura/agenda errónea + título sucio)**: la forma cotidiana **"a última hora"** (fin de
jornada, ~18:00) NO se interpretaba como hora canónica. Sin patrón específico, la hora caía al
default **09:00** (mañana en vez de tarde) — la persona decía "reunión a última hora" y recibía
una agenda a las 9 de la mañana — y además el conector "a última hora" quedaba como residuo en el
título → título sucio ("reunión a última hora"). Asimetría flagrante con **"a primera hora"**
(09:00) que SÍ tenía patrón canónico. Un bug más sutil: **"última hora" sin el conector "a"**
(con tilde en la ú) no casaba en absoluto: el `\b` inicial (boundary ASCII-only, basado en
`\w` = `[a-zA-Z0-9_]`) NO reconoce "ú" como carácter de palabra, así que la posición antes de "ú"
(entre dos `\W`: espacio y "ú") no es un límite de palabra → `match=null` → `dueAt=null` silencioso.
La forma sin tilde "ultima hora" (ASCII) sí casaba. Por separado, **"a mediodía"/"a medianoche"**
(sin la contracción "al"/"a la") dejaban el conector "a" como residuo en el título.

Causa raíz (dos defectos):
1. Ausencia de un patrón canónico de fin de jornada simétrico a `primeraHoraPattern`, de modo que
   la hora caía al default 09:00 y el conector no se limpiaba.
2. `\b` inicial del patrón nuevo (copiado de `primeraHoraPattern`, que empieza con "p" ASCII y por
   eso no exhibe el fallo) no casa antes de una letra acentuada "ú" no-ASCII. La asimetría viene
   de que "primera" no empieza por acento y "última" sí.

Solución (mínima, `NaturalTaskParser.kt`, sin nueva pantalla/botón — "menos interfaz, más potencia"):
1. Nuevo `ultimaHoraPattern`
   `(?i)(?<![a-záéíóúñ])(?:a\s+)?[uú]ltima\s+horas?(?:\s+de\s+la\s+(?:ma[nñ]ana|manana|tarde|noche|madrugada))?\b`
   + `ultimaHoraTime = LocalTime.of(18, 0)`, simétrico a `primeraHoraPattern`. El
   **lookbehind Unicode-safe `(?<![a-záéíóúñ])`** reemplaza `\b` para que "última hora" (con tilde,
   sin "a") también case: el carácter anterior (espacio, inicio) no es letra → lookbehind pasa;
   una "x" antes ("xúltima") sí es letra → rechazado (anti-falso-positivo verificado con probe).
2. Match + resolución de respaldo (`?: ultimaHoraMatch?.let { ultimaHoraTime }` en la cadena de
   `parsedTime`, después de `primeraHoraMatch`) + limpieza del título (`.let { value -> ultimaHoraPattern.replace(value, " ") }`).
3. Si hay una parte del día explícita ("a última hora de la tarde"), ésta tiene prioridad en la
   resolución (el patrón de parte del día entrega su hora propia) y el patrón solo limpia
   "a última hora" — no fuerza contexto PM.
4. `mediodía`/`medianoche` patterns aceptan `a\s+` (sin "la") además de `al`/`a la` → limpian el
   conector "a" del título.

Tests (TDD RED→GREEN): +8 en `NaturalTaskParserTest.kt`: `ultimaHoraSinFechaUsaHoy`,
`ultimaHoraConFechaRelativaCombinaBien`, `ultimaHoraDeLaNocheRespetaCanonicaNoche`,
`ultimaHoraInterpretaFinJornadaYLimpiaTitulo`, `ultimaHoraConParteDelDiaEspecificaRespetaEsaHora`,
`ultimaHoraSinConectorATambienFunciona` (la que expone el bug del `\b` ASCII), `aMediodiaSinContraccionLimpiaTitulo`,
`aMedianocheSinContraccionLimpiaTitulo`. El probe JVM (9 casos) confirmó: "a última hora"→18:00
(con/sin fecha, relativa), "última hora" sin "a"→18:00 (antes null), "a última hora de la
tarde/noche/madrugada/mañana" respeta la parte del día, todos los títulos limpios; `xúltima`
rechazado (sin falso positivo). `bash tools/run_domain_tests.sh` → **705 PASS** (28 clases —
697 c.101 + 8 nuevos); `bash tools/run_domain_checks.sh` → smoke 25 OK. Sin regresión
("a primera hora" intacto, "al mediodía"/"a la medianoche" intactos).

- **NO VERIFICADO**: gradle/lint/assemble/Android/UI/Room con DAOs reales (sin Android SDK).
  Render real del parser en la app no probado en dispositivo.
- **Hallazgos adicionales (descubrimiento continuo)**: el defecto `\b`-ASCII-vs-acento ya apareció
  en c.91 ("último día del mes", resuelto con `(?<!\p{L})`) y ahora reaparece aquí. Esta es una
  **familia recurrente**: cualquier patrón que use `\b` antes de una palabra que empiece con
  acento (ú, á, í...) fallará en Java. Queda una auditoría pendiente: escanear todos los `\b` del
  parser que precedan a clases de caracteres que incluyan acentos y reemplazarlos por lookbehind
  Unicode-safe. Próxima auditoría: salir del parser de horas canónicas y mirar detección de
  compromisos en notas, `RecurrenceEngine` edge cases (DST/clamps), replanificación si OVERLOADED
  recurrente, captura/búsqueda/What Now.

## Último trabajo — Ciclo 101: Parser — "en media hora y cuarto" (fracción + cuarto sin número entero) dejaba residuo en el título y agendaba 15 min antes (P2 precisión de captura)

Bug **P2 (precisión de captura/título sucio)**: `"en media hora y cuarto"` (30+15 = 45 min,
dos fracciones sumadas sin número entero) no se parseaba como punto relativo.
`fractionalRelativePattern` (c.94) casa solo "en media hora" (+30 min) y dejaba "y cuarto"
como residuo en el título; `compoundFractionalRelativePattern` (c.94b) exige número+"horas"
antes del "y", así que no casaba. Resultado:

| Entrada | Antes (bug) | Ahora |
|---|---|---|
| `cita en media hora y cuarto` | title="cita y cuarto", due=+30min | title="cita", due=+45min ✅ |
| `llamar en media hora y cuarto` | title="llamar y cuarto", due=+30min | title="llamar", due=+45min ✅ |
| `en un cuarto de hora y cuarto` | residuo, +15min | title limpio, +30min ✅ |
| `nos vemos de aquí a media hora y cuarto` | residuo, +30min | title limpio, +45min ✅ |

- **Causa raíz**: `fractionalRelativePattern` no contemplaba el sufijo "+ cuarto" sobre una
  fracción sin número entero; el compuesto sí lo hacía pero solo con coeficiente numérico.
- **Solución** (`NaturalTaskParser.kt`, cambio mínimo, reutiliza el flujo existente): nuevo
  `fractionalAndQuarterRelativePattern = prefijo (en|dentro de|de aquí a|de acá a) +
  (media hora|(un )?cuarto (de )?hora) + "y cuarto"` → `now + base + 15` min, donde base = 30
  si "media", 15 si "cuarto". Se procesa **ANTES** que `fractionalRelativePattern` para robar
  la frase completa (sin residuo en el título) y antes de la duración. Incluido en
  `effectiveRelativeDueAt` (prioridad sobre `fractionalRelativeDueAt`) y en la condición de
  exclusión de `relativeIsDays` (sub-hora: la hora explícita no la sobreescribe).
- **Heurística honesta**: el prefijo obligatorio preserva la duración real ("reunión media
  hora" sin prefijo sigue siendo `durationMinutes=30`, no-regresión c.94) y no choca con el
  recordatorio ("media hora antes" lo captura `reminderPatterns`). Forma poco común (la gente
  suele decir "tres cuartos de hora", ya cubierto), pero afecta precisión y limpieza de título.
- **Tests**: +4 tests en `NaturalTaskParserTest.kt`
  (`enMediaHoraYCuartoEsFechaRelativaDe45Min`, `enUnCuartoDeHoraYCuartoEsFechaRelativaDe30Min`,
  `dentroDeMediaHoraYCuartoEsFechaRelativaDe45Min`, `deAquiAMediaHoraYCuartoEsFechaRelativaDe45Min`).
  Verificado sin regresión: "en media hora"=+30, "en un cuarto de hora"=+15, "en una hora y
  cuarto"=+75, "en una hora y media"=+90, "en tres cuartos de hora"=+45 todos intactos.
  Comando: `bash tools/run_domain_tests.sh` → **697 tests PASS** (693 + 4). Smoke
  (`bash tools/run_domain_checks.sh`) → **25 OK**.
- **NO VERIFICADO**: gradle/lint/assemble/Android/UI/Room con DAOs reales (sin Android SDK).
- **Archivos modificados**: `app/src/main/java/com/ordia/app/domain/NaturalTaskParser.kt`,
  `app/src/test/java/com/ordia/app/domain/NaturalTaskParserTest.kt`,
  `AI_AUTONOMY/{BACKLOG,CURRENT_STATE,RUN_LOG}.md`.
- **Estado**: FIXED → VERIFIED (dominio JVM).
- **Próxima prioridad**: continuar descubrimiento de frases cotidianas del parser y auditar
  producto (captura ultrarrápida, What Now, recuperación de vencidas, inbox inteligente).

