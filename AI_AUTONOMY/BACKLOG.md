# BACKLOG — Ordía

> Inventario priorizado de mejoras y correcciones. Formato:
> `PRIORIDAD | ÁREA | PROBLEMA | EVIDENCIA | ESTADO`
> El agente mueve ítems a FIXED/VERIFIED con tests que lo demuestren.
> No añadir ideas vagas; solo problemas reales con evidencia.

## Pendientes (P0 / P1 / P2)

| PRIORIDAD | ÁREA | PROBLEMA | EVIDENCIA | ESTADO |
|-----------|------|----------|----------|--------|
| P1 | Recordatorios / Datos | `EditorDialogs` (edición de tarea) recalculaba `reminderAt = dueAt - 30*60_000L` en CADA guardado cuando el toggle de recordatorio estaba activo. Para una tarea existente con offset explícito distinto de 30 min ("recuérdame 2 horas antes" → `reminderAt = due - 120min`), editar un campo NO relacionado (prioridad/proyecto/etiquetas/flagged) y guardar SOBRESCRIBÍA el offset a 30 min → el usuario pedía "2h antes" y Ordía avisaba solo 30 min antes. En recurrentes, `RecurrenceEngine.nextOccurrence` reutiliza el offset (`dueAt - reminderAt`) en TODAS las ocurrencias futuras → una edición inocua corrompía el recordatorio para siempre en cada nueva ocurrencia | `EditorDialogs.kt:213` `reminderAt = if (reminderEnabled && normalizedDue != null) normalizedDue - 30 * 60_000L else null`; `CommitmentEngine.kt:102` `DEFAULT_REMINDER_OFFSET_MS = 30*60_000L`; `RecurrenceEngine.nextOccurrence` reutiliza offset. Simétrico al c.52 (snooze no corrompe offset) | FIXED (ciclo 62): nueva regla pura `ReminderRules.resolveReminderAt(existing, reminderEnabled, dueAt)` — conserva `reminderAt` si due sin cambios; traslada offset (`dueAt-(oldDue-oldReminder)`) si due cambió; 30min por defecto solo para tareas nuevas o recordatorio recién activado. `EditorDialogs` la usa. Constante `DEFAULT_REMINDER_OFFSET_MS` centralizada en `ReminderRules`. +9 tests en `ReminderRulesTest`; 475→481 domain tests PASS (tras rebase con run paralelo c.61); smoke 25 OK; NO VERIFICADO gradle/Android/UI (EditorDialogs es Compose) |
| P1 | Parser | `NaturalTaskParser` no reconocía fracción sub-hora cotidiana **"y media"/"y cuarto"**: "Cita a las 9 y media" → `due=09:00` (debería 09:30) y `title='Cita y media'` (frase filtrada al título). Cita/reunión programada 30 min (o 15 min) mal = recordatorio/reunión en el momento erróneo | Probe JVM c.58: `Cita a las 9 y media` → `due=09:00`, `title='Cita y media'`; `Cita a las 3 y cuarto` → `due=03:00`. Fix A: timePatterns[0] +grupo fracción, meridiem→grupo4 | FIXED → VERIFIED (ciclo 58): timePatterns[0] gana grupo 3 opcional `(?:\s+y\s+(media|cuarto))?`; `minute = explicit ?: (media→30, cuarto→15)`; respeta meridiem/contexto PM. +7 tests; 450 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android |
| P2 | Parser | `NaturalTaskParser` no reconocía **"en la tarde/noche/mañana"** (forma caribeña/hispanoamericana, zona de la app) como conector de parte del día (solo "a la"/"de la"/"por la"): "hoy en la tarde" → `due=09:00` (debería 15:00) y residuo "en la tarde" en el título, inconsistente con "hoy a la tarde" que SÍ funcionaba | Probe JVM c.58: `Reunión hoy en la tarde` → `due=09:00`, `title='Reunión hoy en la tarde'`; `Reunión en la tarde a las 4` → `due=09:00` sin contexto PM. Fix B: standalonePartOfDayPattern +`en la`, mananaAsDate +`en` | FIXED → VERIFIED (ciclo 58): standalonePartOfDayPattern añade `en\s+la`; mananaAsDate timeMarker añade `en` para que "en la mañana" no cuente "mañana" como fecha. +5 tests; 450 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android |
| P2 | Parser | `NaturalTaskParser` rango horario **CON minutos** ("clase de 9:30 a 11") o con meridiem en ambos extremos ("de 9am a 11am") no parsea duración correctamente: `title='Clase de a 11'` (pierde "9:30") y sin duración; "de 9am a 11am" → `title='Clase de a'` (pierde ambas horas) | Probe JVM c.58: `Clase de 9:30 a 11` → `title='Clase de a 11'`, `dur=null`; `Clase de 9am a 11am` → `title='Clase de a'`, `dur=null` | FIXED → VERIFIED (ciclo 60): `timeRangePattern` gana grupos de minutos `:MM` y meridiem (`am`/`pm`/`de la tarde`/…) en CADA extremo (7 grupos); `rangeMatch` resuelve cada extremo a hora absoluta (offset PM por separado) y la duración es `(fin−inicio)` en minutos reales (no solo horas en punto). Mantiene el guard anti-falsos positivos ("de 2 a 5 entradas" sigue rechazado). +8 tests (`rangeWithStartMinutesParsesRealDuration`=90, `…BothEndpoints…`=120, `…MinutesAndHoursUnit…`=90, `rangeWithMeridiemAm…`=120, `…MeridiemPm…`=120, `…MinutesAndMeridiem…`=120, `rangeWithDeLaTardeMeridiem…`=120, `rangeWithMinutesDoesNotClampToDayMax`=120); 463 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android |
| P2 | Inteligencia / What Now | `WhatNowEngine.suggest` NO desempataba por prioridad entre tareas del mismo rango temporal, mientras que `TaskRules.nextBestTask` (widget/asistente) SÍ (`thenByDescending { priorityScore }`). Inconsistencia real: dos tareas atrasadas donde la NORMAL vencía ANTES que la URGENTE → What Now sugería la normal (por `dueAt` más próximo) y el widget sugería la urgente (por prioridad). What Now y el widget daban respuestas distintas para el mismo conjunto de tareas. Además `priorityScore` estaba duplicado (privado) en `TaskRules` y `DayPlanner`, violando la fuente única de verdad | `WhatNowEngine.kt:52` comparator sin rama de prioridad; `TaskRules.nextBestTask:42` sí la tiene. Test `picksUrgentOverNormalAmongOverdue`: dos atrasadas (normal 9:00, urgente 10:00 mismo día) → vieja lógica elegía normal; tras fix urgente, y coincide con `nextBestTask` | FIXED → VERIFIED (ciclo 53): `TaskRules.priorityScore` ahora público (fuente única); `WhatNowEngine` añade `thenByDescending { TaskRules.priorityScore }` antes de `dueAt`; `DayPlanner` reutiliza `TaskRules.priorityScore` (eliminada su copia privada duplicada). +1 test (422 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/UI) |
| P2 | Planificador / UX | `DayPlanner.planReason` etiquetaba como `DUE_TODAY` ("Vence hoy") a **toda** tarea con `dueAt`, ignorando la `date` del plan. El planificador se construye para la `selectedDate` del usuario (y `AutomationEngine.PLAN_DAY` usa hoy). Consecuencia: al abrir el plan de **mañana** u otra fecha futura, una tarea que vence ese día mostraba **"Vence hoy"** — urgencia falsa que confunde sobre qué vence realmente hoy | `DayPlanner.planReason(task, now)` no recibía `date`/`zone`; el `when` mapeaba `dueAt != null` → `DUE_TODAY` incondicional. Callers: `PlannerScreen` (plan de la fecha seleccionada), `AutomationEngine.PLAN_DAY` (hoy). Test: plan de mañana con tarea que vence mañana → vieja lógica decía `DUE_TODAY` ("Vence hoy", falso) | FIXED → VERIFIED (ciclo 48): `planReason(task, now, date, zone)` compara la fecha de vencimiento con el día **real de hoy** (`DateRules.toLocalDate(now, zone)`): si coincide → `DUE_TODAY`; si no → nuevo `DUE_ON_DATE` ("Vence este día"). La urgencia real de "hoy" no se degrada con la vista. +2 tests; 399 domain tests PASS; NO VERIFICADO gradle/Android/UI |
| P2 | Inteligencia / What Now | `TaskRules.nextBestTask` (usado por el **widget** de inicio, el **asistente** y el fallback `nextTask` del ViewModel) usaba un orden puramente estático (`overdue > prioridad > dueAt > createdAt`) que **ignoraba el tiempo**: una tarea ocurriendo **ahora mismo** (`startAt <= now <= startAt+dur`, p.ej. una reunión a las 15:00 siendo las 15:00) no se priorizaba, y una **URGENTE para mañana** se sugería antes que una **NORMAL que vence hoy**. Inconsistencia con `WhatNowEngine` (tarjeta What Now de `TodayScreen`), que sí es time-aware y dejaba al widget/asistente dando una respuesta menos oportuna que la tarjeta principal. El widget es la superficie más vista | `WhatNowEngine.suggest` (`TodayScreen.kt:113`) prioriza in-progress-now > overdue > dueToday > urgent > high > inbox; `TaskRules.nextBestTask` NO. Callers: `OrdiaWidgetProvider.kt:66`, `AssistantEngine.kt:41`, `OrdiaViewModel.kt:161`. Test: urgente-mañana vs normal-hoy → vieja lógica elegía urgente-mañana | FIXED → VERIFIED (ciclo 45): `TaskRules.nextBestTask(tasks, now, zone)` con orden honesto (1) en curso ahora, (2) vencida, (3) vence hoy, (4) urgente, (5) alta, (6) inbox/orden/creación; las programadas para más tarde quedan últimas. Nuevos helpers `isInProgressNow` (privado) e `isDueToday` (público, zona-aware, reusado por `GuardianCoach`). Retrocompatible: `nextBestTask(tasks)` delega con `now`/zona por defecto. +2 tests; 386 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/UI/widget |
| P2 | Parser | `NaturalTaskParser` no reconocía números escritos en recordatorios relativos ("recuérdame **dos** horas antes", "una hora antes", "treinta minutos antes") ni fracciones ("media hora antes", "un cuarto de hora antes"): `reminderOffsetMinutes=null` y la frase quedaba como residuo en el título (la cita se olvidaba). `parseWrittenNumber` existía pero `reminderPatterns` solo capturaba `(\d{1,3})` (dígitos); además "media hora antes" era robado por el patrón de duración fraccionaria (30 min falsos como duración). Asimetría con la duración relativa ("en dos horas" sí funcionaba) | `writtenAmountPattern` (simétrico a la fecha relativa) en los 2 patrones de recordatorio + 2 patrones nuevos de fracción con contexto obligatorio ("antes"/"anticipación"/verbo); cálculo vía `parseWrittenNumber`; `media hora`=30 / `cuarto de hora`=15; `getOrNull(2)` para patrones de 1 grupo. 8 tests (`parsesWrittenAmountReminderWithVerb`, `parsesWrittenAmountReminderTwoHours`, `parsesWrittenAmountReminderThirtyMinutes`, `parsesWrittenAmountReminderWithoutVerb`, `mediaHoraAntesEsRecordatorio`, `cuartoDeHoraAntesEsRecordatorio`, `recuerdameMediaHoraDeAnticipacion`, `mediaHoraSinAntesSigueSiendoDuracion`); 323 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android | FIXED → VERIFIED (344 domain tests PASS tras merge; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 40 |
| P2 | Parser | `NaturalTaskParser` no reconoce rango horario **sin la palabra "horas"** ("clase de 9 a 11", "taller 18 a 20"): `dueAt=null`, `durationMinutes=null`, rango crudo como residuo en el título. El `timeRangePattern` requiere el sufijo opcional `(horas?|hs|h)` o contexto AM/PM; sin él no casa. Forma cotidiana de expresar un bloque horario | Probe JVM ciclo 35: `clase de 9 a 11` → `title='clase de 9 a 11'`, `dur=-` (debería ser bloque 09:00-11:00 o dur 120) | FIXED → VERIFIED ciclo 42: el guard del `rangeMatch` acepta rangos sin unidad y ambas horas < 13 cuando NO van seguidos de un sustantivo de cantidad (heurística honesta). Envío base (`0a77387`, run paralelo): 3 tests + set de followers básicos (con/y/o/para/hasta/luego/después/pero/porque + puntuación). **Ampliación ciclo 42 (este run)**: el set base dejaba residuo cuando el rango iba seguido de un DÍA de la semana ("el viernes"), un día relativo ("mañana") o un marcador de parte del día ("a la tarde", "por la noche"); se añadieron esos tokens (artículos el/la/los/las/un/una, a/al, por, sin, sobre, desde, del, días de la semana, mañana/hoy/ayer) al regex de followers seguros. +5 tests (`bareRangeSmallHoursFollowedByWeekdayParsesDuration`, `…RelativeDay…`, `…ATarde…`, `…PorLaNoche…`, `…CountableNounStillRejected`); 358 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android |
| P1 | Parser | `NaturalTaskParser` perdía la recurrencia de listas de días **sin prefijo** ("gym sábados y domingos", "fútbol domingos a las 18", "lavar auto sábados domingos"): `recurrence=NONE`, los días quedaban como residuo en el título → la rutina semanal se olvidaba en silencio (pérdida de datos). El patrón de recurrencia solo casaba listas con prefijo "los"/"cada"/"todos los". Forma bare tan común como la prefijada | Probe JVM ciclo 42: `gym sábados y domingos` → rec=NONE (debería WEEKLY 6,7); `fútbol domingos a las 18` → rec=NONE (debería WEEKLY 7) | FIXED → VERIFIED (ciclo 42): `parseRecurrence()` reconoce listas bare de 2+ días y día plural marcado (sábados/domingos) sin artículo; `dayNameRegex` evita falsos positivos. 4 tests (`parsesBareDayListRecurrence`, `parsesBareDayListWithExplicitTime`, `parsesBarePluralSingleDayRecurrence`, `bareSingleNonPluralDayIsNotRecurrence`); 388 domain tests PASS (tras integrar c.43 entre-semana + c.44 quincena); smoke 25 OK; NO VERIFICADO gradle/Android |
| P2 | Parser | `NaturalTaskParser` no reconoce "la **quincena**" como plazo/referencia temporal ("cobro de la quincena el 15", "nómina cada quincena", "próxima quincena"): `dueAt=null`, `recurrence=null`, frase como residuo. La quincena (día 15 y fin de mes) es un hito financiero/laboreal muy común en español; `finDeMes`/`mediadosDeMes` existen pero no la quincena | Probe JVM ciclo 35: `cobro de la quincena el 15` → `due=null`; `nómina cada quincena` → `rec=none` | FIXED → VERIFIED: recurrencia "cada quincena"/"quincenalmente" (WEEKLY x2) resuelta ciclo 42 (`97ec260`); **fecha/hito "la quincena"** resuelta ciclo 43 con `quincenaPattern` (procesado DESPUÉS de `nextPeriodMatch`, así "próxima quincena"/"quincena que viene" siguen como +15d y "en N quincenas" como relativo). Resuelve: "primera quincena"/"1ra"→día 15 (rueda a mes próximo si hoy≥15); "segunda quincena"/"2da"→fin de mes (rueda si hoy es último día); "la quincena"/"de la quincena" sin cualificar→próximo hito (día 15 si hoy<15; fin de mes si 15≤hoy<último; 15 del mes próximo si hoy=último). Combina con hora explícita. 12 tests; 388 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android |
| P2 | UI | `Icons.Outlined.InsertDriveFile` deprecado; usar `Icons.AutoMirrored.Outlined.InsertDriveFile` | warning de compilación en `TaskDetailScreen` | FIXED → VERIFIED (CI ciclo 26; run 31522884362 success, release v3.0.14) |
| P2 | i18n | Revisar coherencia de cadenas nuevas (command_palette, feedback, floating_capture, android_access) | inspección manual pendiente | REVISADO — no hay texto hardcodeado en pantallas (todo via stringResource); eliminadas 47 strings huérfanas (today_what_now_action + 46 intel_* de feature local-model removida). No requiere fix |
| P2 | QA | Verificar que las 6 variantes (Safe/Full/Advanced × debug/release) compilan tras cambios | `./gradlew test` | OPEN |
| P1 | Persistencia | Adjuntos de captura guardan el URI externo en `AttachmentEntity.uri` sin copiar el contenido a almacenamiento interno; `takePersistableUriPermission` se llamaba en `runCatching` silencioso. Si el permiso persistente falla (o caduca/revoca), el adjunto queda inaccesible tras reinicio y `startActivity(ACTION_VIEW)` falla con toast misleading ("Ninguna app pudo abrir…") | `OrdiaViewModel.attachCaptureIfPresent` guarda `attachmentUri` directo; `CaptureScreen` ~L135 `takePersistableUriPermission`; `NoteEditor`/`TaskDetail` abren vía URI externo | FIXED ciclo 32 (cont.4) — `AttachmentStorage` copia bytes a `filesDir/attachments/`, expone vía FileProvider (`${applicationId}.attachments` + `ordia_attachment_paths.xml`); `addAttachment`/`attachCaptureIfPresent` ahora importan el contenido; `resolveAttachmentUri` resuelve para abrir (legacy externo pasa tal cual); `deleteAttachment` borra el archivo interno. `takePersistableUriPermission` eliminado de Note/Task pickers (ya innecesario). Backup de seguridad: si la copia falla se conserva el URI original. NO VERIFICADO (Android/UI/Room requiere SDK); smoke 25 + 275 tests domain OK |
| P2 | Backup | Comprobar restauración con manifiesto corrupto (escenario adverso) | revisión de `RestoreData` | REVISADO — `BackupManager.validateAndParse` ya valida SHA-256 checksum, version, secciones desconocidas, fecha creación, faltantes; lanza `IllegalArgumentException` con mensajes claros; `catch(Exception)` devuelve `ImportResult(false,msg)`. Sin error silencioso. No requiere fix |
| P3 | Rutinas | ~~`saveRoutine` hace delete-then-reinsert de pasos sin transacción atómica; si el proceso muere a mitad, la rutina queda con pasos parciales/perdidos~~ FIXED ciclo 15: `RoutineStepDao.replaceSteps` (`@Transaction` delete+insert) + `saveRoutine` lo usa | `OrdiaViewModel.saveRoutine`; `Daos.kt` `replaceSteps`; smoke+202 tests OK; NO VERIFICADO (DAO/Room requiere Android) | FIXED (NO VERIFICADO Room) |
| P3 | UX | Pulido visual de pantallas renovadas del workspace | capturas tras sesión | OPEN |
| P1 | Parser | `NaturalTaskParser` no parseaba **"esta semana"** (plazo blando cotidiano: "háblalo esta semana", "lo termino esta semana"). `dueAt=null` → tarea olvidada (sin recordatorio, invisible en planificador/What Now) Y, con hora explícita, se fechaba en **HOY** por error ("esta semana a las 18" → hoy 18:00 en vez de "fin de esta semana a las 18") | Nuevo `thisWeekPattern` (`esta semana` + opcional `que viene`); resuelve al **próximo domingo** (fin de semana ISO lun→dom) a las 9:00, o **hoy** si hoy es domingo; detectado y borrado ANTES del período próximo (evita que "semana" active "semana que viene" y limpia "esta semana que viene"); integrado en `effectiveRelativeDueAt` como días para combinar con hora; 4 tests (`estaSemanaParsesDueAtProximoDomingo`, `estaSemanaRespetaHoraExplicita`, `estaSemanaSiHoyEsDomingoEsHoy`, `estaSemanaNoColisionaConSemanaQueViene`) | FIXED → VERIFIED (290 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 34 |
| P1 | Parser | `NaturalTaskParser` fechaba en MAÑANA tareas que usaban "de la mañana"/"por la mañana" como marcador de **hora** (parte del día), porque la palabra "mañana" colisionaba con el token de **fecha** "mañana". Así "Reunión a las 9 de la mañana" se programaba para MAÑANA 09:00 (reunión perdida HOY) y "Llamar a mamá por la mañana" para MAÑANA en vez de HOY. Bug de ambigüedad léxica no resuelto por el branch de fecha (mera presencia de la palabra) | Nuevo `mananaAsDate(working)` recorre todas las apariciones de "mañana" y sólo cuenta como fecha si al menos una NO está precedida por marcador de parte del día (`de la `/`por la `/`a la `/`esta `). "de/por/a la mañana" → HOY; "mañana por la mañana" → mañana (primera aparición suelta). `pasado mañana` se resuelve antes, sin regresión. 3 tests (`deLaMananaWithoutDateStaysToday`, `porLaMananaWithoutDateStaysToday`, `mananaPorLaMananaStillResolvesTomorrow`) | FIXED → VERIFIED (311 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 36 |
| P1 | Parser | **"cada mañana/tarde/noche/madrugada" (y "todas las mañanas/tardes/noches") no se reconocían como recurrencia DIARIA.** La forma más natural de un hábito cotidiano ("meditar cada mañana", "tomar pastillas cada mañana", "pasear al perro cada tarde") caía sin recurrencia. Misma colisión léxica que el c.36: la palabra "mañana" se consumía como **fecha** "mañana" y la rutina quedaba como **tarea única** (sin repetición). El recordatorio disparaba una sola vez y nunca más — la rutina diaria se perdía silenciosamente | Probe JVM ciclo 55: `meditar cada mañana` → antes `frequency=NONE` + fecha "mañana" (tarea única); tras fix `DAILY` 09:00 sin fecha suelta | FIXED → VERIFIED ciclo 55: nueva rama al **inicio** de `parseRecurrence` detecta `cada <parte>`/`todas las <partes>` → `DAILY` con hora canónica (mañana 09:00, tarde 15:00, noche 21:00, madrugada 04:00) y contexto PM. Procesada PRIMERO: "mañana" deja de ser candidato a fecha. Hora explícita preservada; PM offset a horas sin meridiem. +7 tests; 435 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/UI |
| P1 | Parser | `NaturalTaskParser` no reconocía la construcción coloquial multi-palabra **"un par de"** (= 2): "en un par de días/semanas/meses". `relativePattern` sólo aceptaba números o palabras-sueltas, así que caía a `dueAt=null` → tarea **olvidada** (sin recordatorio, invisible en planificador/What Now) | `relativePattern` captura `un\s+par\s+de` como cantidad; `parseWrittenNumber` lo resuelve a `2L`; funciona con cualquier unidad y con hora explícita. 4 tests (`unParDeDiasResuelveMasDosDias`, `unParDeSemanasResuelveMasCatorceDias`, `unParDeMesesResuelveMasSesentaDias`, `unParDeDiasConHoraExplicita`) | FIXED → VERIFIED (308 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 35 |

## Auditorías estáticas realizadas (sin hallazgos P0/P1)

| ÁREA | ALCANCE | EVIDENCIA | ESTADO |
|------|---------|----------|--------|
| Persistencia/Room | Entities (FK + índices), Daos (deleteSubtreeAndSelf transaccional, cascadas), OrdiaDatabase (migraciones 1-7, sin fallbackToDestructiveMigration) | inspección estática ciclo 2 | OK (no requiere fix) |
| Backup/Restore | BackupStore.replaceAll en `withTransaction` atómica (orden FK coherente), pre-restore backup, verify-after-commit + rollback, checksum SHA-256, mutex | inspección estática ciclo 2 | OK (no requiere fix) |
| Recordatorios | ReminderScheduler (enqueueUniqueWork REPLACE), TaskReminderWorker (re-lee task, filtra completed/archived/cancelled, quiet hours reschedule, POST_NOTIFICATIONS retry), ReminderActionReceiver (exported=false, mutex), ReminderResyncReceiver (TIMEZONE/TIME/DATE, futuro-only) | inspección estática ciclo 3 | OK (no requiere fix) |
| Seguridad/Manifiesto | allowBackup=false, cleartextTraffic=false, ReminderActionReceiver exported=false, ReminderResyncReceiver exported=true (requerido por system broadcasts, filtra acciones), MainActivity SEND/PROCESS_TEXT (texto capado, URI permiso en runCatching) | inspección estática ciclo 2 | OK (no requiere fix) |
| Task completion+recurrence | toggleTask bajo TaskMutationGate.mutex, RecurrenceEngine (reminder/start offset preservado, guard de avance), subtask auto-complete con logging de undo | inspección estática ciclo 2 | OK (no requiere fix) |
| Rutinas | RoutineRules (dedup wasRunToday: tarea creada hoy cuenta aun completada → evita duplicados al re-disparar, ciclo 20; antes exigía pendiente), runRoutine (sortOrder preserva orden, undo real vía AutomationUndoRules), archive/restore. saveRoutine ahora transaccional (`replaceSteps`) ciclo 15 | inspección estática ciclo 4 + fix ciclos 15 y 20 | OK (atomicidad saveRoutine + dedup wasRunToday resueltos) |
| Motores no-parser (Ciclo 20) | WhatNowEngine, GuardianEngine, LearningEngine, SubtaskRules, DayPlanner, QuietHours, RoutineRules, HabitRules, RecurrenceEngine, TaskRules, DateRules, TaskSnapshotCodec, UniversalCaptureEngine, FocusTimerRules, ReminderSync, AutomationRules (catalog+guard), AutomationEngine, AutomationUndoRules | inspección estática ciclo 20 | OK (no requiere fix) |

## Completados

| PRIORIDAD | ÁREA | PROBLEMA | EVIDENCIA | ESTADO |
|-----------|------|----------|----------|--------|
| P1 | Recordatorios / Datos | `ReminderActionReceiver.ACTION_COMPLETE` (completar tarea desde la notificación) completaba la subtarea, cancelaba su recordatorio y generaba su recurrencia, pero **NO** completaba la tarea padre cuando era la última subtarea pendiente — a diferencia de `OrdiaViewModel.toggleTask` → `completeParentAutomatically` (`SubtaskRules.shouldAutoCompleteParent`). Al cerrar el último hijo desde la notificación, el padre quedaba "pendiente" para siempre → **tarea olvidada** e inconsistencia notificación vs app | `ReminderActionReceiver.kt` `ACTION_COMPLETE` sin rama de autocompletado del padre; referencia `OrdiaViewModel.kt:~532` `completeParentAutomatically`. Lógica núcleo `SubtaskRules.shouldAutoCompleteParent` cubierta por `SubtaskRulesTest` | FIXED (ciclo 56): nuevo helper `completeParentIfDone` en `ReminderActionReceiver` que replica `completeParentAutomatically` (update padre, cancela recordatorio, recurrencia, `AutomationLogEntity` para deshacer) sin UI events. 435 domain tests PASS; smoke 25 OK; NO VERIFICADO receptor Android (sin SDK) |
| P1 | Parser | `NaturalTaskParser.parseRecurrence` perdía el **intervalo de cadencia** al combinarlo con una lista de días: **"cada 2 semanas los lunes"**, **"cada quincena los lunes y viernes"**, **"cada 3 semanas los martes y jueves"**, **"cada 2 semanas de lunes a viernes"**, **"cada 2 semanas los findes"**. La rama WEEKLY+days devolvía `interval=1` hardcoded → la rutina quedaba programada como **todas las semanas** (cadencia errónea, tareas duplicadas, recordatorios mal cadenciados) Y la frase de intervalo ("cada 2 semanas") no se consumía → **residuo en el título**. Una rutina quincenal se convertía en semanal | Probe JVM ciclo 54: `Gym cada 2 semanas los lunes` → antes `WEEKLY interval=1 days=1` + título `"Gym cada 2 semanas"`; tras fix → `WEEKLY interval=2 days=1` + título `"Gym"`. Auditoría `RecurrenceEngine.nextWeekly`: el motor SÍ avanza bien con interval>1 (while loop) — el bug era solo de parsing | Fix `NaturalTaskParser.kt`: helper `detectWeekInterval()` ("cada N semanas"/"cada quincena/quincenalmente") que las 4 ramas de días consumen (lo aplican a `RecurrenceResult.interval` y añaden su rango a `phraseRanges` para limpiarlo del título). Sin intervalo explícito → `interval=1` (sin regresión). +6 tests (`biweeklyIntervalWithDayListCombinesIntervalAndDays`, `quincenaIntervalWithDayListCombinesIntervalAndDays`, `triweeklyIntervalWithMultipleDaysCombinesIntervalAndDays`, `biweeklyIntervalWithWeekdayRangeCombinesIntervalAndDays`, `biweeklyIntervalWithWeekendCombinesIntervalAndDays`, `dayListWithoutIntervalKeepsWeeklyInterval`); 428 domain tests PASS (26 clases — 422 base c.53 What-Now + 6 nuevos); smoke 25 OK; NO VERIFICADO gradle/Android/UI | FIXED → VERIFIED ciclo 54 (renumerado: ciclo 53 del run paralelo What-Now aterrizó primero en remoto) |
| P2 | Planificador | `DayPlanner.build` marcaba conflicto `MOVED_FROM_SCHEDULED_TIME` comparando solo `hour*60+minute` del `startAt` original contra el `startMinute` del bloque planificado, **sin verificar que el `startAt` cayera en el día del plan**. Una tarea **iniciada ayer** (`startAt` ayer 15:00) que **vence hoy** era reubicada por el plan de hoy y marcada falsamente como "hora movida", aunque nunca tuvo hora asignada *hoy*. Ruido de conflictos espurios en el planificador | Fix `DayPlanner.kt`: antes de comparar la hora se verifica `original.toLocalDate() == date`; si el `startAt` es de otro día no se añade el conflicto; mismo día → comportamiento previo (hora distinta → conflicto real). +2 tests (`noConflictWhenStartAtIsOnADifferentDay`, `conflictStillReportedWhenStartAtIsOnSameDay`); 415 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/UI | FIXED → VERIFIED ciclo 51 |
| P1 | What Now | Un compromiso programado con `startAt` futuro (reunión/llamada/cita) que empieza en pocos minutos **no aparecía** en "¿Qué hago ahora?" aunque sea lo más urgente del momento: el ranking lo trataba como `SCHEDULED_LATER` (rank -1, último recurso) siempre que `startAt > now`, sin importar si faltaban 5 min o 5 horas. Una reunión a las 10:05 escrita a las 10:00 quedaba enterrada bajo una tarea cualquiera de la Bandeja | `WhatNowReason.IMMINENT_START` (rank 4, entre OVERDUE y DUE_TODAY); `isImminentStart(task, now)` = `startAt` futuro y dentro de `IMMINENT_WINDOW_MINUTES = 15`; UI etiqueta `what_now_reason_imminent`="Empieza enseguida" en `TodayScreen.kt`; las que empiezan más allá de 15 min siguen como `SCHEDULED_LATER`. 4 tests; 392 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/UI | FIXED → VERIFIED (392 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/UI) ciclo 46 |
| P1 | Inteligencia / Widget | `TaskRules.nextBestTask` (widget de inicio + asistente + `nextTask` del ViewModel) **no detectaba compromisos inminentes**: un `startAt` futuro dentro de 15 min caía en `isScheduledLater` (rank -1, último recurso) — el widget sugería una tarea cualquiera de la Bandeja mientras una reunión empezaba en 5 min. Inconsistencia con `WhatNowEngine` (que sí detectaba IMMINENT_START desde el ciclo 46); el widget es la superficie más vista | `isImminentStart` + `IMMINENT_WINDOW_MINUTES = 15` movidos a `TaskRules` (públicos, fuente única de verdad); `WhatNowEngine` delega (DRY, sin constante duplicada); `TaskRules.timeRank` añade rama inminente (rank 4) en el mismo orden honesto que `WhatNowEngine`. Retrocompatible: `nextBestTask(tasks)` sigue delegando con `now`/zona por defecto. 3 tests (`nextBestTask_prefersImminentStartOverInbox`, `nextBestTask_startOutsideImminentWindowStaysDeprioritized`, `nextBestTask_overdueBeatsImminentStart`); 404 domain tests PASS (tras rebase); smoke 25 OK; NO VERIFICADO gradle/Android/UI/widget | FIXED → VERIFIED (404 domain tests PASS tras rebase; smoke 25 OK; NO VERIFICADO gradle/Android/UI/widget) ciclo 47 |
| P1 | Parser | `NaturalTaskParser` fechaba en **la próxima semana** una cita de **hoy** expresada con día de la semana suelto + hora futura ("el viernes a las 18" escrito el viernes antes de las 18:00 → viernes siguiente). La rama de fecha suelta usaba `nextWeekday` (siempre +7 si hoy es el día objetivo), correcto para recurrencias pero incorrecto para cita puntual: la cita de hoy se perdía una semana entera (recordatorio 7 días tarde, dato incorrecto en planificador/What Now) | Nueva `nextWeekdayOrSame(from, target)` (devuelve hoy si coincide el día, si no delega a `nextWeekday`); rama de weekday blando la usa; descarte de "hoy si la hora ya pasó" se difiere a combinar fecha+hora (si pasado → +7). Recurrencias sin cambio. 4 tests (`weekdayHoyConHoraFuturaQuedaHoy`, `weekdayHoyConHoraPasadaRuedaProximaSemana`, `weekdayHoySinHoraYMediodiaPasadoRuedaProximaSemana`, `weekdayHoyTardeConHoraFuturaQuedaHoy`); 362 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android | FIXED → VERIFIED (362 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 42 cont.2 |
| P1 | Parser | `NaturalTaskParser` no reconocía **fechas pasadas** cotidianas ("hace N días/semanas/meses/años", "la semana/el mes/el año pasado", "hace poco"/"hace un rato"=recién). Caían a `dueAt=null` (tarea sin recordatorio, invisible en What Now/planificador) **Y** dejaban la frase temporal como basura en el título. Causa raíz: no existían `agoPattern`/`lastPeriodPattern`; además `previousWeekdayPattern` capturaba "el mes pasado" (grupo1="mes" no es día→sin fecha) y **borraba** la frase | Nuevos `agoPattern` (resta N; "poco"/"un rato"=-3h) y `lastPeriodPattern` (resta 7d/30d/365d), detectados antes de `previousWeekdayPattern` e integrados al inicio de `effectiveRelativeDueAt` (las pasadas son explícitas, prioridad sobre futuras ambiguas); hora explícita aplica sobre fecha pasada. 9 tests (`haceNdiasResuelveFechaPasada`, `elMesPasadoResuelveFechaPasada`, `hacePocoResuelveHaceTresHoras`, etc.) | FIXED → VERIFIED (329 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 38 |
| P1 | Parser | `parseMonthNameDate` descartaba **fechas imposibles** ("el 29 de febrero" no bisiesto, "el 31 de abril", "el 30 de febrero"): `LocalDate.of` lanzaba `DateTimeException`, `runCatching`→null, y el fallback **dejaba la frase temporal en el título** con `dueAt=null` (tarea sin fecha y con basura). El usuario que escribe "el 29 de febrero" quiere una fecha real, no perderla | `parseMonthNameDate` refactorizado con `java.time.Year`/`YearMonth`: Feb 29 no bisiesto→siguiente año bisiesto (2028); día>máx del mes→clamp al último día válido del siguiente año (30 abr 2027); Feb 30→Feb 28. La frase se reconoce, se borra del título y la tarea obtiene una fecha útil. 5 tests de regresión | FIXED → VERIFIED (329 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 38 |
| P1 | Parser | `NaturalTaskParser` interpretaba **"a las N horas"** como duración falsa: el sufijo "horas" no se consumía en el `timePattern`, así que "9 horas" era robado por `durationMatch` (540 min falsos) y "a las" quedaba como residuo en el título. La tarea recibía una duración absurda y ninguna hora real → recordatorio/planificación incorrectos. También la guardia de duración global filtraba el ganador tras `minByOrNull`, descartando TODOS los matches (incluido "durante 1h" válido) cuando había un match "N horas" inválido; y los conectores "durante"/"por" no se limpiaban del título tras extraer la duración | `timePatterns[0]` añade grupo opcional `(?:\s*(horas?|hs))?` para consumir el sufijo; guardia de duración refactorizada a filtro por-match ANTES de `minByOrNull` (descarta solo matches "N horas" precedidos por frase temporal, conserva "1h"/"2h"); limpieza de conector extendida a "durante"/"por". 3 tests (`aLasNHorasEsHoraNoDuracion`, `aLasNHorasConFechaNoEsDuracion`, `duranteConnectorBeforeCompactDurationIsRemoved`); 311 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android | FIXED → VERIFIED (315 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 37 |
| P1 | Parser | NaturalTaskParser no parseaba fechas relativas en **semanas/meses** ("en una semana", "en un mes", "en 3 semanas", "en 2 meses", "dentro de un mes") ni las pasadas **ayer/anteayer**: quedaban con `dueAt=null` → sin recordatorio, invisibles en planificador/What Now → la tarea se olvidaba. Bug de regex `meses?` (no coincide con singular `mes`) y `parseWrittenNumber` limitado a `doce` | 11 tests nuevos (`enUnaSemanaParsesDueAt`, `enUnMesParsesDueAt`, `enDigitSemanasParsesDueAt`, `enDigitMesesParsesDueAt`, `dentroDeUnMesParsesDueAt`, `enQuinceDiasParsesDueAt`, `semanaRelativaRespetaHoraExplicita`, `ayerParsesDueAtYesterday`, `anteayerParsesDueAtTwoDaysAgo`, `ayerConHoraResuelveAyerNoHoy`, `anteayerConPartoDeDiaResuelvePasado`); 249 domain tests PASS | FIXED → VERIFIED (249 domain tests PASS; NO VERIFICADO gradle/Android) ciclo 29 |
| P1 | Parser | NaturalTaskParser no parseaba **años** ("en un año", "en 2 años", "dentro de un año") ni el **período próximo** ("la semana que viene", "el mes que viene", "el año que viene", "el próximo mes", "la próxima semana"): `dueAt=null` y la frase "que viene"/"próximo X" quedaba como residuo en el título → la tarea no tenía recordatorio, era invisible en planificador/What Now y se olvidaba. Formas cotidianísimas para plazos largos y posponer a la siguiente unidad | 10 tests nuevos (`enUnAnioParsesDueAt`, `enDosAniosParsesDueAt`, `dentroDeUnAnioParsesDueAt`, `enUnAnioRespetaHoraExplicita`, `semanaQueVieneParsesDueAt`, `mesQueVieneParsesDueAt`, `anioQueVieneParsesDueAt`, `proximoMesParsesDueAt`, `proximaSemanaParsesDueAt`, `mesQueVieneRespetaHoraExplicita`); probe anti-falsos-positivos (pasado/otros contextos OK); 259 domain tests PASS | FIXED → VERIFIED (259 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 30 |
| P1 | Parser | Recurrencia mensual anclada a día del mes ("el 15 de cada mes") NO se parseaba: el día quedaba como residuo en el título ("Pagar la cuenta el 15 de") y `dueAt=null` → la tarea mensual nunca tenía fecha y los recordatorios no disparaban. Además, sufijo de hora "de la manana" creaba falsa fecha "8 de la" que anulaba la resolución de fecha de repeticiones mensuales/semanales con hora | probe JVM (12 casos): `due=2026-08-15` tras anclaje; `Renta el 10 de cada mes a las 8 de la manana` → `due=2026-08-10 08:00` (antes `due=hoy`); 3 tests `parsesMonthlyDayOfMonthRecurrence`, `parsesMonthlyDayOfMonthTodayInclusive`, `monthlyDayOfMonthKeepsExplicitTime` | FIXED → VERIFIED (212 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 17 |
| P1 | RecurrenceEngine | Avance mensual usaba `base.plusMonths(interval)` que clampa días 29-31 al último día del mes destino (ene 31 → feb 28), derivando el ancla de "el 31 de cada mes" (31→30→30…) e inconsistente con `NaturalTaskParser.nextMonthlyDate` (que salta meses sin el día). La funcionalidad mensual del ciclo 17 quedaba a medias: parser generaba la primera fecha correcta pero al completarla el engine rompía la promesa del día | 3 tests `monthly_anchorsToDayOfMonthAndSkipsMonthsLackingIt` (ene31→mar31 no feb28), `monthly_preservesDayForCommonDays` (15→15), `monthly_advancesPastCompletedAt` (completado tardío no regresa al pasado) | FIXED → VERIFIED (215 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 18 |
| P1 | NaturalTaskParser | Recurrencias de **intervalo** sin fecha explícita (diaria "cada día", quincenal "cada 2 semanas", mensual/anual "cada mes"/"cada año") dejaban `dueAt=null` → la primera ocurrencia era **invisible**: no aparecía en What Now/planificador, recordatorio nunca disparaba (`reminderAt ?: dueAt` ambos null), y se olvidaba hasta su primer completado. Simétrico al bug mensual de los ciclos 17-18 | 2 tests actualizados (`parsesDailyRecurrence`, `parsesIntervalRecurrence` de `assertNull(dueAt)` a fecha de captura) + 3 nuevos (`parsesDailyRecurrenceWithTime`, `parsesYearlyRecurrence`, `parsesIntervalRecurrencePreservesExplicitDate`) | FIXED → VERIFIED (218 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 19 |
| P1 | NaturalTaskParser | Recurrencia quincenal con **palabra** ("nómina cada quincena", "reporte quincenalmente"): `intervalPattern` solo admite dígitos ("cada 2 semanas"), así que la forma con palabra no casaba → `recurrence=NONE`, `dueAt=null` → tarea recurrente **invisible** (sin fecha, sin recordatorio, ausente de What Now/planificador → nómina/pago quincenal se olvidaba). Cadencia cotidiana real en español (día 15 y fin de mes / cada 2 semanas) | Patrón dedicado `cada quincena|quincenalmente|todas las quincenas` → `WEEKLY` interval=2 (cada 2 semanas ≈ quincena), sin añadir enum ni migración (representación honesta, reutiliza el avance semanal existente y el anclaje a fecha de captura del ciclo 19). 3 tests (`cadaQuincenaParsesBiweeklyRecurrence`, `quincenalmenteParsesBiweeklyRecurrence`, `cadaQuincenaRespetaFechaExplicita`) | FIXED → VERIFIED (365 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 42 |
| P1 | Summary | `dailySummary` contaba `completedToday`/`completedWeek`/`dueToday` sobre TODAS las tareas (incluidas subtareas): al completar un padre con N subtareas (auto-completadas en cascada), el resumen mostraba N+1 en vez de 1 (doble conteo de tareas lógicas). Infla la percepción de progreso del día | `SummaryEngineTest.summaryCountsOnlyRootTasks` (parent+2 subtasks completados → completedToday=1, no 3) | FIXED → VERIFIED (222 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 20 |
| P1 | Search | Buscar "nota X" no encontraba notas cuyo contenido no contiene la palabra "nota" (casi nunca la incluyen); las notas quedaban invisibles para la búsqueda por tipo. Asimetría con tareas/conversaciones/compromisos que ya tenían `semanticMatches` | `SearchEngineTest.noteTypeFilterDoesNotRequireTheWordNotaInContent` | FIXED → VERIFIED (222 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 20 |
| P1 | NaturalTaskParser | **Pérdida de datos en rutinas semanales de varios días.** "reunión los lunes y jueves" (la forma natural más común en español) solo capturaba un día (los patrones `todos los X`/`los X` admitían un solo día; solo `cada X y Z` admitía dos) y dejaba "y jueves" como residuo en el título → la rutina repetía **solo el primer día** y la cita/reunión no aparecía en los días correctos | probe JVM: `title='reunión y'`, `recurrenceDays='1'` (antes) → `title='reunión'`, `recurrenceDays='1,4'` (después); 3 tests `parsesLosWeekdaysWithY`, `parsesTodosLosWeekdaysWithY`, `parsesCommaDayList` | FIXED → VERIFIED (221 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 20 |
| P1 | NaturalTaskParser | **Pérdida de datos en listas de días sin coma + plurales sábados/domingos.** "los lunes miércoles y viernes" (forma informal, sin coma entre los dos primeros) solo capturaba "lunes" y dejaba "miércoles y viernes" como residuo en el título → rutina que repetía un solo día en silencio (pérdida de los demás días, recordatorios no disparaban). Además los plurales "sábados"/"domingos" no casaban (patrón singular con `\b`) y se perdían. Simétrico y complementario al ciclo 20 (que añadió el conector ","/"y"). | Separador opcional en `dayListPattern` (`(?:,|y)?`): solo casa cuando la palabra siguiente es otro día, sin robar texto ("los lunes con el equipo" para en "lunes"); plural `s[aá]bados?|domingos?`; 3 tests `parsesDayListWithoutCommaSeparator` (1,3,5), `parsesDayListWithPluralSabadoDomingo` (2,4,6), `dayListStopsAtNonDayWord` (no roba "con el comité") | FIXED → VERIFIED (350 domain tests PASS tras segundo rebase sobre origin/openhands/autonomous-ordia integrando run concurrente; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 41 |
| P1 | Privacy | Fragmentos de paquete sin punto (banca genérica) no se filtraban | `ContextPrivacyFilterTest` | FIXED |
| P1 | Capture | `StartActivityAndCollapseDeprecated` en tile de Quick Settings | lint | FIXED |
| P1 | UI | `stringResource` fuera del ámbito composable en `TaskDetailScreen` | lint | FIXED |
| P1 | Parser | Fecha numérica sin año en el pasado NO rodaba al año siguiente | probe JVM; `numericDateWithoutYearInPastRollsToNextYear` | FIXED → VERIFIED (155 domain tests PASS) |
| P1 | Parser | "esta mañana/tarde/noche" no reconocidas; "esta mañana" se leía como "mañana" | probe JVM; `estaMananaIsNotMistakenForTomorrow` | FIXED → VERIFIED (155 domain tests PASS) |
| P1 | Parser | "urgente" como palabra inicial no se detectaba sin prefijo !/# | probe JVM; `leadingUrgenteSetsUrgentPriority` | FIXED → VERIFIED (155 domain tests PASS) |
| P1 | Notas | `NoteBlockCodec.decode` perdía TODOS los bloques si un único elemento del array JSON estaba malformado | probe JVM; `singleMalformedElementDoesNotDiscardValidBlocks` | FIXED → VERIFIED (155 domain tests PASS; 11 tests nuevos) |
| P2 | Parser | `NaturalTaskParser` no reconocía números escritos en tiempo relativo ("en dos horas", "dentro de tres días", "en una hora") | probe JVM: `due=null`; tests `writtenNumberRelativeHoursParsesDueAt` etc. | FIXED → VERIFIED (155 domain tests PASS; 8 tests nuevos) |
| P2 | Tests | `DomainSmoke.kt` obsoleto tras ampliar `SearchKind` | `run_domain_checks.sh` reproducido; alineado con `SearchEngineTest` | FIXED → VERIFIED (smoke 25 assertions OK) |
| P1 | Parser | "de la tarde/noche/mañana" como meridiem se ignoraba: "a las 4 de la tarde" → 04:00 en vez de 16:00; "al mediodía"/"a la medianoche" dejaban "al"/"a la" en el título; "esta mañana" dejaba "esta" huérfano (orden de limpieza destruía la frase) | probe JVM; 8 tests nuevos `deLaTardeAppliesPmOffset`, `alMediodiaParsesNoonAndCleanTitle`, `estaMananaCleanedFullyFromTitle` etc. | FIXED → VERIFIED (163 domain tests PASS; smoke 25 OK) |
| P1 | Parser | Contexto PM de parte del día NO se aplicaba a hora sin meridiem: "esta tarde a las 4" → 04:00 (AM); "esta noche a las 9" → 09:00 (AM); "mañana a la tarde" → 09:00 + "a la tarde" en título; "12 de la noche" → 12:00 (mediodía) en vez de 00:00; "de la madrugada" no reconocido | probe JVM `Probe2.kt`; 6 tests `estaTardeConHoraSinMeridiemAplicaPm`, `doceDeLaNocheEsMedianoche`, `deLaMadrugadaEsAmYLimpiaTitulo` etc. | FIXED → VERIFIED (169 domain tests PASS; smoke 25 OK) |
| P3 | Parser | "salir de madrugada" (sin "a las"/"a la") no reconocido; deja "de madrugada" en título, dueAt null | probe JVM | ABIERTO (caso raro) |
| P3 | Parser | "a las 24" → null (24:00 = medianoche válido pero no aceptado); "a las 3.5" → ".5" suelto en título | probe JVM | PARCIAL: "a las 24" RESUELTO ciclo 7 (172 tests OK); "a las 3.5" sigue ABIERTO |
| P3 | Parser | Residuos de título no limpiados: "que viene" tras fecha ("el viernes que viene"); "del" huérfano ("a las 3pm del jueves", "reunión del jueves") | probe JVM ciclo 7 | FIXED ciclo 8 (commit pendiente); `weekdayPattern` extendido con prefijo `el\|del\|de` y sufijo `que viene\|próximo(s\|a)`; 6 tests nuevos, 178 OK |
| P3 | Parser | "a primera hora" (sin interpretar/limpiar; queda en título, debería ser ~09:00 inicio de jornada) | probe JVM ciclo 7 | FIXED → VERIFIED ciclo 9 (`primeraHoraPattern` + `primeraHoraTime` 09:00 como fallback de `parsedTime`; limpieza de título tras `standalonePartOfDayPattern`; 4 tests nuevos, 182 OK) |
| P3 | Parser | Rango horario "de 18 a 20" no parseado (dueAt=null, title y hora nulos) | probe JVM ciclo 7 | FIXED → VERIFIED ciclo 16 (`timeRangePattern`; duración=(H2-H1)*60; guard anti-falso-positivo requiere unidad o hora≥13; 5 tests, 209 OK) |
| P1 | Parser | "N min antes" clasificado como DURACIÓN en vez de recordatorio: patrón reminder #2 `(\d)(minutos?\|horas?\|días?) antes` no aceptaba abreviatura `min`/`hora`; caía al patrón de duración `\d min` | probe JVM ciclo 10; 2 tests `parsesAbbreviatedMinBeforeAsReminder`, `parsesAbbreviatedMinBeforeAsReminderWithoutVerb` | FIXED → VERIFIED ciclo 10 (184 tests OK; smoke 25 OK) |
| P2 | Parser | ~~`#tag`/`@tag` no se limpia del título ni asigna categoría explícita (cat se infiere por keywords, ignora tag del usuario)~~ RESUELTO ciclo 11: `explicitCategoryPattern` reconoce categorías conocidas, prioridad sobre keywords, limpia título | probe JVM ciclo 10 | FIXED |
| P2 | Parser | "2h" compacto no reconocido como duración ("Trabajar 2h" → dur=null, "2h" en título) | probe JVM ciclo 10 | FIXED → VERIFIED ciclo 12 (patrón `\b(\d)\s*h\b`; unit check `h`→horas; 4 tests, 193 OK) |
| P2 | Parser | `prioridad alta:` y `urgente`/`importante` a mitad de frase no fijan prioridad ("Llamar mamá urgente" → NORMAL) | probe JVM ciclo 10 | FIXED → VERIFIED ciclo 13 (sufijo "urgente"/"importante" final → URGENT/HIGH + guard de negación "no es urgente"; 4 tests, 197 OK) |
| P2 | Parser | Duraciones fraccionarias sin dígitos ("media hora", "(un) cuarto de hora") no reconocidas: `durationMinutes`=null y residuo en el título | probe JVM ciclo 14 | FIXED → VERIFIED ciclo 14 (`fractionalDurationPattern`, guard "hora" para no confundir "cuarto"=habitación; 5 tests, 202 OK) |
| P3 | Parser | "Reunión de 30 minutos" deja residuo "de" en título | probe JVM ciclo 10 | FIXED → VERIFIED ciclo 16 (conector "de" antes de duración numérica se elimina junto; 2 tests, 209 OK) |
| P3 | Rutinas | `saveRoutine` delete-then-reinsert no atómico: crash entre borrado e inserción dejaba pasos parciales/perdidos | `OrdiaViewModel.saveRoutine`; `RoutineStepDao.replaceSteps` (`@Transaction`); smoke+202 domain tests OK | FIXED → NO VERIFICADO (Room/DAO requiere Android) ciclo 15 |
| P2 | Resumen/Plan | `SummaryEngine.remainingMinutesToday` (badge "Xm" de Today) sumaba `durationMinutes` en bruto mientras `DayPlanner` coerciona cada tarea a 10..180 → el headline y el plan del día discrepaban (tarea 600m → "600m" pero el plan agenda 180m; tarea por defecto 5m → 5m cuando el plan la trata como 10m) | `SummaryEngineTest.remainingMinutesCoercesPerTaskToPlanBounds` (600→180, 5→10) + `remainingMinutesMatchesDayPlannerScheduledMinutes` | FIXED → VERIFIED (223 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 20 |
| P1 | Rutinas | `RoutineRules.wasRunToday` solo contaba tareas **pendientes** creadas hoy. Al completar todas las tareas de la rutina de hoy, `wasRunToday` devolvía `false` y un re-disparo (reabrir app/worker/tap manual) volvía a añadir los pasos → **tareas duplicadas** en la bandeja justo cuando el usuario había sido productivo. El guardia anti-duplicados se derrotaba a sí mismo | `RoutineRulesTest.wasRunTodayTrueWhenCreatedTodayAndCompleted` + `wasRunTodayTrueWhenTodayBatchPartiallyCompleted` (corregido `wasRunTodayFalseWhenTaskCompleted` que codificaba el bug) | FIXED → VERIFIED (224 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/ViewModel real) ciclo 20 |
| P1 | Parser | **"mañana por la tarde/noche/mañana" roto.** El conector "por la" no era reconocido como parte del día suelta (solo "a la"/"de la"); la frase cotidianísima "mañana por la tarde" dejaba "por la tarde" como residuo en el título **y** usaba 09:00 en vez de la hora canónica (15:00). "mañana por la mañana" dejaba "por la" huérfano | 4 tests TDD `mananaPorLaTardeDefineFechaYHoraCanonicaYLimpiaTitulo`, `mananaPorLaNocheEs21hYLimpiaTitulo`, `mananaPorLaMananaEs9hYLimpiaTitulo`, `mananaPorLaTardeConHoraSinMeridiemAplicaPm` | FIXED → VERIFIED (232 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/UI) ciclo 21 |
| P3 | Parser | "este fin de semana" / "el fin de semana" → `dueAt=null` (sin soporte de fin de semana). "pasado mañana por la tarde" verificado OK en ciclo 22 (conector "por la" + fecha "pasado mañana" se limpian juntos, due=+2d 15:00) | probe JVM ciclo 21 | ABIERTO (fin de semana; no pérdida de datos) |
| P1 | Parser | **Días relativos + hora explícita perdían la hora.** "dentro de 3 días a primera hora" → el parser tenía `parsedTime` (09:00) y `relativeDueAt` (+3d) pero la rama `dueAt` usaba `today + parsedTime` (perdía los 3 días) o descartaba la hora y usaba la fecha relativa a medianoche. Ambos caminos incorrectos | 3 tests `dentroDe3DiasAPrimeraHoraCombinaFechaRelativaConHora`, `dentroDeNDiasConHoraExplícitaCombinaFechaYHora`, `en3DiasALasCincoDeLaTardeCombinaFechaYHora` | FIXED → VERIFIED (235 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/UI) ciclo 22 |

| P1 | Self-Update/Delivery | `android-ci.yml` publicaba releases con tag `v3.0.0-build.N` y asset `Ordia-3.0-signed.apk`, pero `UpdateSecurityRules.parseVersionCodeFromTag`/`expectedApkName` exigen `v3.0.N-code-C` y `Ordia-3.0-code-C.apk` → el auto-updater nunca detectaba actualización (parseTag→null). Además la rama autónoma no disparaba CI | `UpdateSecurityRulesTest` (cubre el naming); `openhands-delivery.yml` con naming correcto; CI run 31505311240 publicó release `v3.0.8-code-1300000801` con assets `Ordia-3.0-code-1300000801.apk`+`.sha256` | FIXED → VERIFIED (release publicada con naming exacto que el updater acepta; ciclo 24) |
| P0 | Self-Update | `app/build.gradle.kts` buildType `release` declaraba `SELF_UPDATE_ENABLED=false`; en AGP un campo de buildType pisa al del flavor → `previewAdvancedRelease`/`previewFullRelease` compilaban con self-update DESACTIVADO aunque el flavor dijera `true` → APK firmada jamás buscaría/instalaría actualizaciones | `OrdiaUpdateManager.schedule()/checkDetailed()` retornan inmediatamente si `!BuildConfig.SELF_UPDATE_ENABLED`; código HEAD confirmado sin override; `UpdateInstallActivity` compilado en `classes.dex` de la APK firmada | FIXED → VERIFIED (APK `Ordia-3.0-code-1300000801.apk` firmada con v2 + updater presente en DEX; ciclo 24) |
| P1 | Self-Update | End-to-end N→N+1: instalar versión N, publicar N+1, comprobar que Ordía detecta/descarga/verifica/instala y conserva datos | Sin emulador/dispositivo Android en el agente; cadena de seguridad cubierta por tests contract + verificación estática de la APK firmada | BLOCKED-external — requiere dispositivo Android real |
| P1 | Supervisor | v1: lock `fcntl` solo Unix, cron rehabilitado solo en `finally` (irrecuperable ante kill -9/apagón), sin lock distribuido (supervisor+watchdog+cron podían pisarse) | `tools/test_supervisor.py` (lock cross-platform, lease TTL, concurrencia=1, backoff); `ordia_supervisor.py` v2 + watchdog GitHub Actions | FIXED → VERIFIED (tests Python PASS; NO VERIFICADO loop infinito en entorno persistente) |
| P2 | Delivery | Sin workflow de entrega en `openhands/autonomous-ordia` (la rama autónoma no producía APKs instalables) | `openhands-delivery.yml` (gates tests+lint+assemble+sign+sha256+release); concurrency cancela obsoletas; CI run 31505311240 ✓ completa → release `v3.0.8-code-1300000801` | FIXED → VERIFIED (ciclo 24) |
| P2 | Supervisor | Sin modo servicio (requería terminal abierta); no cloud-first | `tools/docker-compose.yml`, `tools/ordia-supervisor.service`, `tools/install-supervisor.sh`, `tools/ordia-status.py` | FIXED → NO VERIFICADO (requiere despliegue en VM/servidor) |
| P2 | UX/UI (Today) | `TodayScreen` mostraba What Now DOS veces: como CompactAction en la fila de 3 botones AND como Card dedicada con eyebrow+razón justo debajo. Mismo destino (`onTask(whatNow.id) ?: onOpenInbox`), dos entradas = ruido visual y jerarquía confusa | `TodayScreen.kt`: quitado el action duplicado; fila de 3→2 actions equilibrados (Revisar mensajes, Nota rápida), spacing 8→10dp. Card What Now (eyebrow "SIGUIENTE PASO") intacta. CI run 31520002066 success (tests+lint+assemble ✓, release v3.0.11-code-1300001101 publicada) | FIXED → VERIFIED (CI; NO VERIFICADO en dispositivo) ciclo 25 |
| P1 | Parser | **Regresión ciclo 30:** "fin de semana que viene" se rompió al añadir el período próximo. "semana que viene" coincidía dentro de "fin de semana que viene", dejaba residuo "fin de" en el título **y** programaba +7d en vez del próximo sábado (tarea mal fechada) | Probe JVM: antes title=[fin de] due=2026-08-05; después title=[Viaje] due=2026-08-01; test `finDeSemanaQueVieneProgramaProximoSabadoYLimpiaTitulo` | FIXED → VERIFIED (260 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 31 |
| P1 | Parser | `NaturalTaskParser` no parseaba **"próximos días"** (con o sin "en los/el/las"): forma cotidiana y deliberadamente vaga de "dentro de poco". `dueAt=null` → tarea olvidada (sin recordatorio, invisible en planificador/What Now). El `nextPeriodPattern` (ciclo 30) solo cubría semana/mes/año; "próximos días" no los contiene | Extensión de `nextPeriodPattern` + rama `else -> 3L` (heurística honesta, +3d); 3 tests (`proximosDiasParsesDueAt`, `proximosDiasConPrefijoParsesDueAt`, `proximosDiasRespetaHoraExplicita`); reutiliza combinación con hora y limpieza de título | FIXED → VERIFIED (263 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 32 |
| P1 | Parser | `NaturalTaskParser` no parseaba **"antier"** (variante coloquial hispanoamericana de "anteayer", MX/CA/parts SA): `dueAt=null` → tarea vencida olvidada (sin recordatorio, invisible en What Now). El patrón de `anteayer` era literal `\banteayer\b` y no incluía la variante regional | Extensión de la rama `anteayer` a `\banteayer\b\|\bantier\b` (misma fecha, -2d) + limpieza de título; 2 tests (`antierParsesDueAtTwoDaysAgo`, `antierConHoraResuelvePasadoNoHoy`); reutiliza combinación con hora explícita | FIXED → VERIFIED (265 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 32 |
| P1 | Parser | `NaturalTaskParser` no parseaba **“próximo trimestre” / “trimestre que viene”** (con o sin artículo “el”): plazo largo cotidiano (impuestos trimestrales, revisiones, informes). `dueAt=null` → tarea olvidada (sin recordatorio, invisible en planificador/What Now). El `nextPeriodPattern` (ciclo 30) solo cubría semana/mes/año | Extensión de `nextPeriodPattern` (añade `trimestre` como unidad en ambas ramas) + rama `trimestre -> 90L` (+90d = 3×30d); **la rama `trimestre` se comprueba ANTES que `mes`** porque “trimestre” contiene la subcadena “mes”; 3 tests (`proximoTrimestreParsesDueAt`, `trimestreQueVieneParsesDueAt`, `proximoTrimestreRespetaHoraExplicita`); reutiliza combinación con hora y limpieza de título | FIXED → VERIFIED (268 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 32 |
| P1 | Parser | `NaturalTaskParser` no parseaba **"fin de mes" / "a finales de mes" / "mediados de mes"** (con "del" o "de"): vencimientos mensuales cotidianos (alquiler, tarjeta de crédito, servicios, facturas). `dueAt=null` → vencimiento olvidado (sin recordatorio, invisible en planificador/What Now). La aritmética `now + millis` del parser no calculaba "último día del mes" ni "día 15". "fin de mes" contiene la subcadena "mes" → riesgo de colisión con "mes que viene" | Nuevos patrones `endOfMonthPattern`/`midOfMonthPattern` detectados y borrados ANTES del período próximo (evita colisión con "mes que viene"); fecha absoluta vía `LocalDate.withDayOfMonth(lengthOfMonth())` (fin de mes) / `withDayOfMonth(15)` (mediados), rodando al mes siguiente si hoy ya es el objetivo; integrados en `effectiveRelativeDueAt` como días para combinar con hora explícita ("fin de mes a las 18"); 7 tests (`finDeMesParsesDueAtUltimoDiaMesActual`, `aFinalesDeMesParsesDueAt`, `finDeMesRespetaHoraExplicita`, `finDeMesRuedaAProximoMesSiHoyEsUltimoDia`, `mediadosDeMesParsesDueAtDia15ProximoMes`, `mediadosDeMesResuelveDia15MesActualSiAunNoLlega`, `finDeMesNoColisionaConPeriodoProximo`) | FIXED → VERIFIED (275 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 32 |
| P1 | Parser | `NaturalTaskParser` no parseaba **“principios de mes”** (vencimientos el día 1: rentas, pagos, cierres). `startOfMonthPattern` existía como regex pero sin resolución → `dueAt=null` o caía a +30d por “mes” → tarea olvidada | `startOfMonthPattern` + rama en `monthBoundaryDueAt` (`withDayOfMonth(1)`, rueda al mes siguiente si hoy>1); detectado ANTES del período próximo; 4 tests | FIXED → VERIFIED (286 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 33 |
| P1 | Parser | **“fines de semana” (plural)** no generaba recurrencia: el patrón singular “fin de semana” no casaba “fines” (quedaba como residuo) y no existía hábito de sábado+domingo. “cada fines de semana”/“los findes” se olvidaba o aparecía una sola vez | `weekendRecurrencePattern` → `RecurrenceFrequency.WEEKLY`, `days=[6,7]` (CSV “6,7”); consume “cada”/“los” inicial; singular sigue siendo fecha única; 2 tests | FIXED → VERIFIED (286 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 33 |
| P1 | Parser | **“el jueves pasado” / “el último lunes” / “el martes anterior”** se fechaban en el FUTURO erróneamente: `weekdayPattern` capturaba el día como próximo y “pasado” quedaba como residuo en el título → tarea vencida mal fechada (no aparecía como atrasada en What Now). El orden inverso “último lunes” tampoco casaba | `previousWeekdayPattern` (forward) + `previousWeekdayReversedPattern` (inverso) + función `previousWeekday()` (última ocurrencia pasada, excluye hoy); detectados ANTES de `weekdayPattern`; rama en `effectiveRelativeDueAt` combina con hora; 5 tests | FIXED → VERIFIED (286 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/UI) ciclo 33 |
| P1 | Parser | `NaturalTaskParser` no reconocía **"mediados de semana"** / "a mediados de semana" (frase cotidiana: "lo termino a mediados de semana"). Caía a `dueAt=null` → tarea **olvidada** (sin recordatorio, invisible en planificador/What Now). Existían `startOfWeekPattern` ("principios de semana" = lunes) y `midOfMonthPattern` ("mediados de mes" = día 15), pero ninguna familia cubría el "mediados" de semana | Nuevo `midOfWeekPattern` análogo a `startOfWeekPattern` y `midOfMonthPattern`: resuelve al **miércoles más cercano en HOY o futuro** (`nextOrSame(WEDNESDAY)`), hora por defecto 9:00, combinable con hora explícita; detectado y borrado ANTES del período próximo (no colisiona con "semana que viene"); integrado en `effectiveRelativeDueAt` y `relativeIsDays`; 4 tests (`mediadosDeSemanaSiHoyEsMiercolesEsHoy`, `mediadosDeSemanaDesdeLunesEsMiercolesMismaSemana`, `mediadosDeSemanaRespetaHoraExplicita`, `mediadosDeSemanaNoColisionaConMediadosDeMes`) | FIXED → VERIFIED (312 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 36 |
| P1 | Parser | `NaturalTaskParser` no reconocía **"a finales de semana"** / "finales de semana" (forma plural cotidiana análoga a "finales de mes": "lo termino a finales de semana"). `weekendPattern` solo casaba `fin de semana` (singular); "finales" no casaba y la frase quedaba como residuo en el título con `dueAt=null` → tarea **olvidada**. Brecha simétrica frente a `endOfMonthPattern` (que sí acepta "finales de mes") | Ampliación mínima de `weekendPattern`: `(?:fin|finales)\s+de\s+semana` + prefijo opcional `a ` (como los patrones de mes); reutiliza todo el flujo existente (detección temprana, borrado, resolución a **próximo sábado**, hora canónica 9:00, combinable con hora explícita); **no** acepta `fines` (f-i-n-e-s) que sigue siendo recurrencia WEEKLY sáb+dom en `parseRecurrence`; 4 tests (`aFinalesDeSemanaProgramaProximoSabadoYLimpiaTitulo`, `finalesDeSemanaSueltoProgramaProximoSabadoYLimpiaTitulo`, `finalesDeSemanaRespetaHoraExplicita`, `finalesDeSemanaNoColisionaConRecurrenciaFinesDeSemana`) | FIXED → VERIFIED (316 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 37 |
| P1 | Parser | **“entre semana” / “días laborables/hábiles/de semana” / “de lunes a viernes”** NO generaban recurrencia: el parser las trataba como texto suelto → tarea única (freq=NONE) que aparece una sola vez y se olvida el resto de la semana (hábitos cotidianos: gimnasio, trabajo, estudio). Peor, “de lunes a viernes” dejaba “lunes” como residuo en el título (`dayListPattern` capturaba solo “lunes”, days=[1]) y el viernes se perdía. Brecha simétrica frente a `weekendRecurrencePattern` (fines de semana → WEEKLY sáb+dom, c.33) | `weekdayRangePattern` (rango `lunes a viernes`, admite prefijo `los `/`de `) + `weekdaySetPattern` (`entre semana`, `días laborables/hábiles/de semana`), ambos → `RecurrenceFrequency.WEEKLY` `days=[1,2,3,4,5]`, evaluados **ANTES** que `dayListPattern` para que “los lunes a viernes” sea rango (no lista [“lunes”]); resuelven a la próxima ocurrencia del primer día, combinable con hora explícita; singular “fin de semana” sigue siendo fecha única. 7 tests (`entreSemanaComoRecurrenciaWeekdayLunAVie`, `diasLaborablesComoRecurrenciaWeekday`, `diasHabilesConDeterminanteComoRecurrenciaWeekday`, `deLunesAViernesComoRecurrenciaWeekdayLimpiaTitulo`, `losLunesAViernesComoRangoNoLista`, `entreSemanaRespetaHoraExplicita`, `finDeSemanaSingularNoEsRecurrenciaWeekday`) | FIXED → VERIFIED (354 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android) ciclo 41 |
| P1 | Parser | `NaturalTaskParser` no reconocía **día del mes suelto con artículo** ("reunión el 15 a las 10", "cita el 20", "entregar el 5 a las 18"): "el 15" no casaba con `numericDatePattern` (exige DD/MM con mes) y quedaba como residuo en el título; la hora suelta ("a las 10") se aplicaba a HOY → la cita se programaba **hoy** en vez del día 15 (día erróneo, reunión perdida). No existía patrón para "el N" aislado | Probe JVM ciclo 42 cont.3: `Reunión el 15 a las 10` (now=2026-07-29) → `dueAt=hoy 10:00` (debería ser 2026-08-15 10:00) | FIXED → VERIFIED ciclo 42 cont.3: nuevo `dayOfMonthPattern` (`el N` / `el N del mes`, con negative lookahead `(?!\s*de\s+[a-z…])` para no colisionar con "el 15 de marzo" —lo resuelve `monthNameDate`— ni "el 15 de cada mes" —recurrencia mensual—). Rama en `date` `when` entre `monthNameDate` y `numericDateMatch`; reutiliza `nextMonthlyDate` (ancla al día N de este mes, o del siguiente si ya pasó). Limpieza de residuo en el título. 4 tests (`diaDelMesSueltoRuedaAlProximoMesSiYaPaso`, `diaDelMesSueltoSinHoraUsaMediodiaCanónico`, `diaDelMesSueltoFuturoEsteMesSeConserva`, `diaDelMesSueltoNoColisionaConFechaConMes`); 388 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android |
| P1 | Parser | `NaturalTaskParser.relativePattern` solo admitía los prefijos `en`/`dentro de` para fechas futuras relativas. Las formas coloquiales **"de aquí a N ..."** / **"de acá a N ..."** (la manera más común y natural de "en N" en español: "llamar al dentista de aquí a tres días", "reunión de aquí a una semana", "viaje de aquí a un mes") NO casaban → `dueAt=null`, la frase quedaba como residuo en el título, tarea sin recordatorio, invisible en planificador/What Now → **olvidada**. "para dentro de N" sí funcionaba ("para" se limpia, "dentro de" casaba) | Probe JVM: `Llamar al dentista de aquí a tres días` → `dueAt=null` (debería ser hoy+3d) | FIXED → VERIFIED ciclo 50: extendido el grupo de prefijos de `relativePattern` de `(?:en|dentro\s+de)` a `(?:en|dentro\s+de|de\s+aqu[íi]\s+a|de\s+ac[aá]\s+a)`. El resto del patrón (cantidad + unidad) y la lógica de cálculo (`relativeDueAt`, `now + millis`) se reutilizan sin cambios; la limpieza de título ya elimina todo el match incluido el prefijo. Heurística honesta, riesgo de falso positivo bajo. 5 tests (`deAquiATresDiasParsesDueAt`, `deAquiAUnaSemanaParsesDueAt`, `deAquiAUnMesParsesDueAt`, `deAquiANDiasRespetaHoraExplicita`, `deAcaAUnaSemanaParsesDueAt`); 413 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/UI |

| P1 | Guardia/datos | `GuardianEngine` contaba subtareas como tareas lógicas en `completedAll`, `completedToday`, `overdue` y `derivedExperience(completedTasks)` — inflaba el ánimo (mood CONCERNED/"Hay 5 pendientes atrasados" con 1 tarea lógica) y la XP (48 en vez de 12 por 1 padre+3 subtareas). Simétrico al doble conteo de `SummaryEngine` FIXED ciclo 20; `GuardianCoach` ya filtraba raíces | Filtro `parentTaskId == null` en los 4 conteos; 2 tests (`overdueCountIgnoresSubtasksToAvoidInflatedConcern`, `derivedExperienceCountsLogicalTasksNotSubtasks`) | FIXED → VERIFIED (392 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/UI) ciclo 46 |
| P1 | Recordatorios/datos | **`ReminderActionReceiver.ACTION_SNOOZE` corrompía `reminderAt` en recurrentes.** Posponer (snooze) un recordatorio sobrescribía `task.reminderAt = now + 10min`. Para una tarea recurrente eso destruye el offset `reminderOffset = dueAt - reminderAt` que `RecurrenceEngine.nextOccurrence` reutiliza en **TODAS** las ocurrencias futuras: un recordatorio "15 min antes" pasaba a "5 min antes" para siempre tras un solo snooze (de forma silenciosa e irreversible — preferencia del usuario perdida). Bug de integridad de datos: una acción transitoria (aplazar 10 min la notificación) mutaba permanentemente la preferencia de cuándo avisar | Extraída la lógica a `ReminderRules.snooze` (dominio puro, testable): el aplazamiento es **transitorio** (vive solo en el worker vía `SnoozeResult.triggerAt` que agenda `ReminderScheduler.scheduleAt`, persistido por WorkManager); `reminderAt`/`dueAt`/`startAt` se preservan intactos, solo se actualiza `updatedAt`. 6 tests en `ReminderRulesTest` incl. la invariante `snoozeThenComplete_preservesReminderOffsetAcrossRecurrence` que reproduce el bug (offset 15min preservado tras snooze+completar; antes quedaría en 5min) | FIXED → VERIFIED (421 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/Room con DAOs reales) ciclo 52 |
| P1 | Parser | `NaturalTaskParser.parseRecurrence` no reconocía el **intervalo de cadencia con número escrito** ("cada dos semanas", "cada tres meses", "cada quince días", "cada dos años"): `intervalPattern` sólo admitía `\d{1,3}` (dígitos) → la regex no casaba, la rama caía a `recurrence=NONE` y `dueAt=null` → tarea recurrente **olvidada** (invisible en What Now/planificador, recordatorio jamás disparaba). El helper `parseWrittenNumber` (que resuelve "dos"/"tres"/.../"quince") ya existía y se usaba en recordatorios y "un par de", pero no se había conectado a `intervalPattern`. Sutil: la rutina nacía sin fecha y solo se manifestaba cuando el recordatorio "no volvía" | Probe JVM ciclo 57: `visitar a mi madre cada dos semanas` → antes `rec=NONE due=null`; tras fix → `WEEKLY interval=2 due=hoy`; `cada tres meses` → `MONTHLY interval=3` | Fix `NaturalTaskParser.kt`: el grupo de captura del número pasa de `\d{1,3}` a `(\d{1,3}|<números-escritos>)`, alternancia acotada a los números conocidos en `parseWrittenNumber` (un–treinta, sin sobrescribir la unidad días/semanas/...). Resolución: `toLongOrNull()` (dígito) y luego `parseWrittenNumber(rawN)` (palabra), `coerceIn(1,366)`. Reutiliza infraestructura existente; sin enum ni migración. +4 tests (`cadaDosSemanasParsesWeeklyInterval2`, `cadaTresMesesParsesMonthlyInterval3`, `cadaQuinceDiasParsesDailyInterval15`, `cadaDosAnosParsesYearlyInterval2`); 439 domain tests PASS (26 clases — 435 c.55 + 4 nuevos); smoke 25 OK; NO VERIFICADO gradle/Android/UI | FIXED → VERIFIED ciclo 57 |
| P2 (descubierto, NO implementado) | Parser | `NaturalTaskParser` no reconoce **adjetivos de cadencia desnudos** ("pago mensual", "reunión semanal", "suscripción anual", "repaso diario"): `recurrence=NONE`, `dueAt=null` → tarea olvidada. Las formas con `-mente` ("mensualmente") SÍ se reconocen, pero el adjetivo sin sufijo es más común. **Riesgo de falso positivo**: "diario" puede ser sustantivo (cuaderno diario), "semanal"/"mensual"/"anual" son ambiguos (un informe mensual puede ser puntual, no recurrente). Forzar recurrencia podría crear tareas recurrentes NO deseadas (otro problema de integridad) | Probe JVM ciclo 57: `pago mensual` → `NONE due=null`; `reunión semanal` → `NONE due=null`. Decisión: documentar y no implementar sin señal desambiguadora (p.ej. exigir "cada"/"todos los" O un sustantivo de cadencia fuerte como "pago"/"factura"/"suscripción") | ABIERTO — requiere decisión de diseño sobre el umbral de falso positivo aceptable |
| P1 | Parser | **Verbo de recordatorio sin cantidad perdía el recordatorio y dejaba residuo.** "recuérdame llamar a mamá mañana a las 3 de la tarde" → `reminderOffsetMinutes=null` (aunque `dueAt` seteado) → **ningún recordatorio agendado** Y el verbo "recuérdame" quedaba en el título. El usuario pedía un aviso explícito y Ordía lo ignoraba silenciosamente → la cita se olvidaba. Los `reminderPatterns` solo programaban offset cuando extraían una cantidad explícita ("2 horas antes"); el verbo solo ("recuérdame") no activaba nada | Probe JVM ciclo 59: `recuérdame llamar a mamá mañana a las 3 de la tarde` → antes title='recuérdame llamar a mamá' offset=null due=true (BUG); tras fix → title='llamar a mamá' offset=30 due=true | Fix `NaturalTaskParser.kt`: nuevo `bareReminderVerbPattern` (`recuérdame\|avísame\|notifícame\|recordatorio\|no dejes que olvide`) detectado tras extraer recordatorios con cantidad (así el offset explícito NO cae al respaldo). Si hay verbo + `dueAt` + sin offset → 30 min antes (convención del projeto = `CommitmentEngine.DEFAULT_REMINDER_OFFSET_MS`). El verbo se elimina del título tras consumir fechas/horas. Sin `dueAt` no se falsifica offset. +5 tests (`verboRecordatorioSinCantidadConDueAplicaOffset30`, `verboAvisameSinCantidadConDueAplicaOffset30`, `verboNoDejesQueOlvideConDueAplicaOffset30`, `verboRecordatorioSinCantidadSinDueNoFalsificaOffset`, `verboRecordatorioConCantidadExplicitaUsaOffsetExplicito`); 455 domain tests PASS (26 clases — 450 c.58 + 5 nuevos); smoke 25 OK; NO VERIFICADO gradle/Android/UI | FIXED → VERIFIED ciclo 59 |
| P1 | Parser | **Meridiem sin "a las" + hora de inicio del rango como dueAt.** "Reunión 2pm"→02:00 (debería 14:00): `explicitTimeData` leía meridiem del grupo 4 (solo patrón[0] "a las N"), pero los patrones[1]/[2] lo llevan en el grupo 3 → meridiem perdido → AM. Y en rango "de 9 de la tarde a 11 de la noche" la `dueAt` caía a la canónica "de la tarde"→15:00 en vez del inicio real 21:00 (el `rangeMatch` se computaba tras `dueAt`, así el inicio del rango no alimentaba la hora) | Probe JVM Probe4 ciclo 61: `Reunión 2pm`→due 02:00 (BUG), `Clase de 9 de la tarde a 11 de la noche`→due 15:00 (BUG) | FIXED → VERIFIED ciclo 61: `explicitTimeData` lee meridiem del grupo 4 si existe, si no del grupo 3; `rangeMatch` movido antes de `parsedTime`/`dueAt` + nuevo `rangeStartTime` (inicio absoluto del rango validado) en la cadena de respaldo de `parsedTime` (tras tiempo explícito, antes de canónicas de parte del día; solo si rango validado, no filtra rangos rechazados). +6 tests; 469 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/UI |
| P2 | Parser | **Forma standalone "N de la tarde/noche" sin rango ni "a las".** "Taller 9 de la tarde" → due=15:00 (debería 21:00): sin un segundo extremo del rango, `rangeMatch` no casaba y no había `rangeStartTime`; la hora caía a `standalonePartOfDayTime` (canónica "de la tarde"→15:00), ignorando la hora explícita 9 y dejando el número como residuo en el título ("Taller 9") | Probe JVM Probe4 ciclo 61 + Probe ciclo 62: `Taller 9 de la tarde`→due 15:00 title='Taller 9' (BUG); `Cita 10 de la mañana`→09:00 (debería 10:00); `Evento 9 de la madrugada`→04:00 (debería 09:00) | FIXED → VERIFIED ciclo 64: nuevo `standaloneHourPartOfDayPattern` `(?i)(?<![:\d])(\d{1,2})(?::([0-5]\d))?\s+de\s+la\s+(tarde\|noche\|madrugada\|ma[nñ]ana\|manana)(?!\s+de\s+[a-záéíóúüñ])` + `resolveStandaloneHourPartOfDay` (tarde/noche→+12 si N<12; 12 de la noche→0 medianoche; 12 de la tarde→12 mediodía; madrugada/mañana→AM tal cual). Se aplica SOLO cuando no hubo tiempo explícito (guard `explicitTime==null`) para no robar "9 de la tarde" de "a las 9 de la tarde" y dejar residuo "a las"; lookahead negativo evita colisión con "9 de marzo" (fecha). Insertado en la cadena de respaldo de `parsedTime` ANTES de `standalonePartOfDayTime` (canónica) para que la hora explícita gane. +9 tests; 494 domain tests PASS (26 clases — 485 c.63 + 9 nuevos); smoke 25 OK; NO VERIFICADO gradle/Android/UI |
| P2 (descubierto c.63) | Inteligencia/Guardián | `GuardianCoach.insight` daba tratamiento **idéntico** a toda tarea vencida: `Tone.GENTLE` + mensaje genérico ("Esta tarea está atrasada. Empieza con un bloque corto." / "Tienes N tareas atrasadas. Comienza por esta."), sin distinguir 10 min de retraso de 10 días. El coach no ayudaba a **recuperar** compromisos olvidados: una tarea con 3 semanas de retraso es un compromiso que se está dejando pasar, pero recibía el mismo nudge que una vencida hace 1 hora. El coach repetía "empieza por esta" sin plantear la decisión real (hacer/reprogramar/quitar) | Auditoría dominio c.63 (área de dirección explícita "detección de vencidas importantes"/"recuperación de tareas olvidadas") | FIXED → VERIFIED ciclo 63: nueva heurística honesta de aritmética temporal en `GuardianCoach.insight`: `mostOverdueDays = max((now - dueAt) / MILLIS_PER_DAY)`; si la más atrasada lleva **≥2 días** (`FORGOTTEN_DAYS_THRESHOLD`) sube a `Tone.FOCUSED` y surface cuánto lleva (`forgottenAgeLabel`: "1 día"/"3 días"/"2 semanas"/"1 mes") + plantea la decisión real ("Hazla hoy o muévela con intención" / "Elige una: hacerla hoy, reprogramarla o quitarla"); si es leve (<2 días) mantiene `GENTLE` + mensaje actual. +4 tests (`mildlyOverdueSameDayStaysGentle`, `forgottenOverdueTaskBecomesFocusedAndSurfacesAge`, `forgottenOverdueUsesWeeksLabelPastSevenDays`, `forgottenOverdueGroupSurfacesOldestAge`); test previo `overdueWorkWinsOverEverythingElse` (27h→GENTLE) sigue verde (27h<2d). 485 domain tests PASS; smoke 25 OK; NO VERIFICADO gradle/Android/UI (coach se consume en Compose) |
| P2 (descubierto c.64) | Inteligencia/Resumen | La tarjeta de resumen de Today mostraba "X completadas · Y para hoy" + badge de minutos ("120m") pero NO convertía ese dato en una decisión accionable: el usuario tenía que hacer la aritmética mental ("120m a las 12:00, ¿caben en el día?"). `DaySummary` no tenía noción de "capacidad restante del día"; `remainingMinutesToday` medía el trabajo pendiente pero no se comparaba con el tiempo libre hasta el fin de jornada → no había veredicto. Área de dirección explícita "priorización inteligencia"/"mejores resúmenes del día"/"What Now más útil" | Auditoría dominio c.64→c.65 (SummaryEngine ↔ TodayScreen) | FIXED → VERIFIED ciclo 65: nueva enum `DayLoad { LIGHT, ON_TRACK, FULL, OVERLOADED }` + campo `dayLoad` en `DaySummary`; `SummaryEngine.assessDayLoad` (dominio puro) compara `remainingMinutesToday` con `freeMinutes = (18:00 - max(now, 9:00))` (misma ventana que `DayPlanner`): <=0 restantes→LIGHT; sin capacidad→OVERLOADED; <=free/2→ON_TRACK; <=free→FULL; >→OVERLOADED. `TodayScreen.kt` añade UNA línea `bodySmall` dentro de la tarjeta existente (sin nueva tarjeta/botón) con 3 strings accionables. +6 tests en `SummaryEngineTest.kt`; 500 domain tests PASS (26 clases — 494 c.64 + 6); smoke 25 OK; NO VERIFICADO gradle/Android/UI/Room |
