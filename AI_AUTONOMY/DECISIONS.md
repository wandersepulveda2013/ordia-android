# Decisions Log
- Adopted strict black and white base palette with semantic colors to achieve the "less is more" constraint (Wave 1).
- Added `OrdiaButton` and `OrdiaCard` into `AppComponents.kt` utilizing the pre-existing `OrdiaShapes` configuration to begin constructing a unified Design System (Wave 1).
- Updated internal UI components (`ScreenHeader`, `EmptyState`) to leverage more generous spacing (Wave 1).
- Addressed `Icons.Outlined` deprecation warnings for `ArrowBack`, `ArrowForward`, `InsertDriveFile`, and `FormatListBulleted` by updating to `Icons.AutoMirrored.Outlined`.
