# DECISIONS — Ordía

| 2026-08-23 (c.937) | **Weekday genitivo tras genitivo de CONTENIDO narrativo («la primera hora de clase del lunes fue aburrida») pertenece a la cadena genitiva del sujeto: no es ancla de fecha ni token borrable (extensión H2 de la doctrina c.936). Un weekday genitivo DIRECTO sin genitivo de contenido intermedio («esa primera hora del lunes fue terrible») SIGUE siendo ancla (hermano del pin c.936 «las primeras horas del lunes son tranquilas» — el genitivo de contenido es la evidencia inequívoca que distingue narrativa de ancla).** | c.936 sólo cubría el genitivo canónico DENTRO del match (H3); con genitivo de contenido intermedio (H2/H1/«a la») el weekday seguía resolviéndose a una fecha FALSA (lun 09:00 para «la primera hora de clase del lunes fue aburrida» — hora ajena incluso al ordinal) y borrándose del título (contenido del usuario). Pero un guard sin genitivo de contenido intermedio mataría el ancla legítima «esa primera hora del lunes fue terrible» — la evidencia que separa ambos mundos es exactamente el genitivo de contenido. | `NaturalTaskParser`: bifurcación en `ordinalHoraNarrativeWeekdayRanges` (match con genitivo canónico → doctrina H3 byte-idéntica; si no, genitivo de contenido `ordinalHoraContentGenitive` no-ancla tras el match + weekday genitivo + predicado); resolución y borrado fluyen del mismo predicado; +20 tests; re-pin legítimo del pin c.936 `compuestoH2ConWeekday_pinLateralFuera` (MÁS estricto); suite 6337 PASS. | Activa |

| 2026-08-21 (c.826) | **El pasado de «deber» («debía/debí/debiste/debió/debíamos/debieron …», con «que» opcional por dequeísmo) entra en `PAST_OBLIGATION_PATTERN` como segunda rama: misma clase anti-overreach que c.824 — afirmar una obligación pasada no compromete el futuro ⇒ el guard posicional descarta el kind gobernado. El cierre de la rama es `(?!\p{L})`, NO `\b` (\b es ASCII-only y no cierra tras vocal acentuada: «debí llamar…» no casaba — misma lección que «quizá» c.649). «debido a…», «deberá/deberás» (futuro) y «debes» (presente) no casan.** | El auxiliar «deber» no exige «que» (a diferencia de tener/haber-que), así no podía compartir la estructura de la rama c.824; y una frontera `\b` tras «debí»/«debió» es inerte en regex Java/Kotlin (\w excluye vocales acentuadas), lo que dejaría el pretérito 1ª/3ª persona sin cubrir silenciosamente. | `ContextIntentEngine`: rama `deb(?:[íi]a(?:mos|s\|n)?\|isteis\|ieron\|iste\|imos\|[ií][oó]\|[íi])(?!\p{L})(?:\s+que\b)?`; +8 tests; suite 5134 PASS (5147 fusionada). | Activa |

| 2026-08-21 (c.827) | La familia «hacer/preparar/meter la maleta» se fija con UN piso objeto-anclado (verbos en alternancia + objeto `maletas?`), no con 3 pisos por verbo; clasifica TASK (renumerado de c.823 a c.827 por colisiones — el hermano tomó c.823 «mandar», c.824, c.825 «encargar» y c.826 «deber pasado») | «hacer»/«preparar» ya son keywords genéricas y «meter» es bivalente (la pata/ruido/gol): un piso por verbo abriría capturas falsas; el objeto «maleta» es el ancla inequívoca (misma doctrina que «hacer copia de seguridad» c.774). TASK: gobierna el equipaje (preparación), no el desplazamiento (TRAVEL es para «viaje/vuelo/hotel») | Piso objeto-anclado con alternancia de verbos (hacer|preparar|meter) + plural idioma «las maletas»; plantilla de título capitaliza el verbo DESDE el match (3 alternativas — no fijable como c.774); lockstep keyword-OBJETO «maleta»; guards en tests (negada, «quizá…», pasado «hice/preparó/metí», nominal «la maleta está hecha», «meter la carta» → NULL) | Activa |
| 2026-08-21 (c.829) | «echar gasolina» clasifica como ERRAND (no TASK ni SHOPPING) con piso objeto-anclado al combustible (`gasolina|gasoil|diésel`) sobre el verbo bivalente «echar» (renumerado de c.827 a c.829 por colisión con el hermano c.827 «maleta» + c.828 «vaciar») | ERRAND: la forma cotidiana implica DESPLAZAMIENTO a la gasolinera (hermano de «llevar el coche al taller» c.684, anclado a lugares físicos); NO TASK (no gestión abstracta); NO SHOPPING (el verbo cotidiano es «echar», no «comprar»). El objeto combustible es el ancla: «echar agua/de menos/a perder/la culpa» quedan fuera (doctrina acotamiento al objeto c.684/c.717/c.728/c.731/c.751, hermana de maleta c.827) | Piso `ERRAND_FUEL_FLOOR` acotado al objeto + keyword-OBJETO «gasolina» en ERRAND (lockstep c.713/c.773; alimenta TRIGGER_WORDS lección c.751) + plantilla «echar gasolina»→«Echar gasolina» (lección c.616) + cláusula negación en `imperativeIsNegated` (cinturón y tirantes c.717/c.748/c.757); guards en tests (negada, hedge, pasado «eché», declarativo «la gasolina está cara», orden inverso, «echar agua al radiador» → NULL) | Activa |
| 2026-08-21 (c.824) | **Obligación/posesión PASADA («tenía/tuve/había que …», «tenía (una) cita|reunión …») que PRECEDE a la acción ⇒ DESCARTA el kind (guard posicional `pastObligationGoverns`); NO es una penalización: afirmar el pasado no compromete el futuro (principio de la familia anti-overreach c.648-c.653). Envoltura PRESENTE sobre contenido pasado («recuérdame que tenía que llamar al banco») SÍ se captura (TASK). «tendré que» (futuro) no casa. «habría/debería …» fuera de alcance (reconocen necesidad — doctrina c.649; requieren evidencia de daño real).** | La acción subordinada a un envolvente pasado se persistía como compromiso FUTURO firme (APPOINTMENT 0.67 / CALL 0.57 / MEETING 0.45), y la forma negada pasada escapaba a la rendija c.648↔c.681 (overreach / dato-falso); pero un guard de frase-completa estilo c.681 también mataría la envoltura presente legítima («recuérdame que tenía que…»). El descarte por-kind posicional conserva ambas propiedades. | `ContextIntentEngine`: `PAST_OBLIGATION_PATTERN` + `pastObligationGoverns` + guard en `scoreKind`; +16 tests; suite 5094 PASS. | Activa |

| 2026-08-21 (c.822) | «tramitar <objeto>» clasifica como TASK (no ERRAND ni APPOINTMENT): el verbo gobierna el OBJETO (pasaporte/visa/certificado/licencia), no el desplazamiento | ERRAND está anclado a destinos físicos y muchas tramitaciones son gestión remota/digital (portales de cita previa); APPOINTMENT es la cita en sí, que se captura por su propia vía — forzar otro kind duplicaría o robaría rutas | Piso TASK con ancla inicio/acuse/prefijo temporal (patrón c.691…c.711) + plantilla «Tramitar X»; guards codificados en tests (negada, «quizá…», sustantivos «tramitación»/«trámite», pasado «tramitó», verbo suelto → NULL) | Activa |
| 2026-08-21 (c.820) | El marcador del pronombre suelto «algo» en `AssistantEngine.isAgendaQuery` se casa por PALABRA (`Regex("""\balgo\b""")`), nunca por substring; la guarda de alcance temporal existente sigue exigida | Dos mentiras posibles: «algoritmo para mañana»/«algodón…» secuestrarían la agenda si fuera substring; «algo de matemáticas» robaría el menú honesto si se relajara la guarda de alcance | Coincidencia por palabra + guarda existente; guards TDD cubiertos (`algoritmoParaManana_wordBoundaryGuard_staysInMenu`, `algoDeMatematicas_sinScope_fallsBackToMenu`) | Activa |
| 2026-08-21 (c.805) | El conector genitivo «antes de/del» tras recordatorio (c.474) NUNCA se consume si lo que sigue arranca una ancla digital (día de mes «del 30», hora «de las 5»); el genitivo de contenido («antes de la reunión») SÍ se consume como siempre. Hipótesis satélite RECHAZADA formalmente: extender el consumo al artículo CON ESPACIO («de la reunión» → título «reunión») rompe el baseline codificado c.474 (títulos con artículo «la reunión» son español natural) — NO re-auditar | Sin el guard, «pago un día antes del 30» perdía el plazo completo (dueAt=null con offset suelto) = olvido seguro (P1). El consumo del artículo espaciado cambia títulos visibles codificados por 3 tests existentes sin ganancia de corrección — churn cosmético contra baseline documentado | NaturalTaskParser bucle reminderPatterns (~l.3235): guard `startsDigitalAnchor` (dígito tras el conector → no consumir); regex de conector restaurada a `(?i)^de(?:l\|la\|los\|las)?\b` | Activa |
| 2026-08-20 (c.788) | Consultas sobre superficies que el asistente NO recibe (notas) ruten a OPEN_SEARCH en vez de caer al menú | El menú genérico oculta una respuesta que SÍ existe en otra superficie: mentira por omisión cruzada; routing honesto reusa la maquinaria existente sin nueva UI | AssistantEngine.isPinnedNotesQuery → AssistantAction.OPEN_SEARCH + AssistantScreen.onSearch | Activa |

> Registro de decisiones de arquitectura/estrategia (append-only).
> Formato: `FECHA | DECISIÓN | MOTIVO | CONSECUENCIA`

| FECHA | DECISIÓN | MOTIVO | CONSECUENCIA |
|-------|----------|--------|--------------|
| 2026-08-22 (c.889) | **«hacerme/hacerse la prueba de sonido» (soundcheck del músico/técnico/evento) se captura como ERRAND — convergente con toda la familia «hacerse»; el ancla `de sonido` excluye el bivalente «prueba del coche», que sigue NULL.** | La lateral era NULL deliberado desde c.876 (guard `bivalente sonido descartada`) por la decisión de dominio pendiente; el soundcheck es una diligencia con desplazamiento al local/sala — hermana de análisis/prueba-de-sangre/tatuaje/embarazo (c.842 «la diligencia gobierna»), así el kind de la familia aplica. | Piso `ERRAND_BLOOD_TEST_FLOOR` + plantilla `matchBloodTest` extendidas con el objeto (lockstep DOS puntos, lección c.616); CERO cambios en `ContextIntent.kt` (keyword «hacer» substring, hermana c.860/c.862). 15 tests nuevos + guard c.876 convertida a regresión de captura (precedente c.843) — NO degradación, intencionalidad conservada. NO re-auditar; si se extiende a la forma desnuda «hacer la prueba…», decisión explícita en su ciclo (doctrina c.862 exige enclítico). | Activa |
| 2026-08-22 (c.883) | **La perífrasis conjugada de 1ª persona «me mido la presión/tensión» se captura como TASK con objeto-ancla; la 2ª persona «midete…», el plural social «presiones/tensiones» y el imperativo «mide…» quedan FUERA.** | En el autocontrol de la tensión arterial, la forma conjugada de presente de 1ª persona («me mido» — «I measure my blood pressure») es la MÁS cotidiana del habla; era NULL deliberado desde c.841/c.843 (guards de contrato documentaban la forma fuera-de-alcance de los pisos de infinitivo). Resuelta como candidata propia — el verbo «medir» es bivalente («medir la mesa»), así el objeto `presi[oó]n|tensi[oó]n` con `\b` singular es el ancla inequívoca (doctrina c.684/c.717/c.731); el plural social y la 2ª persona son otra semántica y siguen NULL deliberado (anti-overreach). | Piso acotado `me\s+mido` + objeto en `hasStrongTaskImperative` + plantilla de título lockstep (conjugado capitalizado desde el match, precedente c.770 «Tomarme la pastilla», doctrina c.653); CERO cambios en `ContextIntent.kt` (keywords presión/tensión preexistentes c.775/c.772); 26 tests nuevos; guards de contrato c.840/c.843 re-codificados como regresiones de captura (NO degradación: deliberada intencionalidad conservada, precedente c.843). NO re-auditar; si se extiende a «midete/mído/mide…», decisión explícita en su ciclo. | Activa |

| 2026-08-21 (c.819) | **«N veces al año» ≡ MONTHLY/⌊12/N⌋ — se extendió `timesPerPeriodPattern` (día/semana/mes → año) en lugar de crear una frecuencia nueva.** | La familia «N veces <período>» ya codificaba la doctrina (mapeo al intervalo exacto o el más próximo, truncación hacia MÁS frecuente); el año con N≤12 cabe exacto en meses (12/N), el planned-anchor (`monthlyAnchor`) ya limita cuántos años programar; extensión mínima (≈12 líneas) vs nueva frecuencia + modelo temporal | Contrato: «dos/tres/cuatro/seis veces al año»→semestral/cuatrimestral/trimestral/bimestral (6/4/3/2 meses); «una vez al año»→YEARLY/1. NO re-abrir salvo evidencia de daño real. |

| 2026-08-21 (c.817) | **«tengo que hacer algo <scope temporal>» ≡ AGENDA EXISTENTE: marcador «hacer algo» en `isAgendaQuery` protegido por la guarda de alcance temporal; SIN scope sigue al menú honesto.** Residuo documentado en openGaps c.815 (ya estaba cubierto por la maquinaria de agenda por franja; solo faltaba el marcador). | El menú genérico ante "tengo que hacer algo en la mañana" era mentira por omisión: los marcadores verbales exigían «que tengo»/«tengo algo» y la forma cotidiana no casaba. Responder con la agenda real ya implementada es el cambio mínimo, evita duplicar lógica de franja, y la guarda de alcance (¿agenda de qué día?) evita secuestrar formas sin scope. Alternativa (marcar sin guarda) robaría «tengo que hacer algo» solo y mentiría con una agenda arbitraria. | `isAgendaQuery` += `"hacer algo" in query` tras las formas oficiales; la guarda existente (`agendaPartOfDay`/`hasAgendaDateScope`/weekday/finde) la protege. Respuesta textual en línea reusa `agendaAnswer` (hoy 6..11 vía `enLaMananaHoy`; «mañana» → mañana). Sonda persistente: regresión (14 frases, 0 GAPs). Suite 5057 OK, smokes 25+9 OK. NO VERIFICADO Android/gradle/UI (sin SDK). NO re-auditar; si aparece una forma sin scope que deba rutear, ampliar la guarda explícitamente. |
| 2026-08-21 (c.816) | **«cuantas rutinas tengo» ≡ RECUENTO HONESTO: `AssistantEngine.answer` recibe `routines` y la rama cuenta las activas (hermana del recuento de pendientes c.798), NO Open Screen Routines.** Decisión de diseño abierta en openGaps c.815 (faltaba `routines` en la firma de `AssistantEngine`). | El menú genérico ante "cuántas rutinas tengo" era mentira por omisión: el dato existe en `OrdiaUiState.routines` pero no llegaba al asistente. Un recuento textual honesto (vacío: "No tienes rutinas activas."; con datos: nombra las primeras 2 activas) responde la pregunta sin nueva UI; la alternativa (abrir la pantalla de rutinas) no responde "cuántas" y es la mentira por redirección ya resuelta con notas en c.788. Cablear `state.routines` es el cambio mínimo; isActive==false se excluye (rutinas archivadas no "cuentan"). | `answer(query, tasks, activeEvents, routines, now, zone)` — param NO default (llamadores actualizados); rama cerca del recuento de pendientes con guard `activeGens`; predicado nuevo excluido de ENTITY_LISTING_FORMS. Sonda persistente: regresión «cuantas rutinas tengo» (13 frases). Suite 5054 OK, smokes 25+9 OK. NO VERIFICADO Android/gradle/UI (sin SDK). NO re-auditar; si se prefiere Open Screen, revertir rama + 3 tests. |
| 2026-08-21 (c.809) | **«qué tengo pendiente(s) de ayer» ≡ RECUPERACIÓN (rama de vencidas con `forgottenIntent`, hermana de «qué olvidé»), NO recap ni conteo frío.** Era el último openGap de la sonda c.803-b, documentado como «decisión de producto (recap vs vencidas)». | Lo que quedó de ayer SIN HACER ya está vencido hoy: la pregunta es intención de recuperación (recuperar QUÉ se pasó), no de panorama. Un recap de ayer incluiría lo hecho (no es «pendiente») y el conteo frío («Tienes N vencidas») no nombra la deuda. El camino de recuperación ya existía completo (WhatNowEngine.ordered + RUN_REPLAN + stale-inbox/missed-start honestos); solo faltaba reconocer la frase. Decisión tomada por el agente autónomo (mandato: no pedir aprobación para decisiones técnicas razonables); reversible si el humano prefiere recap. | «pendiente de ayer»/«pendientes de ayer» rutean a vencidas-forgottenIntent (nombra la más urgente, ofrece reprogramar; vacío honesto «No tienes tareas vencidas…»). «pendiente de mañana» NO casa (agenda futura, guard test). Ronda de la sonda c.803-b CERRADA (0 openGaps). NO re-auditar; si se prefiere recap, revertir estas 2 líneas + 5 tests. |
| 2026-08-20 (c.788) | **Consultas que el asistente no puede responder por falta de datos (notas) ruten a OPEN_SEARCH en vez de caer al menú genérico.** | Caer al menú cuando la app SÍ tiene superficie (SearchKind.NOTE + wantsPinned) es mentira por omisión cruzada: oculta una respuesta existente. Routing honesto = responder y abrir la vista que sí puede listar; cero UI nueva — reusa OPEN_SEARCH + AssistantScreen.onSearch. | «nota(s)» + participio «fijar» por palabra, paridad SearchEngine.PINNED_TOKENS; creación gana antes; «fijada» sin «nota» no rutea; cierra el último gap (v) de la sonda c.779 (COMPLETA: 21 consultas). |
| 2026-08-20 (c.727) | **"tender <objeto>" (14/19 tercera clase hogar) → kind HOUSEHOLD (no TASK).** | La deliberación de clase-hogar pendiente desde c.726 se resolvió por HOUSEHOLD: "tender la ropa/cama/mesa" es quehacer doméstico canónico y la familia HOUSEHOLD (c.638+) ya gobierna los imperativos diarios del hogar (criterio c.704). TASK quedaría como kind solo en envolvente c.613. | El piso vive en `HOUSEHOLD_VERBS`; "recuérdame tender…" sigue gobernando TASK vía `imperativeIsWrapped(HOUSEHOLD)`. Los 5 hogares restantes se deciden forma a forma, no se asume HOUSEHOLD automáticamente. |
| 2026-08-19 (c.670, cierre de hipótesis nul) | **EVALUACIÓN de la hipótesis recidiva "¿debería el parser agendar 'hacia las N' DESNUDA (sin evidencia de reloj)?" → RECHAZADO formalmente; el guard de evidencia inmediata (minutos/meridiem/parte del día/unidad horas) PERMANECE como protección anti-falso-positivo. Se documenta para NO re-auditar la hipótesis vacía.** | "hacia" (a diferencia de los aproximadores puros "a eso de"/"casi a las"/"pasadas") admite lecturas de tema ("presentación hacia las ventas") y de cantidad ("hacia las 3 cajas"), por lo que la hora en punto desnuda es una ambigüedad genuina: agendarla fabricaría citas falsas (P0 datos). La família `approximateTimePatterns` (c.676+ hook "hacia/sobre/cerca de/alrededor de") exige evidencia de reloj PRECISAMENTE para abrir sólo el uso temporal que el usuario DISAMBIGA con su texto (meridiem/parte del día/minutos/fracción/unidad horas). Ejemplo probe c.670: "reunión hacia las 9 pm"→21:00 (se abre por evidencia); "reunión hacia las 8"→null (ambiguo, correcto); "hacia las 8 personas"→null (cantidad preservada por el guard). Lo mismo aplicó el nuevo conector "tipo las N" (c.670): ahí NO se exige evidencia porque "tipo las <número>" con artículo NO es gramatical como tema/cantidad formales (el uso de categoría "documento tipo 8" va sin artículo) — así que abrir sin evidencia es seguro; se documentó en su comentario. | Regla formal de la família `approximateTimePatterns`: UN CONECTOR PUEDE REABRIRSE SIN EVIDENCIA SÓLO SI SU FORMA "conector + las + número" CARECE de lectura de tema/categoría/ cantidad gramatical con artículo. En caso contrario el guard de evidencia permanece y la hipótesis queda RECHAZADA (no se llama bug). Guards canónicos de soportes: "hacia"/"sobre"→exigir evidencia; "tipo" (c.670)+"a eso de"/"casi a las"/"pasadas"→no exigir. NO re-auditar "hacia las N desnuda" ni añadirla como PENDIENTE falso. |
| 2026-08-19 (c.665, cierre de follow-up) | **Follow-up P2 de c.662 ("considerar ELIMINAR el default `zone` de `TaskRules.nextBestTask`") → RECHAZADO con evidencia de auditoría.** | El dominio (`GuardianCoach.kt:45/69/95/131`, `GuardianEngine.kt:404`) YA pasa `now, zone` explícito en TODOS los sitios; las únicas hojas con default (`OrdiaWidgetProvider.kt:67`, `OrdiaViewModel.kt:168`) son legítimas — un widget/fallback no recibe zona del llamador (la zona del dispositivo ES la correcta) y quitar el default sólo las forzaría a fabricar `ZoneId.systemDefault()` explícito (mismo comportamiento, más ruido). El bug-family c.604/c.627/c.662/c.665 era **delegación silenciada** (recibir `zone` y NO propagarla): quitar un default en la HOJA no protege contra eso y rompería ~40 sitios de tests que fijan solo `now`. | El follow-up queda CERRADO (REJECTED, no fuerzo trabajo falso); el default se mantiene como conveniencia UI/test-disciplina c.662 (defaults conservados, seams explícitos propagados). Un NUEVO llamador de dominio debe pasar `zone`; el default no es contrato de negocio. Vía abierta original c.604 (delegación silenciada de `WhatNowEngine` en otros consumidores) YA CERRADA por c.665 (Audit `AutomationActionPlanner`/sites en c.604; seam completo). |
| 2026-08-19 (c.665) | **Seam `now` en `AutomationEngine.runTrigger`: se completa el seam explícito propagando `now` (default `System.currentTimeMillis()`) a `runRule`, NO se tolera la flakiness determinista del probe.** La aserción 4 del smoke persistente (`run_automation_engine_checks.sh`) fallaba cuando el reloj real mapaba a un día local de NY distinto al del instante fijo — el probe no podía fijar el tiempo porque el seam no existía. | MISMA clase de seam silenciado que `zone` (c.604/c.627/c.662); un gate flaky produce falso rojo que bloquea/anti-loopea futuras runs y viola "no falsifiques tests". En producción `System.currentTimeMillis()` es la semántica correcta, así que se conserva como default (disciplina c.662: defaults conservados, seams explícitos propagados). | `runTrigger(trigger, chainDepth, now, zone)` con defaults; el probe pasa `now = now` → aserción 4 determinista. Callers Android (`OrdiaApplication`, `AutomationWorker`) idénticos por default. 9 aserciones OK; suite 3812 PASS. NO re-auditar la flakiness: cerrada. |
| 2026-08-18 (c.637) | **El falso-negativo de los enclíticos/pronominales/flexiones verbales de violencia ("matarte"/"violarla"/"violaron"/"amenazarte"/"asesinarte" no bloqueados por `\b` final) se resuelve con **careful-design**: diseñar/ampliar las exenciones `contain` idiomáticas/técnicas ANTES de revivir el stem, NO revivir a ciegas.** Revivir el stem sin `\b` final bloquearía lo dañino PERO también false-bloquearía (a) idiomáticos "matar el hambre/tiempo/ganas/sed/sueño/aburrimiento" (PRE-fix ya false-bloqueados porque `\bmatar\b` SÍ casa la base "matar" — falso-positivo P1 datos preexistente), (b) "violaron la política" (contain previo `\bviolar\b` no lo casaba), (c) plurales "bombas/pistolas de agua" (contain previo no casaba plurales). | c.631/c.636 detectaron el defecto pero lo dejaron PENDIENTE por anti-riesgo P1 datos. La regla de producto exige que un fix de seguridad no cree falsos-positivos de datos. El `contain` ampliado (idiomáticos "matar el hambre/...", técnico "violar(?:on|...)? la política", plural "bomba[ns]? de agua") permite eximir los contextos legítimos sin reabrir el falso-negativo. Además, este fix CIERRA falsos-positivos P1 datos PREEXISTENTES ("matar el hambre" etc. ya eran false-bloqueados por `\bmatar\b`), así que es win-win (cierra TN + cierra FP). | Contrato: stem `\b(matar|asesinar|violar|bomba|amenaza|escopeta|pistola)` (`\b` inicial SIN `\b` final) + contains ampliados. "matarte"/"violarla"/"violaron"/"amenazarte"/"asesinarte" se bloquean; "matar el hambre/tiempo/ganas/sed/sueño/aburrimiento", "violaron la política", "bombas/pistolas de agua" se eximen. +15 tests TDD (6 afirmativos RED→GREEN + 9 regression guards). 3524 domain tests PASS, smoke 25 OK. NO VERIFICADO Android/gradle/UI/Room (sin SDK). |
| 2026-08-18 (c.636) | **El falso-negativo del stem `cuchill` (amenaza con cuchillo/cuchillada/cuchillazo/acuchillar no bloqueados por `\b` final) se resuelve con **careful-design**: diseñar las exenciones `contain` culinarias ANTES de revivir el stem, NO revivir a ciegas.** Revivir `\bcuchill` sin `\b` final bloquearía lo dañino PERO también false-bloquearía "comprar cuchillo de cocina"/"cuchillo de chef"/"afilador de cuchillos" (tareas legítimas → descarte de captura, P1 datos). | c.631 detectó el defecto pero lo dejó PENDIENTE por anti-riesgo P1 datos. La regla de producto exige que un fix de seguridad no cree falsos-positivos de datos. El `contain` (que el algoritmo `isHarmful` evalúa como envoltura del rango del stem) permite eximir los contextos culinarios sin reabrir el falso-negativo. La alternativa `acuchill` en el stem casa "acuchillar" cuyo límite de palabra lo crea la "a" inicial (sin ella no casa: "cuchill" va precedido de "a"). | Contrato: `cuchill` vive en su propia `ModerationRule` con stem `\b(acuchill|cuchill)` + 4 `contain` culinarios. Los contextos de cocina (cocina/chef/pan/mesón/pescado/afilador/set) se eximen; las amenazas ("amenazar con cuchillo", "cuchillada", "cuchillazo", "acuchillar") se bloquean. La mención aislada "llevar un cuchillo" NO se exime (señal suficiente en captura de tareas personales, regla general del gate). +12 tests TDD (5 afirmativos RED→GREEN + 7 regression guards culinarios). 3509 domain tests PASS, smoke 25 OK. NO VERIFICADO Android/gradle/UI/Room (sin SDK). |
| 2026-08-18 (c.635) | **El falso-negativo de `proximity` GLOBAL en drogas específicas se resuelve SPLITANDO la regla en dos `ModerationRule` (droga-genérica/CON-proximity ↔ específicas/SIN-proximity), NO rediseñando `isHarmful` a proximity por-ocurrencia.** `ModerationRule.proximity` es GLOBAL por diseño (legítimo para "droga": "comprar la droga en la farmacia" es legítimo de punta a cabo), pero al compartir stem conjunto con las específicas, legitimaba "fumar marihuana y luego ir a la farmacia". | El rediseño a por-ocurrencia (eximir sólo la ocurrencia del stem cubierta por la proximity, no las demás) habría cerrado el falso-negativo PERO tocaría `isHarmful` — consumido por TODAS las reglas (sexual/violencia/secuestro/insultos) — y requeriría re-auditar cada una: riesgo de regresión alto y fuera de scope. El SPLIT aísla el defecto a la regla de drogas (cero cambio a las demás), alinea implementación ↔ KDoc (que ya declaraba "las específicas no se eximen" desde c.582), y es el cambio mínimo. Cumple "MENOS ES MÁS" + "datos (sagrados)" + anti-regresión. | Contrato: la `proximity` médica SÓLO aplica a `droga` genérica. Las drogas específicas (`cocaina`/`heroina`/`marihuana`/`metanfetamina`/`narcotrafic`) NO tienen `proximity`; su mención aislada es señal fuerte. Casos técnicos legítimos (anestésico tópico de cocaína, metanfetamina recetada) son raros en captura de tareas personales; si surgen, se eximen con `contain` explícito (careful-design), NO reabriendo el falso-negativo con una proximity vaga. +7 tests TDD (4 afirmativos RED→GREEN + 3 regression guards). 3497 domain tests PASS, smoke 25 OK. NO VERIFICADO Android/gradle/UI/Room (sin SDK). |
| 2026-08-18 (c.627) | **La `zone` del usuario es un parámetro que DEBE propagarse explícitamente a TODA sub-llamada de ranking/fecha; el default `ZoneId.systemDefault()` en `TaskRules.nextBestTask`/`isDueToday`/`isStaleInbox`/`inboxAgeDays` es una TRAMPA que silencia la zona del usuario y usa la del DISPOSITIVO.** Confirmado por c.604 (`AssistantEngine`) y c.627 (`GuardianCoach.insight`): el patrón "el parámetro `zone` existe en la firma pública pero se omite en la sub-llamada interna" hace que dos superficies (filtro `dueToday` con zona del usuario vs `nextBestTask` con zona del dispositivo) den resultados DISTINTOS en la MISMA tarjeta. Cuando el dispositivo viaja a una zona adelantada, una cita que VENCE HOY (zona usuario) cae a "mañana" (zona dispositivo) → su `timeRank` decae y otra tarea le roba el lugar en "SIGUIENTE PASO". | La zona del dispositivo (`systemDefault`) NO es la zona del usuario: un teléfono en roaming o con zona mal configurada produce decisiones erróneas (nombrar la tarea equivocada, silenciar la cita de hoy, agendar en el día incorrecto). El default invita a omitir `zone`; los llamadores que ya reciben `zone` (GuardianCoach.insight, AutomationEngine.runRule) conservan sus llamadas internas sin `zone` por inercia, no por diseño. | c.627 corrige `GuardianCoach` (pasa `zone` a sus 4 `nextBestTask`). Quedan P1 follow-up: `AutomationEngine.runRule` (zona del usuario vs `systemDefault`, NO JVM-testable por Room) y `OrdiaViewModel.guardianInsight` (omite `zone`/`now`, fuera JVM). P2 follow-up: considerar ELIMINAR el default `zone=systemDefault()` de `nextBestTask` para que el compilador fuerce `zone` explícito y prevenga la clase entera — evaluar tras cerrar los P1 (para no enmascarar llamadores que aún no pasan `zone`). +1 test TDD RED→GREEN en `GuardianCoachTest.kt` (fija `TimeZone.getDefault()` a `Asia/Tokyo` +9 para reproducir la divergencia de forma determinista). 3453 domain tests PASS, smoke 25 OK. NO VERIFICADO Android/gradle/UI/Room (sin SDK). |
| 2026-08-15 (c.216) | **La recurrencia mensual ordinal ("primer lunes de cada mes", "último viernes del mes") se representa REUSANDO el campo existente `recurrenceDays` con la codificación `"$ord:$wd"` (p. ej. `"1:1"`=1er lunes, `"-1:5"`=último viernes, `"3:3"`=3er miércoles), en VEZ de añadir un nuevo campo `TaskEntity` + migración Room (la opción que c.199 recomendó como follow-up).** Parser enriquece `recurrenceDays` cuando `frequency==MONTHLY && lastWeekdayOfMonthMatch != null`; motor (`RecurrenceEngine.nextMonthly`) decodifica vía `parseOrdinalWeekday` y ancla la próxima ocurrencia al N-ésimo/último día de la semana del mes objetivo; `BackupManager.validateRelationships` acepta la codificación ordinal MONTHLY válida y rechaza corruptas. `days` vacío conserva el anclaje al día del mes (sin regresión "el 15 de cada mes"). | c.199 dejó la deriva ordinal como PENDIENTE por temor a un cambio de modelo de datos (nuevo campo + migración Room) no verificable en JVM. **c.216 demuestra que NO hace falta cambiar el modelo**: `recurrenceDays` es un `String` (CSV de weekdays ISO para WEEKLY) y, para MONTHLY, estaba sin uso (siempre `""` → día del mes). Reusarlo con una codificación ordinal `ord:wd` es (a) verificable en JVM puro (sin Room/migración), (b) retrocompatible (filas viejas con `""` siguen significando día-del-mes), (c) sin riesgo P0 de corrupción de backups existentes (`BackupManager` ya serializa `recurrenceDays` como String; la codificación nueva es un subconjunto de Strings válidos). El motor ya tenía el cálculo N-ésimo-día-de-semana en `nthWeekdayInMonth` (c.180/181 del parser) — se reutiliza simétricamente parser↔motor. Cumple "DATOS (SAGRADOS)": el anclaje ordinal persiste ciclo a ciclo sin deriva, y restore lo preserva. | Contrato: para `frequency==MONTHLY`, `recurrenceDays` ∈ { `""` (día del mes) } ∪ { `"ord:wd"` con `ord ∈ {1,2,3,4,-1}`, `wd ∈ 1..7` ISO }. `BackupManager` valida este contrato al restaurar (rechaza `"9:9"` fuera de rango). Una futura extensión a más ordinales (p. ej. "quinto" lunes si el mes lo tiene) sólo amplía el conjunto válido de `ord`. Si algún día se quiere un campo explícito, la codificación `ord:wd` migra trivialmente. +12 tests TDD (5 parser, 5 motor, 2 backup); verificación E2E parser→TaskEntity→motor confirma 1ª=07-sep, 2ª=05-oct (NO 07-oct deriva), 3ª=02-nov. 1458 domain tests PASS, smoke 25 OK. NO VERIFICADO gradle/Room/Android (sin SDK). |
| 2026-08-15 (c.200) | **La hora canónica INEQUÍVOCA (medianoche/mediodía, "12 de la noche"/"12 de la tarde") SIN fecha explícita se rueda al día siguiente cuando el instante ya pasó (past-safe). Las horas sueltas ("a las 9") y de franja ambigua ("9 de la mañana") NO se ruedan.** Aplicado en el ensamblaje final de `dueAt` en `NaturalTaskParser` (~l. 2171): nueva rama del `when` que, sólo cuando `date==null && relative==null && parsedTime` es 00:00/12:00 con meridiem explícito y `rawDueAt < now`, desplaza a `base.toLocalDate().plusDays(1)` + la misma hora. | Familia past-safe (c.190 `deferToNextDay`, c.183/189 recordatorios, c.196 recurrencia, c.199 ordinal mensual): un `dueAt` en el pasado hace que `ReminderSync.triggers` descarte el recordatorio (`trigger <= now → null`), así la cita se olvida. "cena a la medianoche" capturada al mediodía caía en hoy 00:00 (12h en el pasado) → recordatorio descartado → cita olvidada. Se rueda SÓLO la hora inequívoca (meridiem explícito + valor 00:00/12:00) porque su día por defecto (hoy) es no-ambiguo: el usuario dice "medianoche" pensando en la próxima. La hora suelta ("a las 9") es ambigua en AM/PM y en día (registrar pasado=deuda vs. mañana); "9 de la mañana" tiene día ambiguo — adivinar rompería la semántica existente y la deuda honesta (What Now muestra vencidas). El mediodía (12:00) capturado al mediodía es == now (no pasado) → se queda hoy. | Contrato past-safe uniforme: ninguna cita de hora inequívoca sin fecha explícita se siembra en el pasado. La deuda honesta se preserva para horas sueltas/franjas (no se finge futuro). Honestidad: NO es IA, es una regla determinista de "no agendar en el pasado una hora inequívoca". +5 tests TDD anclando medianoche/mediodía pasados vs. ahora vs. fecha explícita vs. hora suelta (no se rueda). `twelveDeLaMananaIsMidnight` actualizada (fecha hoy→mañana, hora 00:00 sin cambio). 1335 domain tests PASS (1330 c.199 + 5), smoke 25 OK. |
| 2026-08-15 (c.199) | **c.199 resuelve SÓLO la siembra de la 1ª ocurrencia de una recurrencia mensual ordinal ("el primer lunes de cada mes"); la DERIVA de ocurrencias posteriores queda registrada como follow-up PENDIENTE (no se aborda en c.199).** `lastWeekdayOfMonth` ahora rueda la fecha sembrada al próximo mes cuando el día ordinal del mes en curso ya pasó (recurrente) — así la 1ª cita NO se olvida. PERO `RecurrenceEngine.nextMonthly` ancla la cadencia a `base.dayOfMonth` (ej. 7) → Oct 7, Nov 7… que NO son "primer lunes". El modelo `TaskEntity` no representa ordinal-de-mes (`recurrence`+`recurrenceInterval`+`recurrenceDays`-semanal). | El cambio de modelo de datos (nuevo campo `TaskEntity` + migración Room + lógica `RecurrenceEngine` + wiring parser + coordinación con `BackupManager` que serializa `TaskEntity`) es de mayor impacto y NO es verificable en JVM pura (Room/migración requiere Android SDK). Abordarlo a ciegas violaría "DATOS (SAGRADOS)" — un error de migración podría corromper backups o perder recurrencias existentes. El fix de c.199 resuelve el caso AGUDO (1ª ocurrencia olvidada = pérdida inmediata, el caso que un usuario nota al capturar); la deriva en ocurrencias N≥2 es pre-existente (afecta a toda recurrencia mensual ordinal, no introducida por c.199) y de menor urgencia inmediata. Honestidad: NO se simula capacidad — la cadencia "primer lunes de cada mes" NO se mantiene tras la 1ª ocurrencia con el modelo actual, documentado explícitamente. | Follow-up PENDIENTE en BACKLOG (P1/P2): diseñar el campo ordinal-de-mes (ej. `recurrenceMonthlyOrdinal: Int?` 1-4/-1 + reuso de `recurrenceDays` para weekday) + migración Room + `nextMonthly` que use `nthWeekdayInMonth` cuando el ordinal esté presente + wiring parser→TaskEntity. Requiere entorno Android SDK para verificar migración + backup/restore. Condición previa: auditar `BackupManager` para que el nuevo campo se serialice/deserialice sin romper backups antiguos (campo null en filas viejas → MONTHLY por día-del-mes, comportamiento actual). |
| 2026-08-14 | **NO clampar `reminderAt` a futuro en el camino de captura** (`OrdiaViewModel.addParsedTask` l. ~416-418, `QuickCaptureActivity` l. 150), donde se calcula `reminderAt = dueAt - offset*60_000L` sin past-safe. Resuelve la decisión (iii) que c.190 dejó abierta. Se DEJA el cálculo literal; `RecurrenceEngine.nextOccurrence` (c.189) ya sanea el offset por ocurrencia, y `ReminderScheduler.scheduleAt` coerce `delay >= 0` (disparo inmediato al guardar). | Clampar corrompería el **offset de recurrencia**: `RecurrenceEngine.kt:24` deriva `reminderOffset = task.dueAt - task.reminderAt` en cada ocurrencia. Un `reminderAt` clampado (p. ej. "25 días antes" de un vencimiento en 3 días → clampado a `due - 30min`) reescribiría `reminderOffset` de 25 días a 30 min, alterando la cadencia de recordatorio de TODAS las ocurrencias futuras → **regresión P0 de datos**. Clampar SÓLO el caso no-recurrente (ramificar por `recurrence==NONE` en 3 sitios muy testeados) añade complejidad visible desproporcionada al beneficio (el caso no-recurrente no es olvido real: avisa al guardar + visible en "vence hoy"). | Tradeoff P3 conocido y aceptado: para una tarea NO recurrente con offset de captura pasado, el aviso puntual no sobrevive a un reboot (pérdida severidad P3, mitigada por disparo inmediato + visibilidad de la fecha). NO introducir un clamp en la captura sin resolver primero la derivación de `reminderOffset` en `RecurrenceEngine` (p. ej. persistir el offset explícitamente en vez de derivarlo de `dueAt - reminderAt`). |
| 2026-08-14 | La traslación del offset de recordatorio al mover/reprogramar un vencimiento SIEMPRE debe ser past-safe: si el instante trasladado cae en el pasado, recaer a un default futuro (no a `null`, no a 30 min fijo). Aplicado de forma consistente en `ReminderRules.resolveReminderAt` (c.183, editor → `defaultReminderAt`) y `AutomationActionPlanner.RESCHEDULE_OVERDUE` (c.187, automatización → `due - 1h`, el mismo default que su rama sin-reminder). | Un `reminderAt` pasado lo descarta `ReminderSync.triggers` (`trigger <= now → null`), así que sin past-safe la tarea movida/reprogramada perdía silenciosamente su aviso → olvido, justo lo que el recordatorio debe evitar. El offset grande es alcanzable por captura natural ("recuérdame N días antes"). c.164 cerró la rama por defecto; c.183 y c.187 cierran las dos ramas de traslación, completando el hilo past-safe. | Contrato uniforme: en TODA traslación de offset de reminder, conservar el offset del usuario si cabe en el futuro; si no, garantizar un reminder futuro. Una futura 4ª ruta que traslade offsets debe aplicar la misma regla. No se usa `null` como fallback en `RESCHEDULE_OVERDUE` porque su contrato es "no quedar al olvido" (a diferencia del editor, donde `defaultReminderAt` puede devolver `null` sólo si no cabe ni 1 min de antelación). |
| 2026-08-14 | Enlace rutina↔tarea: se mantiene por NOMBRE (`details="Rutina: <nombre>"`) y se reetiquetan las tareas de hoy al renombrar la rutina (`RoutineRules.relinkAfterRename`), en vez de migrar a una columna `routineId` en `TaskEntity`. | El ÚNICO consumidor del enlace es `RoutineRules.wasRunToday` (anti-duplicado diario); no hay agrupación/historial UI que dependa del nombre. Reetiquetar solo hoy (ámbito exacto del dedup) cierra el bug de duplicados al renombrar SIN migración de schema → cero riesgo P0 de datos en un entorno sin Android SDK para verificar una migración. Reusa todo el flujo existente. | `saveRoutine` ahora lee el nombre viejo y aplica el relink sobre `uiState.value.tasks`. No se reescribe histórico (solo hoy). Queda como mejora opcional futura (no P0) migrar a `routineId` cuando exista entorno Android para verificar la migración + backfill; hasta entonces el nombre sigue siendo el enlace y el renombrado es seguro. |
| 2026-08-10 | El trabajo autónomo vive SOLO en `jules/autonomous-ordia`; `main` jamás recibe auto-merge. | Proteger la base estable de cambios no revisados por humanos. | Todo PR autónomo apunta a `jules/autonomous-ordia`; el humano revisa y decide si avanza a `main`. |
| 2026-08-10 | El rebuild de Codex se consolida en commits coherentes (tema por tema) y NO en un único commit gigante. | Historial legible y revisable. | 9 commits + rama de publicación `feature/ordia-total-rebuild-2026-08-10`. |
| 2026-08-10 | Cron autónomo cada 2 horas (minuto 17) + `workflow_dispatch`. | Suficiente para progreso continuo sin saturar CI; permite arranque manual. | Ventana de oportunidad cada 2h; si una sesión dura más, el session lock impide solape. |
| 2026-08-10 | Session lock por rama: `jules/autonomous-ordia` se bloquea durante la sesión; sin lock, nueva sesión no inicia. | Evitar dos agentes editando lo mismo. | El runner termina sin cambios si detecta lock activo (retry posterior). |
| 2026-08-10 | Failsafe `ORDIA_AUTONOMY_ENABLED` y verificación del archivo de bypass: si se desactiva, ninguna sesión nueva arranca. | Parada de emergencia independiente del código. | Apagar autonomía = crear/marcar archivo de bypass + desactivar variable. |
| 2026-08-10 | No reintentar indefinidamente; ante N fallos consecutivos, el workflow abre un ISSUE y la rama `jules/autonomous-ordia` queda intacta (revert si fuera necesario). | Evitar ciclos de agentes que se pisan y corrompen el repo. | Fallo visible y accionable para el humano. |
| 2026-08-10 | El workflow Jules usa `vars.ORDIA_AUTONOMY_ENABLED` como failsafe con valor por defecto ACTIVO (si la variable no existe o no es `false`/`0`/`no`/`off`, se lanza). | Coherente con el workflow previo (que lanzaba siempre) y con la decisión de autonomía continua; la parada explícita es la variable `false` o el archivo `AI_AUTONOMY/AUTONOMY_BYPASS`. | Requiere que el humano configure la variable solo si quiere DESACTIVAR la autonomía. |
| 2026-08-10 | Session lock por PR abierta: si hay cualquier PR abierta hacia `jules/autonomous-ordia`, no se lanza una nueva sesión. | Con cron cada 2h, evita que dos sesiones de Jules se solapen sobre la misma rama. | Una PR abierta de una sesión anterior pausa los ciclos hasta que se cierre/mergee; el humano puede cerrarla. |
| 2026-08-10 | El workflow verifica contra la API de Jules que la rama `jules/autonomous-ordia` exista en el source ANTES de lanzar; si no, el ciclo se omite (no falla el job). | La rama nueva puede tardar en sincronizarse con el conector de Jules; lanzar contra una rama inexistente fallaría en la sesión. | Primeros ciclos pueden ser no-op hasta que Jules vea la rama. |
| 2026-08-10 | Cron cada 2 horas en el minuto 17 (`17 */2 * * *`). | Suficiente para progreso continuo sin saturar CI; evita el minuto 00 y reduce colisión con otros jobs; `workflow_dispatch` permite arranque manual. | Ventana de oportunidad cada 2h. |
| 2026-08-10 | LA INFRAESTRUCTURA DE ORQUESTACIÓN VIVE EN `main`: `.github/workflows/ordia-autonomous-jules.yml` (scheduler), `.github/workflows/ordia-autonomous-merge.yml` (auto-merge) y `.github/workflows/android-ci.yml` (CI) se publican en `main` porque GitHub Actions ejecuta los schedulers desde la default branch. | El workflow nuevo NO estaba en `main` (solo en la rama autónoma) → el cron no corría. | Scheduler y auto-merge activos desde `main`; el código del rebuild sigue en `jules/autonomous-ordia` y NO se mergea a main. |
| 2026-08-10 | Auto-merge real y restringido: nuevo workflow `ordia-autonomous-merge.yml` mergea (squash, sin force) PRs hacia `jules/autonomous-ordia` cuando pasan 12 guardas (base exacta, no-main guard clause, no fork + patrón de rama Jules, no draft, mergeable, checks success, security checks success). | `AUTO_CREATE_PR` crea la PR pero NO la mergea; el session lock antiguo (cualquier PR abierta → no lanzar) habría bloqueado la autonomía indefinidamente. | PRs autónomas se fusionan solas tras CI verde; el humano NO tiene que mergear cada PR de Jules. |
| 2026-08-10 | El session lock distingue estados: sesión activa (API Jules) → skip; PR fallida → `proceed_with_failure_context` (la nueva sesión recibe el fallo); CI corriendo → `skip_ci_running`; PR lista → `skip_ready_for_merge`; ≥4 PRs → `skip_too_many_prs`; mismo área fallada ≥2 veces → `loop_warning`. | El lock antiguo (una PR abierta bloquea todo) era demasiado burdo y detendría el progreso tras la primera PR. | La autonomía continúa corrigiendo regresiones reales en lugar de detenerse. |
| 2026-08-10 | Jules NUNCA auto-mergea hacia `main`; el auto-merge tiene guard clause explícito (`base_ref == "main"` → MERGE PROHIBIDO) además de requerir base exacta `jules/autonomous-ordia`. | Proteger la base estable de cambios no revisados por humanos. | Sin camino automático `* → main`; solo el humano decide si algo llega a main. |
| 2026-08-10 | CI cubre también PRs hacia `jules/autonomous-ordia`: `android-ci.yml` ampliado (`branches: [main, jules/autonomous-ordia]`). | Los checks de la rama head solo existen si el workflow está presente; sin CI en las PRs autónomas el auto-merge esperaría forever. | Cada PR de Jules corre verify (tests+lint+assemble); sign/publish solo en push a main. |
| 2026-08-11 | La verificación del dominio en entornos sin Android SDK se hace con kotlinc + JUnit4 + stubs (`tools/domain-smoke/RoomStubs.kt`, `PreferenceStubs.kt`), no con gradle. | `./gradlew test` requiere Android SDK no siempre disponible; el dominio es Kotlin puro (java.time, kotlinx, org.json) y sus tests solo dependen de `data.local` (Room, con stubs) y `data.preferences` (DataStore, con stubs). | Baseline de dominio reproducible en JVM pura (125 tests + 25 smoke assertions). Los tests que necesitan DAOs/Context/RoomDatabase (backup, context, repositories) quedan fuera de esta baseline y requieren Android SDK o Robolectric. |
| 2026-08-11 | El smoke de dominio (`tools/domain-smoke/DomainSmoke.kt`) debe usar el set explícito de kinds core alimentados, NO `SearchKind.entries.toSet()`. | `SearchKind` creció a 7 valores (CONVERSATION/COMMITMENT/AUTOMATION añadidos) pero el smoke solo alimenta 4 listas; `entries.toSet()` es un bug obsoleto que nunca pasaría. | Smoke alineado con `SearchEngineTest`; `run_domain_checks.sh` vuelve a ser verde. |
| 2026-08-11 | `NoteBlockCodec.decode` debe ser resiliente por elemento, no por array completo: el `runCatching` no puede envolver todo el bucle de parseo porque un único elemento malformado causaría pérdida total de bloques. | Data loss silencioso P1: un elemento corrupto (string donde se esperaba objeto) hacía que toda la nota se colapsara a un párrafo vacío/fallback. La codificación nunca produce tales elementos, pero restauraciones externas o futuras extensiones podrían. | El array raíz se valida por separado (corrupción total → fallbackBody); dentro del array, los elementos malformados se descartan y los válidos se conservan; solo si todos fallan se cae al fallback. 11 tests cubren el comportamiento. |
| 2026-08-11 | Se estandariza `tools/run_domain_tests.sh` como runner JUnit4 reutilizable (compila stubs + dominio + tests, ejecuta con `JUnitCore`) para todas las sesiones autónomas sin Android SDK. | La sesión anterior corrió 136 tests con un comando ad-hoc no persistido; sin script reusable, cada sesión reconstruiría la classpath/stubs. | 147 tests se ejecutan con `bash tools/run_domain_tests.sh` desde cualquier sesión; la baseline de dominio es reproducible y extensible. |
| 2026-08-11 | **Modo continuo**: se adopta un supervisor persistente (`tools/ordia_supervisor.py`) como orquestador único de la Automation `Ordía Continuous Evolution`, en lugar del cron. | El cron cada 2h dejaba huecos de hasta 2h (no es continuidad real). Además, el cron dispatcha ciegamente sin comprobar runs activos → se detectaron 2 runs concurrentes (viola MAX_CONCURRENT=1). El automation service NO previene solapamiento. | El supervisor (ejecutado en una máquina siempre encendida del usuario) comprueba runs activos vía API y solo dispatcha cuando no hay ninguno (MAX_CONCURRENT=1 garantizado); encadena el siguiente run en ~15–40 s. Deshabilita el cron al arrancar y lo rehabilita al detenerse (watchdog de seguridad). Sin supervisor, el cron cada 15 min es modo degradado. Timeout de la automation subido 600→1800 s (los runs "FAILED" por timeout sí commiteaban antes de morir). Rama de trabajo: `openhands/autonomous-ordia` (memoria Git persistente de OpenHands, separada de `jules/autonomous-ordia`). |
| 2026-08-11 | **Supervisor v2**: lock cross-platform (fcntl/msvcrt/pidfile), lease distribuido vía GitHub Gist con TTL, y watchdog GitHub Actions externo. | v1 dependía de `fcntl` (solo Unix), rehabilitaba el cron solo en `finally` (irrecuperable si `kill -9`/apagón), y no tenía lock distribuido (supervisor + watchdog + cron podían pisarse). | Lock funciona en Linux/Windows/WSL. El lease expira automáticamente (TTL 300s) sin depender de `finally`, así el watchdog GitHub Actions detecta un supervisor muerto y rehabilita el cron + dispatch recovery. Concurrencia=1 garantizada en 3 niveles (API check, lock proceso, lease+watchdog). Cloud-first: docker/systemd/install permiten apagar el PC. |
| 2026-08-11 | **Continuous Delivery dedicado a `openhands/autonomous-ordia`** con naming exacto del auto-updater, en lugar de reutilizar `android-ci.yml`. | `android-ci.yml` publicaba releases con tag `v3.0.0-build.N` y asset `Ordia-3.0-signed.apk`, pero `UpdateSecurityRules` exige `v3.0.N-code-C` y `Ordia-3.0-code-C.apk` → el auto-updater nunca detectaba la actualización (funcionalidad falsamente implementada). Además no disparaba en la rama autónoma. | `openhands-delivery.yml` produce artifacts que el updater SÍ consume end-to-end: tag `v3.0.{run_number}-code-{versionCode}`, asset `Ordia-3.0-code-{versionCode}.apk` + `.sha256`, no-prerelease, gates tests+lint+assemble, concurrency cancela obsoletas, no sobrescribe releases existentes. |
| 2026-08-13 | **Dos FileProviders vía subclase dedicada (`UpdateFileProvider`)**, no dos `<provider>` con el mismo `android:name`. El manifiesto `main` declara `${applicationId}.attachments` (adjuntos); los flavors `previewAdvanced`/`previewFull` declaran `${applicationId}.update-files` (auto-update). Ambos usaban `android:name="androidx.core.content.FileProvider"` → el manifest merger los fusiona por `android:name` y colisiona en `authorities`/`resource` → `processPreviewAdvancedDebugMainManifest` FAILED, lo que bloqueó TODOS los releases del delivery durante ~12 runs consecutivos. | El merger de AGP trata elementos con el mismo `android:name` como un único elemento lógico: dos FileProviders con distinta authority pero mismo nombre NO coexisten. La forma correcta y estándar de tener dos FileProviders es darles `android:name` distintos, lo que requiere una subclase. | Creada `app/src/main/java/com/ordia/app/updates/UpdateFileProvider.kt` (`class UpdateFileProvider : FileProvider()`); los dos manifests de flavor referencian `.updates.UpdateFileProvider`. El call site `OrdiaUpdateManager` resuelve URIs por authority string en runtime (independiente de la clase), sin cambio de código. Verificado localmente con Android SDK: clean+test (3768 tests, 0 failures)+lint+assemblePreviewAdvancedRelease BUILD SUCCESSFUL; manifest merge verde para las 3 variantes. Tras el push, run 31735618004 success → release `v3.0.80-code-1300008001` publicada. Lección: cualquier nuevo FileProvider DEBE usar una subclase propia si ya existe otro FileProvider en el manifiesto. |
| 2026-08-13 | **Delivery `cancel-in-progress: false`** (era `true`). El bucle autónomo empuja commits cada ~3 min y la build de delivery tarda ~10 min; con `cancel-in-progress: true` cada push cancelaba la build en curso, de modo que ninguna build finalizaba y las mejoras de los ciclos 51-57+ no se publicaban (última release quedó estancada en ciclo ~50). | `cancel-in-progress: true` es útil para no desperdiciar CI cuando los pushes rápidos hacen obsoletas las builds, pero cuando la cadencia de pushes (~3 min) supera la duración de la build (~10 min), cancela TODAS las builds y el canal de release queda mudo. La prioridad es que las mejoras se publiquen, no ahorrar minutos de CI. | Con `cancel-in-progress: false`, la build en curso completa y publica su release; el push más reciente queda pendiente (queued) y corre después. GitHub mantiene a lo sumo 1 job pendiente por grupo (los pushes intermedios se reemplazan entre sí), así no hay cola infinita. Verificado: run de ciclo 57 (053e7ff) success → release `v3.0.93-code-1300009301`; builds posteriores (e611da6, db30b9c) quedan pending/in_progress en vez de cancelled. El canal de release vuelve a avanzar. |
| 2026-08-14 | **Auditoría del patrón `working.replace(it.value, " ")` en `NaturalTaskParser`**: 29 sitios lo usan para limpiar del título el token de fecha/hora ya consumido. Se auditó tras hallar el bug P0 "ya" (c.114). Solo `nowPattern` (token `"ya"`, 2 chars) corrompe títulos: `String.replace(oldValue,newValue)` reemplaza TODAS las ocurrencias literales, no solo el rango casado, así "comprar maya ya" → "comprar ma " (corrupción de datos). Fix puntual `replaceRange(it.range, " ")` en el sitio de `nowMatch`. | Los 28 sitios restantes casan frases largas (`"más tarde"`, `"el jueves pasado"`, `"fin de mes"`, `"después"` 7 chars, `"ahora mismo"`, `"mañana"`, `"noche"`) o usan `\b`; un token de 5+ chars rara vez aparece como subcadena dentro de otra palabra española. Una refactorización global a `replaceRange` es arriesgada en el sitio `findAll`+mutación (línea 733: rangos obsoletos al cambiar la longitud) y no se puede verificar plenamente sin Android SDK. | Se hizo el fix P0 verificado en `nowMatch` (c.114) y se dejó documentado que los 28 sitios restantes fueron PROBADOS con 15 títulos adversariales (`maya`/`playa`/`raya`/`manzanillo`/`nochera`/`tardanza`/`finalizar`/`despuéso`/`aljaba`/`ensillar`…) → 0 corrupciones. No se refactorizan a ciegas. Si un futuro run quiere uniformar, debe procesar los `findAll` en orden inverso y re-verificar con la probe de c.114. |
| 2026-08-15 (c.249) | **`SubtaskRules.progress()` ahora excluye las subtareas `CANCELLED` del total (denominador), alineándose con `allCompleted()`/`isResolved()`.** Esto **supera** la nota de c.181 que decidió "DEJAR `progress` contando `completed` (evita cambio de comportamiento UI no verificable)". | c.181 dejó `progress()` contando sólo `completed` por miedo a un cambio de comportamiento UI no verificable en JVM. Ese miedo queda obsoleto por dos hechos verificados en c.249: (1) **la UI NUNCA envía subtareas canceladas a `progress()`** — `OrdiaViewModel.subtasks()` (l.182) filtra `status != CANCELLED` antes de pasar la lista a los 5 call sites (`Inbox/Planner/Today/Tasks/ProjectDetail`), así `progress()` no recibe canceladas → **cambio de comportamiento UI = NINGUNO** (mismas entradas, mismas salidas); (2) al consolidar los 5 cálculos inline (`subtasks.count{it.completed} to subtasks.size`) en una ÚNICA función de dominio (`SubtaskRules.progress`), el cambio vive en UN solo sitio totalmente verificable en JVM, no en 5 pantallas UI. El cambio cumple el INTENTO explícito de c.181 ("no inflar progreso con canceladas") mejor que la implementación de c.181: antes la no-inflación dependía de un filtro aguas-arriba que cualquier caller podría olvidar; ahora la regla de dominio es correcta por construcción (defensa en profundidad, simétrica a `isResolved`). | Contrato: `progress(subtasks) = (completadas, total-no-canceladas)`. Una subtarea `CANCELLED` no cuenta como completada ni como pendiente (igual que si el usuario la quitó del checklist). `total==0` (todas descartadas) → la UI oculta la barra. Sin cambio observable para los call sites actuales (input ya sin canceladas). Si un futuro caller pasa la lista sin filtrar (p. ej. un editor que muestre canceladas), la fracción visible será honesta y consistente con `allCompleted` en vez de mostrar "1/2" sobre un padre ya completo. +3 tests de contrato del dominio (entrada sintética con canceladas). 1615 domain tests PASS, smoke 25 OK. **NO VERIFICADO** gradle/lint/assemble/Android/UI/Room (sin SDK); pero el cambio de comportamiento UI es nulo por construcción (filtro aguas-arriba) — la consolidación sí toca 5 pantallas (sustitución mecánica `inline → SubtaskRules.progress`, import añadido). |
| 2026-08-14 | **No se centraliza el predicado "tarea activa" en `TaskRules.isActive`** tras el fix P1 del estado CANCELLED (c.169). Se corrigieron 5 rutas que repetían `!completed && !archived` sin `status != CANCELLED` (DayPlanner, BackupManager restore, AssistantEngine, GuardianEngine.suggestedAction, OrdiaWidgetProvider). | `SummaryEngine` ya demuestra el patrón correcto (predicado `active` local con `status != CANCELLED`), y consolidarlo en `TaskRules.isActive(task)` + migrar las 6 rutas evitaría la recurrencia de esta familia de bugs. PERO el bug está fijado y testeado (1166 PASS), y un refactor de churn que toque de nuevo las 6 rutas arriesga romper el verde sin aportar valor de comportamiento. La filosofía del producto ("cambio mínimo", "no refactorices código correcto para demostrar actividad") pesa más que la elegancia arquitectónica aquí. | Queda registrado como mejora opcional P2 (anti-recurrencia). Si un futuro run encuentra un 7º/8º sitio que olvide `CANCELLED`, será la señal de que la centralización sí aporta valor y se deberá hacer entonces (con migración completa + tests). Mientras tanto, cada superficie mantiene su predicado local coherente con `TaskRules`/`ReminderSync` (fuente de verdad). **Actualización c.170:** se encontró la 6ª ruta — el predicado de ENTRADA de tareas en `SearchEngine` (c.169 había marcado SearchEngine como "SEGURO" pero sólo auditó `taskMatchesDateScope`, no el predicado superior que cubre búsqueda genérica + atributos). Fijada con 1 cláusula + 2 tests (1168 PASS). El umbral (7º/8º sitio) aún NO se cruza → la centralización sigue diferida. Próxima señal de acción: un 7º sitio. **Actualización c.173:** el umbral se cruzó — c.171 halló un 7º sitio (GuardianEngine línea 148) y la recurrencia seguía siendo la causa raíz de la familia de bugs CANCELLED. Se ejecutó la centralización diferida: nuevo `TaskRules.isActive(task) = !task.completed && !task.archived && task.status != TaskStatus.CANCELLED`; migradas 7 rutas (las internas `isOverdue`/`isDueToday`/`isDueOn` + `DayPlanner`/`SummaryEngine`/`WhatNowEngine`/`ReminderSync`/`GuardianEngine`). **Cambio de comportamiento: NINGUNO** (todas ya excluían CANCELLED tras c.169/170; ahora lo hacen vía fuente única). +8 tests anclando el contrato. Esta decisión queda SUPERADA: el predicado canónico `isActive` existe y es la fuente única; futuras superficies activas DEBEN usarlo (no duplicar el trío). 1181 PASS. |
| 2026-08-16 (c.406) | **EVALUACIÓN: "hora explícita en PASADO capturada" NO es bug de olvido — la cita se recupera por el camino de captura (`ReminderScheduler.schedule`), no por `ReminderSync.triggers`.** Una cita capturada con `dueAt`/`reminderAt` pasado se entrega al guardar: `OrdiaViewModel.saveTask` → `scheduleAt(taskId, reminderAt ?: dueAt)` → `delay = (triggerAt - now).coerceAtLeast(0)` → WorkManager `setInitialDelay(0)` (disparo inmediato). Además `TaskRules.isOverdue` + `WhatNowEngine`/widget/`SummaryEngine.overdue` muestran la deuda honesta. `ReminderSync.triggers` descartar `trigger <= now` es CORRECTO: el re-sync masivo (boot/medianoche) no debe re-encolar triggers ya entregados (duplicaría notificaciones). | La sospecha de bug venía de un probe c.399/c.400 cuyo label decía "PASADO(recordatorio descartado por ReminderSync)" pero sólo inspeccionaba `ReminderSync.triggers` (re-sync masivo), NO el camino de captura. El past-safe de c.200 (rodar medianoche/mediodía inequívocas al día siguiente) cubre el único caso no-ambiguo; las horas sueltas/franjas se dejan intencionalmente en el pasado como deuda honesta (no se finge futuro). Simétrico y consistente con DECISIONES 2026-08-14 "NO clampar reminderAt". | Cierre formal de la pregunta recurrente "hora explícita en PASADO" (próxima prioridad c.399/c.400) para evitar re-auditar la misma cadena ciclo tras ciclo. 0 cambios de código: es una síntesis de evaluación sobre implementación madura. 2405 domain tests PASS (base c.404), smoke 25 OK, probe empírico `ProbePastExplicitTime`/`ProbeParserTemporalForms` (24 formas) ejecutado y eliminado. NO re-abrir sin evidencia REAL de olvido. |
| 2026-08-17 (c.550) | **EVALUACIÓN: "hoy"→[] y "compromisos hoy"→todos los PENDING en `SearchEngine` son comportamiento INTENCIONADO, no bug.** Un compromiso sin vencer NO es información "de hoy"/"esta semana" hasta que el usuario lo convierte a tarea; los compromisos son ítems de recuperación (4.º olvido), no ítems agendados con date-scope. | c.541 (P1) decidió deliberadamente que los compromisos sólo emergen en la búsqueda universal bajo scopes de recuperación (OVERDUE/MISSED vía `overdueRecovery`, o "vencidas"/"olvidadas") o por coincidencia léxica explícita ("compromisos"), NO bajo scopes de calendario ("hoy"/"esta semana"). El filtro de compromisos tiene `(!pureDateScope || overdueRecovery)`: "hoy" puro → `pureDateScope=true`, `overdueRecovery=false` (TODAY≠OVERDUE/MISSED) → suprime compromisos por diseño. "compromisos hoy" → `pureDateScope=false` (hay texto) + `wantsCommitments=true` → emergen TODOS los PENDING (el "hoy" es no-op para compromisos, pues no hay `commitmentMatchesDateScope`). | Cierre formal de la pregunta "¿debería 'hoy'/'compromisos hoy' filtrar compromisos por fecha?" para evitar re-auditarla ciclo tras ciclo. Sobrescribir esta decisión añadiría filtrado por fecha (complejidad) contra la filosofía MENOS ES MÁS, sin evidencia de daño real. c.550 sí corrigió el bug REAL adyacente: "pendientes" no recuperaba compromisos PENDING (asimetría con "vencidas"). NO re-abrir la pregunta date-scope sin evidencia REAL de daño (p. ej. un compromiso vencido-pero-no-detectado, ya cubierto por c.541). 3035 domain tests PASS, smoke 25 OK. |
| 2026-08-18 (c.616) | **EVALUACIÓN: "comprar pan"/"reunión con el equipo" (verbos de SHOPPING/MEETING con objeto explícito PERO sin ancla temporal ni imperativo) se descartan INTENCIONADAMENTE bajo `MINIMUM_CONFIDENCE` (0.45) — NO es bug de olvido asimétrico con CALL.** No se bajará el umbral ni se añadirá un object-bonus análogo al de CALL (c.610) a SHOPPING/MEETING. | Auditoría (c.616) sobre `ContextIntentEngine.scoreKind`/`scoreSpecificPatterns`: "comprar pan"→SHOPPING 0.37 (keyword "comprar" 0.12 + `comprar \w+` 0.15 + "pan" 0.1; <0.45 → NULL), "reunión con el equipo"→MEETING ≤0.32 (`reunión (con|de|del)` 0.2; <0.45 → NULL). La asimetría con CALL (c.610 sí añadió +0.25 de objeto para "llamar a María") es DELIBERADA y está codificada en DOS tests de contrato anti-overreach: `ContextIntentEngineTitleTest.genitiveDeHoyIsNotStrippedAsResidue` = `assertNull(analyze("comprar el diario de hoy"))` y `ContextIntentEngineTitleTest.titleWithoutTemporalAnchorIsPreserved` = `assertNull(analyze("comprar leche y huevos"))` (comentario explícito: "no clasifica (sin seña de recordatorio/cita)"). La razón de fondo: "comprar"/"reunión" aparecen constantemente en chat casual ("quiero comprar eso", "no compres", "compramos ayer", "nos vemos pronto"), mientras "llamar a <persona>" es raro en chat ocioso y por eso es seguro capturarlo. `scoreAmbiguityPenalty` (chatCount>2→−0.15, >4→−0.2) ya protege pero no basta para "comprar" suelto. Bajar el umbral o añadir un object-bonus ROMPERÍA esos 2 tests (assertNull→assertNotNull), lo que las reglas prohíben ("no elimines tests para conseguir verde", "no fuerces"). | Cierre formal de la pregunta recurrente "¿debería 'comprar pan'/'reunión con el equipo' capturarse como CALL?" (surgida del patrón c.610) para evitar re-auditarla ciclo tras ciclo. NO re-abrir sin evidencia REAL de daño (p. ej. una captura legítima de "comprar pan" perdida que el usuario reporte) Y un mecanismo que distinga "comprar pan" (compra) de "no quiero comprar eso" (chat) sin degradar la protección anti-overreach. El camino correcto, si algún día se persigue, NO es bajar `MINIMUM_CONFIDENCE` global: es un discriminador de imperativo/afirmación (como `hasStrongTaskImperative` de c.614 para TASK) reservado a SHOPPING/MEETING con objeto, que NO active en chat negativo/condicional. Hasta entonces, el descarte es la protección honesta. 3409 domain tests PASS (base c.615), smoke 25 OK. 0 cambios de código: síntesis de evaluación sobre implementación madura. |
| 2026-08-18 (c.626) | **RE-APERTURA Y CIERRE de la condición c.616: se implementó el "discriminador de imperativo/afirmación reservado a SHOPPING/MEETING con objeto, que NO active en chat negativo/condicional" que c.616 condicionó como "el camino correcto".** | c.616 cerró la pregunta con descarte deliberado CONDICIONADO a que surgiera "un mecanismo que distinga 'comprar pan' (compra) de 'no quiero comprar eso' (chat) sin degradar la protección anti-overreach". c.626 es ese mecanismo: piso simétrico `if (kind == ContextIntentKind.SHOPPING && hasStrongShoppingImperative(lower)) score = maxOf(score, MINIMUM_CONFIDENCE)` (idem MEETING), donde `hasStrongShoppingImperative = Regex("""^comprar\s+\w""")` y `hasStrongMeetingImperative = Regex("""^reunión\s+(con|de|del)\s+\w""")`. El ancla `^` es la protección anti-overreach: exige imperativo AFIRMATIVO al INICIO de la notificación. "no comprar pan" (negación → capta lo OPUESTO a la intención del usuario, falso positivo grave), "mañana no comprar pan" (negación incrustada), "quiero comprar eso" (condicional) y "compramos ayer" (pasado) NO comienzan por "comprar" → no activan el piso. Los casos afirmativos con ancla temporal ("mañana comprar pan") ya superaban el umbral vía `extractDateTime`, sin regresión. NO se bajó `MINIMUM_CONFIDENCE` global (c.616 proscrito) ni se añadió object-bonus (c.616 proscrito). Los 2 tests anti-overreach de c.616 se ACTUALIZARON honestamente de `assertNull`→`assertNotNull`+`assertEquals(SHOPPING)`: antes codificaban el DESCARTE (contrato c.616); ahora codifican la CAPTURA (contrato c.626). NO es degradar tests: es el cambio de contrato que persigue c.626. +3 tests anti-overreach NUEVOS (`negatedComprarDoesNotTriggerFloor`, `negatedReunionDoesNotTriggerFloor`, `dontBuyImperativeDoesNotTriggerFloor`) cristalizan la condición c.616 que c.626 satisface. Determinista (regex+maxOf), sin random, sin IA fingida. `bash tools/run_domain_tests.sh` → 3438 PASS (3428 c.620 + 10 c.626), 0 failures; smoke 25 OK. **NO VERIFICADO** Android/gradle/lint/assemble/UI/Room (sin Android SDK). |
| 2026-08-19 (c.662) | **Seam de `zone`: se cierran los CALLERS de dominio que silenciaban la zona, NO se elimina el default `ZoneId.systemDefault()` de las firmas del dominio.** `AutomationEngine.runTrigger/runRule` ganan `zone` inyectable (default preservado) y propagan a `AutomationActionPlanner.build`; `OrdiaViewModel.guardianInsight` queda sin cambio; `TaskRules.nextBestTask` conserva su default. | El defecto real de la clase c.604/c.627/c.662 no es el default: es que un motor de dominio que RECIBE la zona del llamador la silenciaba hacia abajo (`runRule` calculaba `dayStart`/límite diario con la zona del dispositivo y no la pasaba al planificador). Eliminar el default (a) rompería la uniformidad: TODO el dominio usa `zone: ZoneId = ZoneId.systemDefault()`; (b) rompería callers Android-only (`OrdiaViewModel`, `OrdiaWidgetProvider`) no compilables sin Android SDK → violaría la regla "NO VERIFICADO"; (c) no existe zona de usuario distinta de la del dispositivo (sin preferencia `ZoneRules` en el repo): en runtime el default ES la zona correcta, y en tests el seam explícito ya permite inyectar. | Disciplina vigente: (1) funciones de dominio mantienen `zone` con default; (2) cualquier función que RECIBA `zone` DEBE propagarla explícita a todos sus sub-llamados del dominio (la clase c.604/c.627 queda cubierta); (3) callers UI/Android-only usan el default (correcto por construcción). Verificación c.662: probe JVM de 9 aserciones con fakes concretos mismos-nombres compila `AutomationEngine.kt` por PRIMERA vez sin Room y demuestra dayStart/límite diario/dueAt de reprogramación distintos entre NY y Sydney; suite 3796 PASS; smoke 25 OK. NO re-abrir la eliminación del default sin una preferencia de zona de usuario real que la motive. |
| 2026-08-20 (c.778) | **"esta semana" en la búsqueda es la semana CALENDARIO completa (lunes→domingo) también para tareas pendientes (dueAt/startAt) — se abandona el arranque en hoy.** La asimetría THIS_WEEK documentada OPEN en c.777 se resuelve del lado de la búsqueda (NO recortando la agenda del asistente). | La búsqueda escondía una tarea pendiente vencida a principios de ESTA semana: el asistente sí la listaba (rango lun→dom, c.261) y "vencidas" no la recuperaba acotada (OVERDUE no acota). La agenda, LAST_WEEK, NEXT_WEEK y THIS_MONTH (c.139) ya cubren su período completo incluido el pasado — la semana pendiente era el único rango asimétrico. La lectura honesta de "qué tengo esta semana" incluye la deuda de principios de semana; el solape con OVERDUE es deliberado (ya existía entre "este mes" y "vencidas"). | Contrato: THIS_WEEK = [lunes, domingo] para dueAt/startAt/completedAt; `anchorMatchesScope` pierde `fullCalendarWeek`. Una pendiente vencida el lunes sale por "esta semana" Y por "vencidas". TDD: 3 tests reescritos + 2 nuevos (solape OVERDUE + guard completadas). Suite 4839 OK. NO re-abrir salvo evidencia REAL de daño (ruido percibido); si se quisiera "qué me QUEDA esta semana" (sólo futuro), la vía sería una consulta NUEVA, no revertir la membresía honesta. |
| 2026-08-24 (c.959) | **Doctrina del despoje trailing temporal**: cada forma canónica nova (`medianoche|mediodía`), como la rama numérica («a las »), DEBE consumir su conector («al »/「a la 」) en `stripTrailingTemporalResidue`; la alternativa con-conector va primero, la desnuda («entrega medianoche» sin «a») sigue protegida. | La rama numérica despoja «a las 3» completa; la canónica despojaba sóla la palabra → «al» huérfana, título visible roto (P2). Simetría: alternativas más largas primero. | Cierre formal — la familia canónica queda en el mismo contrato; futuras formas canónicas (si aparecen) DEBEN adoptar la misma plantilla y TDD-RED exacto. Suite 6660 PASS, dominio 25/25, autom 9/9. NO VERIFICADO Android/gradle/lint/assemble/UI/Room (sin SDK). |
| 2026-08-25 (c.960) | **Franja blanda (c.688) introducidores**: la alternancia `(?:por|en)` canoniza que el papel introductor de franja tolera tanto la forma canónica «por» como «en» en el despoje trailing; cualquier introductor nuevo debe entrar por alternancia explícita con TDD-RED exacto, no por guardia informal. | La familia «en» dejaba conector huérfano visible; ahora sigue la misma plantilla («en las mañanas» plural incluida). | Cierre formal — suite 6668 PASS, smokes 25/25+9/9. NO VERIFICADO Android/gradle/lint/assemble/UI/Room (sin SDK). |
