# Ordía Autonomy Roadmap

## Wave 1: Foundation + Design System
* Fix global typography and styling (`Theme.kt`, `Type.kt`).
* Update deprecated icons (use `AutoMirrored.Outlined.*` variants instead of `Outlined.*`).
* Update spacing and border radius in components (e.g. `OrdiaShapes`).
* Modernize empty states and screen headers.

## Wave 2: Home + Navigation
* Redesign `TodayScreen` to emphasize 'NOW', 'LATER', 'TODAY'.
* Modernize bottom navigation/rail behavior in `Navigation.kt`.
* Implement 'Adaptive Workspace' concept.

## Wave 3: Universal Capture
* Build Universal Capture overlay / input logic.
* Integrate NLP parser (`NaturalTaskParser.kt`) more deeply.
* Add intelligent suggestions.
...
