<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- previewAdvanced: advanced permissions for overlay, notifications and IME -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application>
        <!-- Floating virtual companion (overlay) -->
        <service
            android:name=".overlay.GuardianOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-enabled animated virtual companion for productivity, quick capture and focus" />
        </service>

        <!-- Notification listener for contextual attention -->
        <service
            android:name=".context.OrdiaNotificationListenerService"
            android:exported="true"
            android:label="@string/notification_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Keyboard IME for contextual text capture -->
        <service
            android:name=".ime.OrdiaKeyboardService"
            android:exported="true"
            android:label="@string/ime_service_label"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/ordia_ime" />
        </service>

        <!-- External confirmation for contextual suggestions -->
        <activity
            android:name=".context.external.ExternalConfirmationActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:theme="@style/Theme.Ordia.QuickCapture" />

        <receiver
            android:name=".context.external.ExternalConfirmationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.ordia.app.action.ADD_SUGGESTION" />
                <action android:name="com.ordia.app.action.IGNORE_SUGGESTION" />
                <action android:name="com.ordia.app.action.POSTPONE_SUGGESTION" />
                <action android:name="com.ordia.app.action.OPEN_SUGGESTION" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
