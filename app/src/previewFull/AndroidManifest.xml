<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- previewFull: advanced permissions for overlay, notifications and APK self-update -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application>
        <!-- Floating virtual companion (overlay) -->
        <service
            android:name=".overlay.GuardianOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-enabled animated virtual companion for productivity, quick capture and focus" />
        </service>

        <!-- Notification listener for contextual attention -->
        <service
            android:name=".context.OrdiaNotificationListenerService"
            android:exported="true"
            android:label="@string/notification_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Keyboard IME for contextual text capture -->
        <service
            android:name=".ime.OrdiaKeyboardService"
            android:exported="true"
            android:label="@string/ime_service_label"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/ordia_ime" />
        </service>

        <!-- APK self-update infrastructure.
             Usa una subclase propia (UpdateFileProvider) en vez de
             androidx.core.content.FileProvider para que el manifest merger no
             lo fusione con el FileProvider de adjuntos del manifiesto main
             (mismo android:name, distintas authorities → conflicto de merge). -->
        <provider
            android:name=".updates.UpdateFileProvider"
            android:authorities="${applicationId}.update-files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/ordia_update_paths" />
        </provider>

        <activity
            android:name=".updates.UpdateInstallActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:theme="@style/Theme.Ordia.QuickCapture" />

        <receiver
            android:name=".updates.UpdateDownloadReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
