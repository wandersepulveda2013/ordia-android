Trabaja directamente en este proyecto local:
C:\Users\wsepulveda\Documents\GitHub\ordia-android

OBJETIVO
Aplicar, verificar y subir Ordia 3.0 a GitHub de forma segura, sin perder cambios locales, sin exponer secretos y sin afirmar que algo pasó si no fue comprobado.

INSTRUCCIONES OBLIGATORIAS
1. Inspecciona primero `git status`, la rama actual, `git remote -v`, el último commit y cualquier stash existente. No borres ni sobrescribas trabajo del usuario.
2. Localiza en Descargas la carpeta extraída más reciente llamada `ordia-3.0.2-instalador-corregido` o el archivo `APLICAR_ORDIA_3.ps1`. Verifica primero su sintaxis con:
   powershell.exe -NoProfile -Command "$errors=$null;$tokens=$null;[System.Management.Automation.Language.Parser]::ParseFile('<RUTA_COMPLETA_AL_PS1>',[ref]$tokens,[ref]$errors)|Out-Null;$errors|Format-List"
   No ejecutes el instalador si aparece cualquier error.
3. Ejecuta `INICIAR_ORDIA_3.bat`. Si el instalador falla, lee el mensaje completo, `ORDIA-3.0-RESULTADO.txt` y el log generado. Corrige la causa real en el repositorio o en una copia de trabajo; no desactives pruebas, lint, hashes, validaciones de firma ni controles de seguridad para hacerlo pasar.
4. Confirma que el código aplicado corresponde a Ordia 3.0 y que incluye guardianes virtuales, atención contextual local y confirmada, restauración segura, recordatorios endurecidos, actualización validada y CI sin secretos en pull requests.
5. Ejecuta desde la raíz del repositorio:
   .\gradlew.bat clean test lintDebug assembleDebug --stacktrace
   Guarda la salida completa. No continúes si falla una prueba, lint o compilación.
6. Verifica que exista `app\build\outputs\apk\debug\app-debug.apk`, que su tamaño sea mayor que cero y calcula su SHA-256.
7. Usa o crea la rama `feature/ordia-3.0`. Si ya existe, comprueba que pertenezca a este trabajo; si no, crea una rama segura con fecha, por ejemplo `feature/ordia-3.0-YYYYMMDD-HHMM`. Nunca hagas force-push a `main`.
8. Revisa `git diff` y excluye keystores, contraseñas, archivos `.clixml`, APK locales, logs, carpetas de build y secretos. Asegúrate de que `.gitignore` los cubra.
9. Crea un commit claro, por ejemplo:
   `Build Ordia 3.0 with private contextual attention and virtual guardians`
10. Haz push de la rama al remoto `origin` únicamente si apunta exactamente a `wandersepulveda2013/ordia-android`. No publiques en otro remoto.
11. Si GitHub CLI está autenticado, abre un pull request hacia `main` con resumen funcional, pruebas ejecutadas, riesgos, migraciones y SHA-256 del APK. Si no está disponible, entrega la URL exacta para abrir el PR manualmente.
12. Revisa GitHub Actions. No fusiones si algún check falla. Si todos los checks pasan y no hay cambios inesperados, deja el PR listo para fusionar; no publiques una Release ni reemplaces `main` silenciosamente.
13. Al terminar, informa con evidencia:
   - rama y commit;
   - URL del push o PR;
   - resultado exacto de test/lint/build;
   - ruta y SHA-256 del APK;
   - archivos modificados;
   - cualquier punto que no pudiste completar.

REGLAS
- No inventes resultados.
- No elimines funcionalidades existentes para resolver errores.
- No uses `--force`, `--no-verify`, `-x test`, ni desactives lint.
- No guardes claves o contraseñas en Git.
- No mezcles este trabajo con cambios no relacionados.
- Corrige todos los errores encontrados antes de subir la rama.
